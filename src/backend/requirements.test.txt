# T4120: backend test deps — baked into the /dotask task image ONLY (task.Dockerfile),
# never into requirements.prod.txt (Fly prod must not ship test deps).
# Lets `python run_tests.py` / pytest run in-container with no manual pip install.
pytest==8.3.4
pytest-asyncio==0.25.3
pytest-mock==3.14.0
