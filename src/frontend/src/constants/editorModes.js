/**
 * Editor mode constants - the main workflow stages of the video editor
 */
export const EditorMode = {
  ANNOTATE: 'annotate',
  FRAMING: 'framing',
  OVERLAY: 'overlay',
  PROJECT_MANAGER: 'project-manager',
};

export default EditorMode;
