# Dependencies
node_modules/
.pnp
.pnp.js

# Production
dist/
build/

# Testing
coverage/

# Misc
.DS_Store
.env
.env.staging
.env.production
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDEs
.vscode/
.idea/
*.swp
*.swo
playwright-report/
test-results/
dev-dist/

# T5450 dev-only harness: ephemeral sample video generated by the QA spec
public/overlaydiag-sample.mp4

# T6230 real-browser SW-gate test: the two production builds it compiles into a
# scratch dir (beforeAll) + the deliberate build-B marker it injects into public/
# (build-then-remove). Both are ephemeral test artifacts, never committed.
e2e/.sw-gate-builds/
public/__t6230_sw_gate_marker__.js
