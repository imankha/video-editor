# Project Plan

> **DONE rows are archived** to [PLAN-archive.md](PLAN-archive.md) (moved 2026-08-03) to keep this file short. When a task hits DONE, move its row there under the same headings.

## Current Focus

**TOP PRIORITY (2026-07-17): Durability Hardening Campaign.** The most critical un-worked bugs
all share one theme — **user writes are not durable or authoritative and can be silently lost or
clobbered across machine cycles and concurrent tabs** (the deepest layer, data-integrity/sync).
This was escalated after **T5310 proved the failure mode is live on prod**: arshia lost 2 profiles
to a create-without-durable-sync race (repaired 2026-07-17; source fix still owed), and **again on
2026-07-24** — the same user lost a 400-credit grant AND 5 published reels to the force-push
clobber, **both on the single-server stack** (deploy machine-replacement + R2-error stale
force-push — see the epic re-escalation). Point-fixes landed (branch `fix/admin-credit-grant-r2-sync`:
grantee-DB sync, `TrackedConnection` owner tracking, move_reels `require_fresh`); the general
guarantees are **T4310** (upload-side CAS) + **T4315** (restore-if-newer) + **T5840** (credits →
Postgres, standalone). Campaign =
the [durability-sync epic](tasks/durability-sync/EPIC.md) (T4310–T4360) **+** its siblings in other
epics: **T4400** (backend-authoritative export — client state clobbers surgical edits, DB≠video),
**T2260** (data-loss detection/recovery on reconnect), and the **T5310 source-fix** (durable sync
on `POST /api/profiles` create — folds into T4320). Sequence: **T4320 first** (prod-proven, lowest
complexity, no design gate) → **T4310** + **T4400** (both Impact 9, Architect design gate) → the
rest. This is a sequenced campaign with design approvals, NOT a parallel fan-out.

**UI Runway (user-ordered 2026-08-03):** every near/mid-term task that visibly changes the UI lands FIRST, then the tutorials are reshot once against settled screens — see the UI-runway note at the end of Milestone TOP below.

**Phase: Feature** — Season Highlights & Collections epic: My Reels becomes the curation home (annotate → publish → rank → share). Spec: [season-highlights-spec.md](season-highlights-spec.md) · Tech notes: [season-highlights-tech-notes.md](season-highlights-tech-notes.md)

**Landing Page:** Already live at `reelballers.com`

### Production Reported Bugs

Bugs reported by users on production. Populated from Postgres `bug_reports` table via task board API. Use "Copy Kickoff Prompt" to investigate.

*All tasks in this section are complete — rows archived to [PLAN-archive.md](PLAN-archive.md).*

### Staging Reported Bugs

Bugs reported or discovered on staging. Populated from Postgres `bug_reports` table via task board API. Use "Copy Kickoff Prompt" to investigate.

*All tasks in this section are complete — rows archived to [PLAN-archive.md](PLAN-archive.md).*

### Milestone TOP: Durable Sync — stop active prod data loss

| ID | Task | Impact | Cmplx | Pri | Status | Migr | Description |
|------|------|------|------|------|------|------|------|
|  | **[Player Intro](tasks/player-intro/EPIC.md)** | 7 | 6 | 1.2 |  |  | NEW feature 2026-07-15: broadcast/recruiting-style player intro card (photo + player facts) prepended to shared collections and multiclip videos. Visual target = C:/Users/imank/Videos/Captures/stafford intro.mp4 (animated stats card -> white flash -> footage). Reuses branded_outro.py card+concat engine (prepend-first). 9 children across 6 waves + 1 off-wave (T5225) - EPIC.md is the authoritative ordering. COPPA research: likely N/A (parent-provided data on an adult-directed app) but adopt a children's-data posture anyway - minimize (grad-year not DOB), consent attestation, encrypt DOB if kept, R2 SSE baseline, retention/deletion, public-exposure warning, NO face-recognition. See EPIC.md. |
| T5180 | ↳ [Rich text engine](tasks/player-intro/T5180-rich-text-engine.md) | 7 | 5 | 1.4 | STAGING | [ ] | Wave 1. TextSpec + 4-font catalogue (anton/oswald/graduate/playfair) + text_render.py + RichText.jsx + backend/frontend parity test. Foundation for T5205 and T5225; no user-visible feature of its own. Merged 6a7a2934. |
| T5190 | ↳ [Card image upload + consent](tasks/player-intro/T5190-card-image-upload-consent.md) | 7 | 5 | 1.4 | STAGING | [x] | Wave 1. Image upload under the per-profile intro/ R2 prefix + parental-consent attestation + position/class/team profile facts. Scope REVERSED by epic decision 3 - 3 structured fields, not the original 10, and no DOB. Merged 9696365f. |
| T5195 | ↳ [Card library: schema, CRUD, default](tasks/player-intro/T5195-intro-card-library.md) | 7 | 5 | 1.4 | STAGING | [x] | Wave 2. intro_cards table (profile_db v034) + final_videos.intro_card_id, REST CRUD with surgical updates, exactly-one-default in a single transaction, composition DERIVED from (has_photo, field count) never stored. Merged 139d570c - needs POST /api/admin/migrate after each deploy. |
| T5205 | ↳ [Card editor UI](tasks/player-intro/T5205-card-editor-ui.md) | 8 | 6 | 1.3 | STAGING | [ ] | Wave 3. Library grid + editor stage + per-slot text controls + browser motion preview. Merged 2a3594a6. The stage composes what player_intro.py renders (treatment + focal-framed photo in its composition rect + scrim + RichText per slot at contract geometry), pinned by a _merge_spec parity test. SCOPE: per-slot size/align controls REMOVED - the contract makes layout geometry-owned, so those controls would have edited fields the renderer ignores; reintroduced as relative overrides in T6520. The first screen where a card can actually be built, so it is the epic's first real payoff. Reuses the shared TextSpecEditor that T5225 defines. Depends on T5195. |
| T5210 | ↳ [Card render engine](tasks/player-intro/T5210-intro-card-generation.md) | 7 | 6 | 1.2 | STAGING | [ ] | Wave 3. app/services/player_intro.py mirroring branded_outro.py: card row + PNG text layers -> animated MP4, probe-matched, per-format cache, non-fatal. Motion + professionalism IS the deliverable (user direction) - a static frame is not acceptable. Pitch/position diagram DROPPED. Merged 9c603f6f - also lands the SHARED contract (slot geometry + motion timings + treatment colours; Python source of truth, generated JS mirror, parity test) that T5205 consumes. QA built 24 real MP4s across 4 compositions x 3 treatments x 2 aspects. |
| T5215 | ↳ [Attachment + resolution](tasks/player-intro/T5215-intro-attachment.md) | 7 | 5 | 1.4 | STAGING | [ ] | Wave 4. Wire final_videos.intro_card_id (column already shipped in v034) + collection-definition field + resolution helper + reel/collection pickers. NULL = inherit the profile default, 0 = explicitly no intro - never collapse the two. Depends on T5195. |
| T5220 | ↳ [Apply the intro at every egress](tasks/player-intro/T5220-add-intro-integration.md) | 7 | 5 | 1.4 | STAGING | [x] | Wave 5. Implemented 2026-08-08: owner download composes intro+outro in one ffmpeg pass, share GET carries the resolved intro payload, new GET /api/shared/{token}/download, collection playback pre-roll, edge-page hand-rolled DOM intro, public-exposure notice. Design doc + 3 open questions approved 2026-08-08. Fixed a real bug found by QA (sqlite3.Row.get() crash on composed download, mid-stream hang) + 2 CI-only regressions (test-user cleanup registry, a payload-shape pinning test). Merged to master 2026-08-08 (`0951c6ff`) - this merge happened before the user's own hands-on re-test, which then found 2 more real bugs live on master: share playback didn't auto-resume after the intro (autoplay attribute toggle on an already-mounted <video> is a no-op), and desktop "Share" popped the native OS share sheet instead of ShareModal (some desktop Chromium builds expose navigator.share; fixed to check the isMobile-gated capability, and desktop now opens ShareModal directly per user decision). Both fixed, live-verified in a real browser (screenshot + console evidence), fix-forward merged to master `25663d26`, Master CI green. A 3rd reported issue (owner in-app Play button doesn't show the intro) is a real scope gap, not a regression - filed as [T6700](tasks/player-intro/T6700-owner-inapp-playback-intro.md), needs an Architecture design gate. |
| T5225 | ↳ [Overlay text layer](tasks/player-intro/T5225-overlay-text-layer.md) | 7 | 6 | 1.2 | STAGING | [ ] | Off-wave - needs only T5180, so it runs early. Pause-during-text was raised against this layer and is DEFERRED as T6490 (Post Launch), design already settled. Timeline text layer + clip-boundary snapping + burn-in in BOTH render loops. Design approved 2026-08-04. Merged 2e3b532e; Modal (reel-ballers-video-v2) redeployed BEFORE the merge so both render paths agree. Integrates into the live OverlayMode.jsx - OverlayTimeline.jsx is dead code. |
| T5230 | ↳ [Children's-data compliance hardening](tasks/player-intro/T5230-childrens-data-compliance.md) | 7 | 4 | 1.8 | STAGING | [x] | Wave 6, gates public launch. Merged to master 2026-08-08 (`42abf80d`, auto-deploys staging). Consent-gate on card creation, privacy export+purge now cover intro_cards + R2 intro/ images (proven against a real object), no-biometrics guardrail test, grad-year/DOB clause satisfied by absence (no DOB field exists), privacy-policy draft copy (flagged for human/attorney sign-off). Playwright-verified live before merge; found + fixed a real UX bug (consent-gate 403 silently swallowed by the frontend, now throws + toasts). One CI-runner Postgres flake (test_vacuum_on_signout) hit and confirmed via same-SHA rerun, logged to known-failures.md. 2 of 6 acceptance criteria intentionally partial: public-exposure-warning verification PENDING on T5220 (still unmerged as of this merge), policy copy is a DRAFT not self-certified. |
| T5200 | ↳ [Player cut-out ("player outline")](tasks/player-intro/T5200-player-cutout.md) | 6 | 5 | 1.2 | TODO | [ ] | DEPRIORITISED by epic decision 11 - a card works with a plain photo, so this no longer blocks T5210. Optional background removal (rembg/u2net-class) cached at image_cutout_key, with upload-your-own-PNG fallback. Matting NOT face-recognition. 2026-08-09: explored to the Architecture design gate - the storage column, renderer and resolution seam already exist and already prefer a cutout when present, only the segmentation dependency + generation gesture are missing (see the worker's design doc if resumed). User reviewed the 3 open design questions (matting approach, gesture placement, committing the onnx model) and asked to drop the task entirely rather than pick an option - not started, container torn down. |
| T4945 | [Download Collection as Stitched MP4 (with outro)](tasks/T4945-collection-download-stitched-mp4.md) | 6 | 5 | 1.2 | TODO | [ ] | From T3950 pivot (2026-07-12): user wants compilations burned on download, but NO collection download exists - collections are composited playback only. New feature: stitch member reels in rank order into one MP4 ending with exactly one branded outro (reuse T3950 branded_outro engine). Design decides sync-vs-job, caching (derived, disposable), normalization (T4140 pattern). |
| T5140 | [Reshoot Tutorial Videos (carousel/poster UI + Team layer; slow narration baked in, 1x playback)](tasks/T5140-reshoot-tutorial-videos.md) | 7 | 4 | 1.8 | TODO | [ ] | LAST task of this milestone — reshoot ALL 4 quest tutorials once the UI-visible runway above has landed. The 2026-07-18 recordings predate the poster-tile home (T5672/T5673/T5681), the Team / My Athlete layer + Team-only teammate tagging (T5700/T5725/T6400), the Google-Docs share modal + public game link (T5720/T5740), export cost on the button (T5780/T5790), the looping "Play spotlight" control (T5370), and the My Reels tile/CTA rework (T5678/T6180/T6300) — every quest shows dead UI. Also bakes the slow pace into the AUDIO (tts rate ~ -10%) so playback defaults to 1x: after upload, flip DEFAULT_RATE 0.8 -> 1 in TutorialVideoModal.jsx + landing TutorialModal.tsx. Work happens in C:/Users/imank/Videos/Captures/ReelBallersTutroials (read its CLAUDE.md first); the task file carries the per-quest drift table, recommended talk-track edits, and the capture-spec triage list. |

All near/mid-term work that **visibly changes the UI** lands first; the tutorial reshoot ([T5140](tasks/T5140-reshoot-tutorial-videos.md)) closes the milestone so the videos capture settled screens. Deliberately NOT gating the reshoot: T5750 (evidence-gated), Multi-File Ingest & Prep (deprioritized 2026-07-31), Movement Tracking + Dual-Camera milestones (post-reshoot features — when one lands, the affected quest gets a touch-up reshoot, not a full redo), the UI Pass epic row (all children shipped; row open only for ledger reconciliation), and all refactor/perf/durability work (no visible UI change). If Player Intro slips badly, drag T5140 above it on the board — the tutorials don't demo intros.

**Queued directly after the reshoot (user-ordered 2026-08-04):** the [JIT Migration epic](tasks/jit-migration/EPIC.md) (T5081-T5089). Not a UI-runway member — it sits here because the user placed it immediately after T5140 and ahead of Tile Video Preview.

## Single-Server Priority (2026-07-18; durability re-escalated 2026-07-24)

The stack is currently ONE server. **Correction (2026-07-24): the durability epic is NOT safely
deferred to multi-server.** It was parked on the belief that its failure needs two live machines,
but arshia lost a 400-credit grant AND 5 published reels on the *single-server* stack — via deploy
machine-replacement (a local-only write dies when the machine is swapped) and R2-error stale
force-push (a writer builds on a stale local copy and overwrites newer cloud state). So the
*silent-data-loss* half of the old epic is now the **TOP** milestone below (the deeper
two-machines-both-live race in T4310 is still partly multi-server, but its single-server face is
real and included). The lower-priority format/concurrency tasks split out to the
[Write Correctness epic](tasks/write-correctness/EPIC.md).

### Milestone A: Do Now — user-reported single-server bugs (no blockers)

The class that has twice destroyed real prod data (arshia's profiles 2026-07-17; his credits + 5
reels 2026-07-24). Point-fixes already landed (branch `fix/admin-credit-grant-r2-sync`:
grantee-DB sync, `TrackedConnection` owner tracking, move_reels `require_fresh`, + the 5 reels
recovered); these tasks are the general guarantees behind those point-fixes.

| ID | Task | Impact | Cmplx | Pri | Status | Migr | Description |
|------|------|------|------|------|------|------|------|
| T6350 | [move-to-profile half-applies: target keeps the reels, user is told "not moved"](tasks/T6350-move-reels-half-apply-on-sync-failure.md) | 8 | 4 | 2.0 | TODO | [ ] | **Found 2026-08-02 by running T5810's step-4 staging verification.** move-to-profile returned 503 `sync_failed` ("Your reel was not moved") but the target profile had ALREADY committed durably - verified by reading staging R2 directly, not just the API: target profile.sqlite held games=1 + final_videos=2 (the reference game AND both reels) while the source still had both reels. The reels existed in BOTH profiles and the user was told nothing happened, violating T4850's all-or-nothing contract with an actively false message. NOT a T5800/T5810 logic fault - the epic behaved perfectly in the same run (one reference row for two reels of the same game = dedup, is_reference true, correct source pointers, no expiry). Target-first ordering is correct per invariant 6b; the hole is that a PHASE-2 (source-side durable_sync) failure has no compensating action and no honest message. Decide one of: compensate (undo the target write), roll forward (retry the source delete, report success-with-pending-cleanup), or report honestly (say it was copied but the original could not be removed, with an idempotent retry). Smallest correct option is honest reporting; the message must never claim a state the DB contradicts. Repro + exact ids in the task file; staging was restored. Possible trigger: brand-new target profile (db-version=3) vs long-lived source (db-version=2698), or residual R2 flakiness from T6340. |
| T6345 | [postgres migration runner skips version gaps permanently (max vs set membership)](tasks/T6345-postgres-migration-runner-skips-version-gaps.md) | 8 | 2 | 4.0 | TODO | [ ] | **Sibling defect filed out of T6340 (2026-08-02); a separate runner bug, its own regression test.** `MigrationRunner.get_pending` (`app/migrations/base.py:38`) computes pending as `version > MAX(applied)`. For postgres, `get_current_version` is `SELECT MAX(version) FROM schema_migrations` - a maximum, NOT a set-membership test - so a migration numbered BELOW an already-applied version is skipped SILENTLY and permanently, no error, no log. It really happened: T5770 correctly numbered its migration v022 (v020/v021 reserved by then-unmerged Share the Game branches) and merged first, leaving dev + staging at `applied: [1..19, 22], GAPS: [20, 21]` while `run_all_migrations()` reported success. `shares.share_type` never gained `'game_link'`, so every public-game-link creation would 500 on the CHECK constraint on an env reporting itself fully migrated. Fixed by hand on dev + staging (both now contiguous 1-22); the runner defect is untouched and will recur the next time branches merge out of numeric order - which the branch-numbering discipline actively encourages. FIX: compare against the applied SET (`SELECT version FROM schema_migrations`), not the max. Regression test: seed `[1..19, 22]`, assert v020/v021 still pending. Also assess whether the `PRAGMA user_version` tracks (user_db/profile_db) can gap the same way - they hold a single integer so they only move forward, but a merge landing a lower version has the same silent-skip shape. |
| T6410 | [migration swap discards unsynced local writes - and T6340 now publishes the result](tasks/T6410-migration-swap-discards-unsynced-writes.md) | 6 | 3 | 2.0 | TODO | [ ] | **Filed 2026-08-03 from T6340 review (out-of-scope note).** `_migrate_profile_db` decides swap-vs-keep by SCHEMA version (`PRAGMA user_version`), which says nothing about data recency - a local copy with unsynced writes (pending/failed upload, `.sync_pending` set) usually has the SAME schema version as R2, so the else-branch swaps R2 bytes over it and discards the pending writes. Pre-existing, but T6340 raised the stakes: the swap now records a confirmed baseline and uploads at r2_version+1, making the discard canonical (before, CAS refused and the loss stayed local). FIX: refuse the profile (distinct status, mirror of wal_busy) when a pending-sync signal is present; never merge, let the retry flush first. Test: seed unsynced local writes + pending marker, assert writes survive and nothing uploads. |
| T6520 | [Per-slot size and alignment control on an intro card](tasks/T6520-card-slot-size-align-overrides.md) | 5 | 3 | 1.7 | TODO | [ ] | **Follow-up from T5205's merge 2026-08-04.** T5205 shipped WITHOUT the size (S/M/L/XL) and alignment rail controls its spec asked for - correctly, because the shared contract makes layout geometry-owned and those controls would have edited fields the renderer ignores, making the preview lie. Give them back WITHOUT taking layout ownership from the contract: size becomes a MULTIPLIER on the geometry size (aspect-agnostic by construction, same reason the photo uses a focal point not a crop rect), alignment becomes an override inside the slot's maxWidth box. Both resolved in the ONE shared _merge_spec + its JS mirror, parity-tested. Known worst case to test: Graduate + a long club name at 16:9 recruiting already wraps to 2 lines at x1.0. |
| T6500 | [A font catalogue for overlay text](tasks/T6500-overlay-font-catalogue.md) | 6 | 4 | 1.5 | TODO | [ ] | **User request 2026-08-04.** The 4 faces (anton/oswald/graduate/playfair) were chosen for INTRO CARDS - full-frame, mostly-caps display. Overlay text is short callouts burned over live footage, read at a glance, often small on a busy background. Main design question: split the catalogue by context or grow one list - decide against RENDERED samples, not prose. T5180's standing note said add a neutral sans "THEN, not speculatively" if small print reads badly in real use; overlay use is that THEN. Both sides must ship each face (parity test is the proof). Check lowercase + a long string - neither has ever been tested. |
| T6480 | [Overlay text editor reads bright-on-bright](tasks/T6480-overlay-text-editor-contrast.md) | 5 | 2 | 2.5 | STAGING | [ ] | **Merged 2780032d** (with T6610 - same rail). Root cause was HOST, not component: the shared dark-tuned TextSpecEditor sat on the Overlay screen's LIGHT glass panel, which is why it reads fine in the card editor and only fails here. Fixed host-side - the Edit Text panel is now dark glass (bg-gray-900/85 border-gray-700) matching the card editor's rail; the shared component is BYTE-UNCHANGED, so the card editor cannot regress (the trap was darkening labels in the shared component, which would have inverted the bug there). Measured, not asserted: old surface <4.5:1, fixed overlay + card + footer all >=4.5:1. **User report 2026-08-04 with screenshot.** The Edit Text rail sits on the Overlay screen's light-purple panel and several controls are light-on-light; worst is the COLOR row rendering as a near-white swatch on a pale panel, effectively invisible. Labels and slider tracks need the same pass. TextSpecEditor is SHARED (T5225 built it, T5205 extends it) so fix must not regress the card editor. Real-browser screenshots are the evidence - jsdom cannot prove a contrast fix. |
| T6510 | ["Preview image" - rename it, and make it a frame choice not an upload](tasks/T6510-rename-cover-photo-preview-image.md) | 6 | 3 | 2.0 | STAGING | [ ] | **Merged f6b6c4fc.** Rename landed separately in 91629fc0; the behavioural half removes the upload write path, always resolves a default frame, SHOWS the actual still (grabbed from a hidden preload=metadata video so it never yanks the user scrub position; canvas draw is display-only so no CORS), and lets the user move it. Legacy poster_source=upload reels GRANDFATHERED - read path kept and commented, one-way "Use a frame instead" switch. Surfaced T6550. **Two user requests 2026-08-04/05, same component, filed together to avoid two passes.** (A) "Cover photo" reads like an album cover; its real job is the LINK PREVIEW shown when a share link unfurls - rename to "Preview image" + hover copy saying where it appears, including the poster marker's aria-label. (B) REMOVE the upload path: the preview image must always be a FRAME from the reel - default to one, SHOW the user which frame was chosen (not just a marker position), let them change it. Decision needed on existing `poster_source === 'upload'` reels - grandfathering is likely right, which means the upload READ path survives while the WRITE path goes (comment it so nobody deletes it as dead). Rename half is label-only; the frame-only half is real behavioural work. |
| T6580 | [Card editor: bigger card, readable controls, treatments that differ, order-dependent render bug](tasks/T6580-card-editor-presentation-and-order-bug.md) | 7 | 4 | 1.8 | STAGING | [ ] | **Merged d91a11c7. ITEM 1 IS NOT CLOSED BY THIS MERGE** - the scrim/blur/panel change landed and correctly dims the margin, but the reel-cards-on-top defect is Z-ORDER and closes only when [T6600] lands; a69f1cba records that limitation in the code rather than leaving a comment claiming a fix. Item 5 (order bug) root cause was NEITHER supervisor hypothesis (no ResizeObserver latch, no measureText font-timing): geometry slots are ORDINAL and shown_fields was built in CLICK order, so the same facts ticked in a different sequence landed on different lines - now rebuilt in canonical FACT_SLOTS order every toggle, regression test pins both order axes at desktop and 375px. Item 4 shipped as B+C (user decision from rendered samples): each treatment owns a solid lower-third BAND (explicit heightFrac in the contract, gold desaturated, none for photo-forward, gated to hero/broadcast) plus a photo GRADE (tint + vignette with explicit extent so CSS and PIL compute the same pixels) - all in the shared contract, parity test extended to value-compare band/photoMood and bandCompositions. Items 2/3 (bigger card, readable controls) done without regressing T6540 density. **Staging feedback 2026-08-05, 4 items.** (a) The modal does not suppress the page - reel cards with bright cyan buttons are legible behind it; T6540 already went /70->/80 and it is not enough. (b) The card is too small (STAGE_MAX_H 420 / MAX_W 480) - it is the thing being designed; must NOT regress T6540 measured scroll density. (c) Control text (text-xs/text-[11px]) is hard to read. (d) Changing treatment barely changes anything on a photo card - the "how it looks" axis needs to actually own something (accent, scrim, band, tint) EXPRESSED IN THE SHARED CONTRACT so preview and export agree; ui-designer + samples before committing. (e) BUG: the card renders differently depending on whether you click aspect or facts first - REPRODUCE with a two-order diff before fixing; leads (not conclusions) are the ResizeObserver width latch and RichText canvas measureText before/after webfont load. |
| T6570 | [Card title comes from the profile, not a text box](tasks/T6570-card-title-from-profile-full-name.md) | 7 | 3 | 2.3 | STAGING | [ ] | **Merged d91a11c7.** Full Name lives in per-profile user_settings (intro_full_name.{profile_id}) - NO migration, no column, as the task predicted - and is deliberately kept OUT of INTRO_FACT_FIELDS so it never counts toward composition. Title resolves from the profile in BOTH preview and export; legacy title_text is a grandfathered override and new cards set none, so they always follow the profile. **The two later additions shipped in the same branch:** a free-text SUBTITLE (profile_db **v035**, slot geometry recovered from the approved commit c806e2a5 rather than reinvented, orthogonal to composition so it never changes the derived layout, read AND write column-guarded for the deploy->migrate window) and STOP NAMING THE LAYOUT (badge removed, caption now 'The layout adapts to the facts you show.'). **v035 does NOT auto-run - trigger POST /api/admin/migrate after deploy.** **User request 2026-08-05.** The name is a property of the athlete, not of a card - the same reasoning that moved position/class/team to the profile in epic decision 3. Add "Full Name" following the EXISTING precedent: those facts live in the per-profile user_settings key/value table, NOT as profiles columns, so this needs NO MIGRATION. Do not reuse profiles.name (that is the profile switcher label). Remove the card editor title box; the title renders from the profile in preview AND export; unfilled behaves like any unfilled fact (inline prompt, omit-and-log). Decide + document what happens to legacy intro_cards.title_text. |
| T6560 | [Overlay: the preview image can be switched off, and one line of copy earns nothing](tasks/T6560-overlay-preview-image-and-copy.md) | 6 | 2 | 3.0 | STAGING | [ ] | **Merged a61c0dd8.** Fixed in TWO layers, the authoritative one being persistence: POST /poster-time now REQUIRES a finite non-negative time and 422s a null/missing body, so the clear-to-null path is gone and no UI path can blank the poster; reset-to-auto stays on /poster/revert, which regenerates a real frame. Interaction half: a pointerdown->up under a 4px threshold is a CLICK and commits nothing (previously ANY pointerup snapped the marker to the click X inside its 32px hit box, which is what cleared/moved the frame). Verified in a real browser: click, click-again-in-place, keyboard-activate, drag-release-in-place all no-ops; a deliberate drag still moves; reload persists; {time:null} and {} both 422. **Staging feedback 2026-08-05.** (a) BUG: clicking the poster marker on the timeline can clear the preview image. T6510 established there is NO valid "none" state - a default is always resolved. The consequence is invisible until someone shares the link and gets a blank unfurl. Marker must move but never clear; any reset returns to the AUTO frame, never to nothing. (b) Remove "Applies highlight overlay (H.264)" - it names a codec to a user with no decision to make; delete, do not replace. |
| T6610 | [Overlay text elements: drag to reposition, and a delete button you can hit](tasks/T6610-overlay-text-element-manipulation.md) | 6 | 3 | 2.0 | STAGING | [ ] | **Merged 2780032d.** Worker VERIFIED the current state first (existing T5225 lever spec 10/10) and confirmed the task's premise: multi-block, click-to-add, delete, select-drives-editor and lever resize all already worked, so only the body drag was built - no rebuild. moveTextBlock moves both edges together (duration preserved, clamp-only), leading-edge snapping reuses snapToBoundary/SNAP_PX, arrow-key nudge for a11y. Lever path stays separate (lever stopPropagations first) so a lever press RESIZES and a body press MOVES; sub-4px press is a click that selects. Persistence: local move per tick, EXACTLY ONE move_text_edge write on drag end, counted in the e2e spec, no new write path. Delete padding: lane h-14->h-28 with getTotalLayerHeight/label in lockstep so controls clear the scrollbar, 44px coarse hit box. **Declared limitation:** 375px screenshots captured but no-horizontal-overflow NOT asserted, because the textdiag harness is fixed-1000px and asserting there would measure the harness not the component (correct call - this is the failing-open harness class); change is vertical-only. **User request 2026-08-05 with screenshots.** Expectation is add / remove / drag-to-reposition text elements in a text region, with Edit Text acting on the SELECTED element. **Most of that already works and must NOT be rebuilt** - multiple blocks, click-to-add (OverlayScreen.jsx:947), delete, selection driving the editor, and lever resize with boundary snapping all exist. The REAL gap is that only the two levers drag: grabbing the block BODY just selects it, so moving an element in time means dragging one edge then the other, changing its duration on the way. Add a body drag that moves start+end together preserving duration, reusing the existing computeSnappedTime/SNAP_PX mechanics (T5225 says explicitly this is not a new drag idiom), leading edge snaps, clamped to the region, lever-vs-body hit-testing preserved, ONE surgical write on drag END extending the existing onMoveTextStart/End path (no second write path, no useEffect). Second item: the delete control sits directly above the horizontal scrollbar so the pointer hits the scrollbar instead - add separation and a 44px coarse hit box (PosterMarkerLayer precedent), and check the other stacked per-block controls. Contrast complaint from the same report routed to T6480, not duplicated here. Real-browser evidence mandatory (drag work, T5380). |
| T6640 | [Cards that cannot be made ugly: template-owned typography + wrap-safe layout](tasks/T6640-cards-cannot-be-ugly.md) | 9 | 6 | 1.5 | TODO | [ ] | **User 2026-08-06, two card screenshots: "I don't like the overlap", "does this look professionally designed to you?", and the requirement - "the point of the templates is they all look professionally designed. The user shouldnt be able to make it ugly."** DESIGN-GATED. One root cause behind both complaints: the card system offers axes and hopes, it does not GUARANTEE a good result. (A) ACUTE: slot geometry places every slot at a FIXED fractional y (intro_card_geometry.py:144-190, hero 9:16 title y=0.40 subtitle y=0.50) assuming a ONE-LINE title, so a long name wraps and line 2 collides with the subtitle and fact lines - every athlete with a long name hits it. Needs MEASURED layout (reflow following slots from measured line counts and/or shrink-to-fit the title), computed IDENTICALLY in RichText.jsx and text_render.py or the parity gate breaks. (B) AMENDS EPIC REQUIREMENT 2 (epic decision 12): for CARDS the template owns font/colour/size/align/weight/shadow/stroke/spacing derived from the treatment; remove font + custom colour picker + swatches + shadow + stroke from the CARD rail (IntroCardRail already passes hideSize/hideAlign - extend that host-prop pattern, do NOT fork the shared editor). The Overlay text rail KEEPS full control - it is the user annotating their own video, no template promise. (C) Composition defects to answer in the design: flat hierarchy below the name, even vertical rhythm so nothing groups, saturated colour against a desaturated photo applied inconsistently within one line, hard 50/50 photo/panel seam with no bleed, no margin grid, photo not composed for its frame. (D) Existing cards store per-slot font/colour in text_elements - decide migrate-to-NULL (correct-data, the T6620 title_text precedent) vs preserve, no silent fallback; T5215 holds v037 so take v038+. Design gate must ship RENDERED SAMPLES: 3 treatments x 2 aspects x {short name, long wrapping name}. PII: the report screenshots show a real minor - never commit the name or photo. File ownership: T6630 owns TextSpecEditor.jsx while in flight. |
| T6630 | [Text elements: add, remove and drag in the most obvious way](tasks/T6630-overlay-text-add-remove-drag-ux.md) | 8 | 4 | 2.0 | TODO | [ ] | **User 2026-08-06, screenshot at 500% zoom: "what is supposed to happen when i click the text region?" The requirement is add/remove text elements from a text layer in the most obvious and intuitive way, plus move each element via drag.** T6610 shipped believing add/remove already worked and only body-drag was missing; the user can do none of the three. The capabilities EXIST in code - they are undiscoverable and partly unreachable. Root causes verified in code: (1) the add click target is a 40px `.text-track` (TextLayer.jsx:267) inside a 112px `h-28` lane (:264) - the lower ~72px is visually identical lane and completely inert, so a click low in the lane cannot do anything; (2) the only affordance, "Click to add text", is gated on `blocks.length === 0` (:272), so it vanishes after the first block and no other add control exists anywhere; (3) body drag IS fully wired (wrappedMoveTextBody OverlayScreen.jsx:982 -> OverlayModeView:808 -> OverlayMode:276 -> TextLayer draggingBody) but was verified HARNESS-ONLY via /textdiag.html + skipOnDeployedTarget - the same pattern that produced the 2026-08-05 false green - and the user reports it dead at 500% zoom. Design: mirror the existing prominent `+ Add Spotlight` button with `+ Add Text` (adds at playhead, reuses wrappedAddText - no second add path); extend the click target to the whole lane while keeping the button/lever guards; make the hint persistent; put a Delete action in the Edit Text rail where the user already is, keep the per-block trash, add Delete/Backspace on the focused block; REPRODUCE drag at 500% zoom before touching drag code (leads not conclusions: pointer capture vs the horizontally-scrolled container, re-render per commit=false move, trackRef measured while scrolled). Invariants: ONE surgical write per completed drag, lever-vs-body hit-test and the 4px click-vs-drag threshold unchanged. Harness-only verification is EXPLICITLY REJECTED. Also fix the lane/label height disagreement (lane `h-28` unconditional vs label `h-20 lg:h-28`, OverlayMode.jsx:182). |
| T6650 | [Deleting an intro card destroys the profile's intro photo](tasks/T6650-card-delete-destroys-profile-intro-photo.md) | 7 | 3 | 2.3 | TODO | [ ] | **User-hit data loss 2026-08-07, root-caused by the supervisor.** One R2 object, two owners: `introCardDefaults.js:57` defaults a new card's `image_key` to the profile's `introPhotoKey` (the SAME key, by epic decision 3b, test-pinned), and `intro_cards.py:331-333` hard-deletes that object when the card is deleted - while `user_settings.intro_photo_key` still points at it. Create a card, delete it, and the profile photo is silently destroyed; the Edit Profile thumb breaks and a presigned GET 404s. Verified live: key persisted for `66b98be0...png`, full paginated R2 listing under the profile's `intro/` prefix returns 6 objects ALL dated 2026-08-04 - today's object is gone. Contradicts the ownership contract asserted in `profiles.py:301-316` ("this endpoint remains the sole owner of the R2 object"). **The handoff's upload-ordering theory is REFUTED and must not be re-derived**: `store_intro_image` runs before `set_intro_photo_key`, returns None on failure (500), and `retry_r2_call` re-raises when retries exhaust - a failed PUT cannot produce a persisted key. Fix is ownership, not ordering: either the card delete checks for other referencing cards + the profile key before deleting (recommended, no schema change), or defaulting COPIES the object to a card-scoped key. Must not weaken T5230's compliance purge, which deletes intentionally. Dangling keys need a visible "photo missing" state, and repair CLEARS the key - never re-points it at the surviving 2026-08-04 photo, which is a different image. **SECOND HALF confirmed by the user the same morning:** they re-uploaded (object landed 05:39:30Z, verified) and the CARD still showed broken - cards store a SNAPSHOT of the key (`introCardDefaults.js:57`, `IntroCardRail.jsx:372`), so a profile re-upload never propagates, and the card renders a bare `<img>` with no missing-object state. User recovered via Remove -> "Use profile photo" and **approved that flow - do not redesign it**; note it is gated on `!hasPhoto` so a broken card cannot refresh in one click. |
| T6620 | [Shadow blur inert, profile name blocked by legacy title, rename Title to Athlete Name](tasks/T6620-shadow-blur-inert-and-title-override.md) | 7 | 2 | 3.5 | STAGING | [ ] | **Merged 0c376d42. NEEDS POST /api/admin/migrate (profile_db v036).** **User report 2026-08-05, investigated on the REAL screens by the supervisor.** (1) ROOT-CAUSED: the Shadow blur slider can never do anything - RichText.jsx:233 gates the shadow on `spec.shadow.opacity > 0`, DEFAULT_TEXT_SPEC ships `opacity: 0` (OverlayScreen.jsx:61), and the overlay rail exposes only blur, no opacity. Inert by construction for every overlay text block. Recommended fix: blur implies a shadow. Check stroke width for the same shape. (2) ROOT-CAUSED: an existing card can never show the profile Full Name - resolveTitleText returns legacy title_text whenever present, and T6570 removed the title box in the SAME change, so the override is permanent and unclearable. Fix: profile always wins; decide+document what happens to stored values. (3) The eye toggle: STATE flips correctly (title Hide/Show, bg-opacity 20/10) and TextOverlayPreview:32 does filter enabled===false, but whether the text visually disappears is UNRESOLVED - check the undefined-vs-false case on DB-loaded blocks first, and verify the EXPORT honours it too. (4) Rename the Title slot label to "Athlete Name" (LABEL only - the `title` slot key must stay, or the shared contract parity test breaks). **VERIFICATION RULE:** T6610/T6480 drove /textdiag.html + /textspecdiag.html with skipOnDeployedTarget and reported green while criteria were unmet in the app - verify on the REAL screens with an EXISTING DB-loaded record, re-measure geometry before every pointer interaction, and assert elementFromPoint before pressing. |
| T6600 | [Draft tiles paint over the intro-card modal - nested stacking contexts and an ad-hoc z-index scale](tasks/T6600-modal-z-order-and-stacking-scale.md) | 7 | 4 | 1.8 | STAGING | [ ] | **Merged b6878608.** **Split out of T6580 item 1, 2026-08-05, after a second screenshot round.** Draft/reel tiles render ON TOP of the Intro cards modal - over the panel, not through the backdrop; the tell is crisp UNDIMMED cyan buttons, which content behind an 80% scrim cannot be. **Misdiagnosed twice as bleed-through and "fixed" with a heavier scrim** (T6540 /70->/80, T6580 /80->/90+blur); neither could work because the elements paint ABOVE the scrim. T6540 explicitly left the question open and this settles it: a layering bug. Cause is two facts combining - (1) ManageProfilesModal's root is fixed z-50, which ESTABLISHES a stacking context, and IntroCardsModal renders as a CHILD of that subtree with its own z-50 (ManageProfilesModal.jsx:302/:451, IntroCardsModal.jsx:88), so its root-level ceiling is z-50 whatever number it uses; (2) DraftTile portals overlays TO document.body at z-[60]/z-[70] (DraftTile.jsx:708,711), deliberately, because its hover:scale transform (:411) breaks fixed descendants. Tiles outrank the modal BY CONSTRUCTION. Fix = portal/hoist the modal out of the parent context PLUS one ordered z-index scale in a single module (values are ad hoc across ~30 components: z-40/50/[60]/[70]/[90]); nested modals must be expressible (ManageProfiles->IntroCards->ConfirmationDialog). Bumping IntroCardsModal alone is REJECTED - it moves the collision to CollectionPlayer z-[70] and LockedReasonModal z-[90]. SWEEP for other collidable pairs; three wrong diagnoses means the ordering is not inspectable by reading. Verify WITH A TILE HOVERED (hover is what portals - an unhovered shot falsely passes, the failing-open harness class). |
| T6590 | [Thumbnail marker owns the interaction: rename, drag-to-set, stop occluding it](tasks/T6590-preview-image-marker-linkage-and-occlusion.md) | 7 | 4 | 1.75 | TODO | [ ] | **UPDATED by user direction 2026-08-06, which SUPERSEDES the original 'link the two surfaces' framing: you do not link two surfaces that read as unrelated, you delete one and let the marker own the interaction.** (1) RENAME preview image -> thumbnail in ALL user-visible Overlay text (third name for this datum after cover photo -> preview image in T6510, so leave NO mixed vocabulary); rename UI text only, NOT the poster_* model/columns/R2 keys/API fields - a data rename is a separate change with migration cost. (2) DELETE the 'Use current frame' button (OverlaySettingsCard.jsx:218-270); dragging the marker is the only way to set the frame, and the card becomes feedback not control. Discoverability is the risk this creates and must be answered explicitly - losing the button loses the obvious affordance, and T5410 makes visible-at-rest a hard invariant (T5910/T6300 are the hidden-affordance bug class), so NO hover-gated affordance. (3) Tooltip (:129) + aria-label (:139) must state that DRAGGING CHANGES THE THUMBNAIL FRAME - a statement of the interaction, not a noun label; it becomes the primary explanation once the button is gone. (4) Two distinct defects: the icon is CLIPPED by an overflow ancestor (find it, do not nudge the -top-3 offset until it looks right) AND OCCLUDED by the playhead - it is pinned to the video-track top rail DELIBERATELY 'the same band as the playhead' per its docstring, which dodged the region drag handles and traded it for the playhead, worst exactly when the feature is used since setting the frame puts the playhead AT the marker. User proposes moving it LOWER (middle) - evaluate against what the top rail was protecting; if it works UPDATE THE DOCSTRING so nobody restores the old placement. Never hide-on-overlap. Check against region handles, the text lane (T6630 is changing its height), and at 500% zoom. ONE surgical write per drag on the existing wrappedSetPosterMarkerTime path. Use the merged zLayers.js scale, not a raw z-index. ||
| T6550 | [set_project_poster_marker_time is not column-guarded - the write 500s in the deploy->migrate window](tasks/T6550-poster-marker-write-unguarded.md) | 6 | 1 | 6.0 | TODO | [ ] | **Surfaced by the T6510 worker 2026-08-05; pre-existing from T5410, not introduced by T6510.** The READ of projects.poster_marker_time is column-guarded with an explicit deploy->migrate-window comment (poster.py:553); the WRITE at :572 is a bare UPDATE. So on a below-head profile the overlay screen loads fine and then dragging the preview-image marker 500s with "no such column" - reproduced in a container. Migrations do not auto-run on deploy, so EVERY deploy opens this window. Guard it mirroring the read, and decide explicitly what a guarded write returns (must not silently swallow the gesture). SWEEP for other guarded-read/unguarded-write asymmetries rather than fixing only this one. |
| T6540 | [Card editor: the rail is hard to parse](tasks/T6540-card-editor-information-design.md) | 6 | 4 | 1.5 | STAGING | [ ] | **Merged bd17b228.** Rail now has three tiers (On the card / Photo / Style), zoom moved beside replace/remove, composition badge reads as feedback not a debug string, slot picker hides when there is nothing to choose, shadow/stroke behind a collapsed Effects disclosure (opt-in prop; T5225 overlay host unchanged and pinned by tests). Also fixed a real 53px horizontal overflow at 375px in 16:9. VERIFICATION LESSON: the first pass measured in a harness with NO size constraint, so the h-[85vh] cap that causes the cramped-rail complaint was never reproduced - under the real cap that redesign scrolled WORSE than baseline. Harness now wraps the editor in IntroCardsModal exact box; final density at or below baseline at every width, one scroll region, zero overflow at 1024 and 1440. Critique + numbers in T6540-critique.md. **User feedback on first real use 2026-08-05: "TBH this UX is hard to parse."** Sibling of T6530 - that one is about FINDING the feature, this is about using it once you are in. Two clear defects ALREADY FIXED in 58d6eceb (treatment swatches rendered only the near-black backdrop so all three looked identical; colour had two affordances for one value). What remains is hierarchy: 8 same-weight ALL-CAPS sections in one scrolling column with nothing separating what-is-on-the-card from how-it-looks from the-photo; zoom and photo split across two columns; the `title-only` composition badge - the entire visible explanation of the no-template-picker behaviour - reading as a debug label; a lone "Title" slot chip with nothing to choose between. Also resolve whether a reel card seen inside the modal in the report is page bleed-through or a layering bug. ui-designer required; proposal approved before code. |
| T6530 | [UX pass: how should the intro card feature actually be surfaced?](tasks/T6530-intro-card-discoverability-ux.md) | 7 | 4 | 1.8 | DECIDED | [x] | Research + written proposal (22 live screenshots) delivered 2026-08-08, reviewed and decided by the user the same day. Split into 4 implementation tasks: T6660 (naming, final pick "Athlete Intro Card"), T6670 (card selector inline create flow), T6680 (default card before user creates one, design-gated), T6690 (non-active-profile dead end fix). Full decision record in the task file. No code from this task itself - see the 4 children. |
| T6660 | ↳ [Rename to "Athlete Intro Card"](tasks/player-intro/T6660-rename-athlete-intro-card.md) | 5 | 3 | 1.7 | STAGING | [x] | From T6530's decision 2026-08-08. Swept every user-facing string to "Athlete Intro Card"/"Athlete Intro Cards" - copy-only, no internal identifier renamed. Fixed the T6530-found inconsistency (ProfileIntroSection heading vs ManageProfilesModal button) plus the generated card name, toasts, aria-labels, and the privacy policy pair. Done inline (no container), 57 relevant tests green, eslint gate holds at the frozen 360-warning baseline, merged to master `fa3e6ba3`, Master CI + both deploys green. |
| T6670 | ↳ [Card selector: inline create flow](tasks/player-intro/T6670-card-selector-inline-create-flow.md) | 7 | 4 | 1.8 | TODO | [ ] | From T6530's decision 2026-08-08, refined beyond the original proposal by the user. IntroCardCarousel gets a "create new card" affordance that opens the editor inline and returns to the SAME picker with the new card visible + pre-selected, no trip through Settings. Must not bypass T5230's consent gate. |
| T6680 | ↳ [Default Athlete Intro Card before user creates one](tasks/player-intro/T6680-default-athlete-intro-card-provisioning.md) | 7 | 6 | 1.2 | WIP | [ ] | **Direction reversed 2026-08-09 (design v2, approved):** instead of provisioning+gating a default card, REMOVE the default/inherit concept entirely - closes a live prod consent hole where NULL-inherit reels published an unconsented minor's default card with no gate. Answers: OQ1 no-intro-until-attach confirmed; OQ2 picker drops inherit option only (no prompt-to-attach, out of scope); OQ3 clean break, no migration; OQ4 remediation of already-frozen shares out of scope, flagged to T5230; OQ5 retire `is_default` end-to-end. |
| T6690 | ↳ [Non-active-profile dead end fix](tasks/player-intro/T6690-nonactive-profile-dead-end-fix.md) | 6 | 2 | 3.0 | STAGING | [x] | From T6530's decision 2026-08-08 (Q4, approved as proposed). Replaced the dead grey "switch to this profile" text with a real button chaining the existing switchProfile gesture + opening the card library. Done inline (no container - S/M-tier single-area fix), 3 new tests, merged to master `1853e4ca`, Master CI + both deploys green. |
| T6700 | ↳ [Owner in-app playback doesn't show the intro card](tasks/player-intro/T6700-owner-inapp-playback-intro.md) | 6 | 6 | 1.0 | WIP | [ ] | Design approved 2026-08-09 (docs/plans/tasks/T6700-design.md, v2) - implementation started: two thin per-play GET endpoints + an unmount/mount swap inside DownloadsPanel, CollectionPlayer.jsx left untouched. |
|  | **[Post-T6190/T6200 Latency Findings (HAR 2026-07-31)](tasks/post-har-latency/EPIC.md)** |  |  |  |  |  | EPIC. Findings from the HAR captured while VERIFYING T6190+T6200 on the integration branch. Both confirmed fixed by that capture: project-open fires games=0/clips=1/health=0, first R2 video byte ~485ms (prod baseline 2.7s), and the four-requests-at-identical-1460ms queueing signature is gone. What the same capture then exposed is below. Two caveats bind every child task: durations are LOCAL dev (counts and orderings are the signal, not ms), and React StrictMode double-invokes effects in dev, so any exact x2 may be x1 in prod while an ODD count proves multiple real owners. Do T6240 FIRST and re-measure - several smaller findings may be the same event-loop contention and could vanish with it. |
| T6240 | ↳ [App boot serializes for ~22s - user_session_init blocks the event loop](tasks/post-har-latency/T6240-cold-boot-session-init-blocks-loop.md) | 9 | 5 | 1.8 | TODO | [ ] | Epic 1/6, **do first**. The first 8 requests of a session (profiles, downloads/count, credits, quests/progress, admin/me, projects, settings, games) are issued within 37ms and EVERY one takes ~22s, all finishing within ~40ms of each other - T6200 exact serialization fingerprint at boot, 15x larger. 5 concurrent /api/health checks returned status 0 (aborted): backend unresponsive throughout. HYPOTHESIS (not established): T6200 deliberately left user_session_init (blocking R2 download of user.sqlite + profile.sqlite) on the loop, judging it "rare on the hot path" since normal clients send X-Profile-ID - but the FIRST request of a session is /api/profiles, which by definition cannot. Consistent with /api/profiles being the shortest (20644ms) while the rest wait ~22.1s. Confirm with concurrency_probe.py --no-profile (committed by T6200) before fixing; offload via run_in_context. Do NOT add --workers (T6200 rejected it: _USER_WRITE_LOCKS would go per-process = CAS/data-loss hazard). |
| T6250 | ↳ [Entering Overlay fires overlay-data 3x and outdated-clips 2x](tasks/post-har-latency/T6250-overlay-entry-duplicate-fetches.md) | 6 | 3 | 2.0 | TODO | [ ] | Epic 2/6. Framing->Overlay on project 30 fires 5 requests inside 11ms where 2 would do: outdated-clips at t=44352 + 44355, overlay-data at t=44354 + 44356 + 44363. Same defect class T6190 fixed for project-open (two owners fetching the same data), on a transition T6190 did not cover. **overlay-data x3 cannot be StrictMode alone** - dev double-invoke gives EVEN multiples, so an odd count proves >=2 genuine owners. outdated-clips x2 may be StrictMode; verify against a production build before touching it. Unlike T6190 sequential ~685ms-apart pair these are 1-9ms apart and genuinely concurrent, so an in-flight latch would collapse them - there is not one. One owner per fetch; do NOT add a blanket request cache. |
| T6260 | ↳ [Read endpoints send no-cache with no validator - nothing can ever 304](tasks/post-har-latency/T6260-etag-304-read-endpoints.md) | 6 | 4 | 1.5 | TODO | [ ] | Epic 3/6. All 14 JSON read endpoints audited in the HAR return `private, no-cache, stale-while-revalidate` with NO ETag and NO Last-Modified - so the browser can never send If-None-Match, the server can never answer 304, and every request re-sends the full body. stale-while-revalidate is inert without a validator. The stack already does this correctly for poster images (max-age=86400 + ETag), which is the proof the pattern works here; the JSON endpoints just never got it. Add ETags starting with the boot set (projects/games/downloads/settings/profiles). Do NOT add max-age to these - they are mutable per-user data and a stale window would show users their own edits disappearing; 304-on-revalidate is the win. Reuse the existing poster ETag helper, do not write a second one. |
| T6270 | ↳ [Every achievement POST is chased by a quests/progress GET](tasks/post-har-latency/T6270-achievement-post-progress-roundtrip.md) | 4 | 2 | 2.0 | TODO | [ ] | Epic 4/6. /api/quests/progress fetched 4x per session, and 3 of them follow an achievement POST by 100-1400ms (returned_home t=22202->23602, opened_framing_editor t=37514->37619, opened_overlay_editor t=44311->44391). The POST already mutates progress server-side, so returning updated progress in its own response body removes the follow-up GET entirely. Note the opened_framing_editor one lands on the project-open critical path - the exact window T6190 was clearing. Keep the standalone GET (boot needs it). Check first whether callers await the POST at all - some achievement posts are fire-and-forget, and there the right fix is dropping the follow-up GET rather than enriching the response. |
| T6280 | ↳ [Small double-fires - rank/confidence x2, games/{id}/video 302 x2](tasks/post-har-latency/T6280-small-double-fires.md) | 3 | 2 | 1.5 | TODO | [ ] | Epic 5/6, lowest impact. rank/confidence fires twice at the IDENTICAL millisecond (t=87807, 142ms each) - two subscribers or an unguarded effect, not a double click. games/6/video 302s twice (t=54756, 54767) with games/6/load between them, each paying a redirect; worth checking because a duplicated 302 on a media URL can mean two src assignments = two connection setups. **Both are x2, so both may be dev-only StrictMode** - capture against a production build FIRST; "StrictMode only, no action" written up is a successful close. Keep the fix proportional; if the cause is structural, file it rather than forcing a fix. |
| T6290 | ↳ [10 posters fire at once during boot and compete with it](tasks/post-har-latency/T6290-poster-first-load-batch.md) | 4 | 3 | 1.3 | TODO | [ ] | Epic 6/6. 25 poster images per session, 60-78KB each; the first 10 all start at t=22278 (the instant the boot storm releases) and take 1.8-2.3s each, exceeding the browser ~6-connection HTTP/1.1 limit. **Caching is already correct and is NOT the problem** - posters send max-age=86400 + ETag, so repeat visits are cheap; this is first-load only. Re-measure AFTER T6240: these durations are contaminated by the 22s backend stall and may substantially resolve with it. Then check how many of the 10 were actually on screen (ReelTile already uses loading="lazy"; does the Drafts batch?) and confirm HTTP/2 at the edge via T2540 rather than re-deriving it. Do not add a service-worker or bespoke image cache - the HTTP cache is doing its job. |

### Milestone 0: Tooling first — make the staging gate trustworthy (DONE)

Do this before the feature bugs: right now "is staging green?" can't be answered by one run, so
every derisk is manual. Fixing it de-risks all the rest.

*All tasks in this section are complete — rows archived to [PLAN-archive.md](PLAN-archive.md).*

### Next Up

Standalone, immediately actionable — no epic prerequisites.

| ID | Task | Impact | Cmplx | Pri | Status | Migr | Description |
|------|------|------|------|------|------|------|------|
|  | **[Cross-Profile Game Attribution (bug 37p)](tasks/profile-game-attribution/EPIC.md)** | 6 | 4 | 1.5 | TODO |  | Prod bug 37p (arshia, 2026-07-24) is a feature request: reels moved to a kid's profile lose by-game Gallery grouping because T4850's move nulls final_videos.game_ids (target profile has no games row to resolve). Design locked with user: on move, materialize a METADATA-ONLY game reference in the target profile (reuse share-materialization's _copy_game; dedup by (source_profile_id, source_game_id) + video hash) and remap game_ids — gallery read path unchanged, grouping just works. References are VISIBLE in the target Games tab as LINK cards to the owning profile (single source of truth); no storage refs/expiry (refcount minefield untouched). Referenceness derived (source_profile_id NOT NULL), profile_db v030. "Assign to game" gesture DEFERRED; instead a gated heal migration re-attributes arshia's already-moved reels. Smart collections (Top Plays) are tag-based — untouched, reels stay in the mixes as he wants. |
| T5830 | ↳ [Heal arshia's already-moved reels](tasks/profile-game-attribution/T5830-heal-arshia-moved-reels.md) | 4 | 3 | 1.3 | DONE | [x] | **2026-07-31: NOT STARTED - hard-blocked, deliberately not spawned.** Two gates must clear first: (1) T5800+T5810 merged AND DEPLOYED TO PROD (the heal writes T5810-shaped state, so the primitive must exist there), and (2) a dry-run mapping table produced from arshia's downloaded prod R2 profile DBs with explicit user sign-off BEFORE any write (data-safety rule). Also re-derive his kid-profile ids from prod user.sqlite at dry-run time - the T5310-era ids 6ff007e6/22c7616a are stale, do not trust them. Note the heal version is head+1 at implementation time, i.e. **v031** now that T5800 claims v030. Epic 4/4, after 1-3 are on prod. His pre-fix moved reels have game_ids NULL + source rows deleted; "Assign to game" gesture deferred (user), so heal directly: match single-clip reels via clip_game_start_time == default-profile raw_clips.start_time + name/tags (UNAMBIGUOUS matches only, never guess), write T5810-shaped state (reference + game_ids) via a gated versioned profile_db migration (v012/v018/v019 precedent, no-op for everyone else). Dry-run on downloaded R2 DBs + user sign-off on the exact mapping BEFORE any write. **UPDATE 2026-07-24: the 5 clobber-lost final_videos rows are ALREADY RECOVERED** (re-inserted from authoritative source data — see [[project_arshia_lost_reels_move_clobber]]); this task is now ONLY the game_ids attribution for his moved reels, which now includes those 5. **File contention: T5810 edits the SAME `move_reels_to_profile` that got the durability fix (a5ff3e48 `require_fresh`) — rebase on it, don't revert it.** |
| T5750 | [Perspective/keystone correction in Framing (acquisition-corrections family)](tasks/T5750-perspective-keystone-correction.md) | 4 | 6 | 0.7 | TODO | [ ] | **EVIDENCE-GATED — do not implement until >=2 real user clips show keystone distortion rotation can't fix.** Third member of the acquisition-corrections family (per-clip, fix-it-not-creative, before-crop): T5640 rotation fixed camera ROLL; this fixes off-axis aim (yaw/pitch -> converging goalposts, trapezoidal skew) via FFmpeg `perspective` homography. Canonical order dewarp -> perspective -> rotate -> crop; T5640 coordinate contract + safe-area clamp generalize to the composed transform. Prereq architecture (extract T5640's triple-site rotate-before-crop insertion into ONE shared corrections builder) belongs to T5657's design gate — whichever lands second inherits it. UX candidate: drag-2-vertical-guides (Lightroom Guided-Upright style, consistent with the straighten tool). Filed 2026-07-22 to name the family + reserve design constraints, not to schedule work. |
| T6360 | [Pack poster + rich metadata into every downloaded video](tasks/T6360-download-metadata-cover-art.md) | 5 | 4 | 1.3 | TODO | [ ] | Filed 2026-08-02. Downloads leave the app as bare MP4s: no cover art, no title/author/album, container date = the download moment (so a December game files under today in any date-sorted timeline), and no machine-readable attribution to pair with the burned-in outro. We already generate a good poster at publish (final_videos/posters/{filename}.jpg, T5280/T4890) and never write it into the file the user takes home. Fix = ONE serve-time stamping pass with `-c copy` (no re-encode, no quality change) right after the T3950 outro append, so stored R2 objects stay byte-identical, playback/streaming/ranking are untouched, and every EXISTING reel gets it with no backfill. Writes: attached_pic cover art + title/artist/album_artist/album/date+creation_time (the GAME date, not export date)/comment/copyright/genre, keeping `+faststart`. Deliberately NOT written: location/GPS (children at recurring venues; the coordinate travels with every re-share), chapters, any pixel or audio change. Covers all 4 egress points; path 3 is a real pre-existing gap - the share-page download (SharedVideoOverlay) fetches the presigned R2 URL directly and today gets NEITHER metadata NOR the branded outro, so it gets routed through a backend endpoint. T4945 collection downloads must reuse the same helper. Known-and-accepted limits to document rather than fight: Windows Explorer ignores `covr` and makes its own thumbnail; social re-uploads strip most tags - the win is the user's own filesystem/camera roll/AirDrop/Plex/Apple ecosystem. Top risk: the attached_pic adds a second video stream, so the iOS share-sheet -> Instagram path MUST be verified before merge; if a target chokes, gate cover art off for web-share and keep the (zero-risk) tags. Missing poster or missing game date -> omit that field, never fabricate. Backend + a small frontend change; M-tier. |
|  | **[Multi-File Ingest & Prep](tasks/prepare-stage/EPIC.md)** | 3 | 8 | 0.4 | TODO |  | DEPRIORITIZED 2026-07-31 (user): "most users won't need this." Impact 6 -> 3, so Priority 0.8 -> 0.4 ("consider if it's worth doing at all"), and the block moves to the bottom of Next Up. The deprioritization is an AUDIENCE judgment, not a technical one: the core customer is a soccer parent with a phone or a Veo, and multi-file 62GB 8K drone ingest serves a narrow slice of that. Nothing here is wrong or blocked — the design spike is still valid and the 50-200x reduction still holds if it is ever picked up. Child scores are unchanged: they express value RELATIVE to each other inside the epic, while the epic row is what competes at milestone level (see Epic Prioritization). Revisit if real users start arriving with multi-file/high-res footage. Original scope: Bring in large, multi-file, non-Veo footage (62GB 8K DJI folder) without uploading the raw monster. Relevant video drops 50-200x (50GB -> <1GB) via edit-on-proxy + manual trim + conform-crop-from-master. Design spike DONE (hands-on ffmpeg + YOLO on the real footage): [study](research/T5650-dji-8k-ingest-reduction-study.md) (section 0 = locked decisions). Decisions: manual client trim (auto-trim dropped); dual-asset pipeline (upload LRF+MP4, Annotate+Framing-preview on LRF, Framing conforms crop ROI from MP4, upscale only when native crop < target); new `VideoMode.MULTI_FILE` ("Multiple Files") -> client-side Prep (assemble, manual trim, low-fi LRF preview + "annotate this" markers, stream-copy trim+concat, resumable dual-asset upload); use shipped .LRF else generate. Pairs with T5640 (rotation) + T5657 (fisheye de-warp). |
| T5651 | ↳ [Proxy/master asset pairing data model + credit accounting](tasks/prepare-stage/T5651-proxy-master-data-model.md) | 6 | 5 | 1.2 | TODO | [x] | Epic 1/7. Game video carries proxy (LRF) + master (MP4) refs with a verified frame-accurate link; upload cost counts both; keep master while game active. Foundational for the rest. |
| T5652 | ↳ [`MULTI_FILE` mode + client-side Prep workspace shell](tasks/prepare-stage/T5652-multifile-mode-prep-shell.md) | 6 | 5 | 1.2 | TODO | [ ] | Epic 2/7. Third `VideoMode` ("Multiple Files") beside Full Game / Per Half -> folder pick -> full-screen client-side Prep shell (file rail + timeline + preview + Create). Full Game / Per Half stay byte-identical. Engine is T5653. |
| T5653 | ↳ [Client assemble/trim/concat + resumable dual-asset upload](tasks/prepare-stage/T5653-client-assemble-trim-upload.md) | 7 | 8 | 0.9 | TODO | [ ] | Epic 3/7 (hardest). Auto-order by timestamp + drag; manual multi keep-range trim; STREAM-COPY concat both proxy+master (ffmpeg.wasm/WebCodecs, lossless, keeps 8K); resumable dual-asset upload. Validate browser ceiling on the real 62GB folder; cross-codec/fallback explicit. |
| T5654 | ↳ [Annotate reads the proxy (LRF)](tasks/prepare-stage/T5654-annotate-on-proxy.md) | 6 | 4 | 1.5 | TODO | [ ] | Epic 4/7. Dual-asset games annotate on the 720p proxy (fast scrub); single-asset games unchanged; clip in/out times stay master-valid (frame-aligned invariant). |
| T5655 | ↳ [Framing conform-from-master + upscale-only-as-needed](tasks/prepare-stage/T5655-framing-conform-upscale-as-needed.md) | 7 | 6 | 1.2 | TODO | [ ] | Epic 5/7. Framing preview on proxy; export conforms crop ROI x clip window from the MASTER (native, never whole 8K); AI upscale runs ONLY when native crop < target (8K crops usually already >=1080p -> skip). Shares filter-stage design with T5640/T5657. |
| T5656 | ↳ [Prep markers -> Annotate clip candidates](tasks/prepare-stage/T5656-prep-markers-to-annotate.md) | 5 | 4 | 1.3 | TODO | [ ] | Epic 6/7. "Annotate this" markers dropped during the low-fi Prep preview persist on the game and surface in Annotate as pre-seeded clip candidates (human-in-loop, not auto-clips). |
| T5657 | ↳ [Fisheye de-warp + horizon level](tasks/prepare-stage/T5657-fisheye-dewarp-level.md) | 5 | 6 | 0.8 | TODO | [ ] | Epic 7/7. Lens de-warp (OpenCV cv2.fisheye / ffmpeg lenscorrection |

### Milestone B: Single-server data correctness (needs the export harness first)

Real single-server data bugs, but each needs the export golden harness [T4370](tasks/export-write-path/T4370-export-golden-harness.md) or its epic in place first — so **T4370 is the actual unlock** for this milestone:

- [T4390](tasks/export-write-path/T4390-finalize-publish-single-writers.md) (imp 9) — the finalize-a-reel DB step is copy-pasted 5×, one missing version/duration → inconsistent reel records. *(export-write-path epic)*
- [T4430](tasks/T4430-ffmpeg-params-probe-consolidation.md) (imp 8) — `-shortest` truncation live in 2 render paths → truncated exported video. *(needs T4370)*
- [T4420](tasks/T4420-single-interpolation-module.md) (imp 7) — 4 diverging crop-interpolation copies → local vs Modal export crop differently. *(needs T4370)*
- [T4340](tasks/write-correctness/T4340-canonicalize-segments-at-write.md) (imp 7) — segments_data stored in two formats → the inverted-clip / wrong-recap render class. *(write-correctness epic, a pure single-server data bug)*
- [T4350](tasks/write-correctness/T4350-reexport-retransform-highlights.md) (imp 6) — highlights drift onto the wrong moments after a re-export. *(write-correctness epic; after T4340)*

### Milestone C: Editor correctness (epic-gated)

Single-server bugs sitting inside sequenced epics — do their prerequisite first:

- [T4450](tasks/keyframe-unification/T4450-shared-keyframe-track.md) (imp 7) — the "can't delete the first keyframe" bug. *(keyframe-unification; needs T4440 dead-code sweep first)*
- [T4500](tasks/editor-decoupling/T4500-selectedproject-selector.md) (imp 6) — stale selectedProject → wrong name/ratio after a rename. *(editor-decoupling; needs T4470–T4490 first)*

### Code Quality & Refactoring (2026-07-03 audit)

| ID | Task | Impact | Cmplx | Pri | Status | Migr | Description |
|------|------|------|------|------|------|------|------|
|  | **[Write Correctness & Concurrency](tasks/write-correctness/EPIC.md)** | 6 | 4 | 1.5 | TODO |  | Split from the durability epic 2026-07-24: real correctness/concurrency bugs, but none silently destroys committed data across a machine cycle (lower priority than the loss class). Writes produce correct, canonical, atomically-ordered, conflict-safe data. |
| T4330 | ↳ [Unified Action Client: FIFO + Versioning + 409](tasks/write-correctness/T4330-action-client-serialization-conflicts.md) | 7 | 4 | 1.8 | TODO | [ ] | framingActions ≡ overlayActions (same sendAction twice); actions fire-and-forget → network reordering races on whole-blob RMW; overlay's expected_version check is commented out (protects nothing). One createActionClient: per-entity FIFO, version threading, 409 → refresh prompt. May need working_clips version column (migration). Needs concurrent editors to bite (lower frequency than the clobber class). |
| T4340 | ↳ [Canonicalize segments_data at Write Time](tasks/write-correctness/T4340-canonicalize-segments-at-write.md) | 7 | 5 | 1.4 | TODO | [x] | Dual format (splits-only vs full-list) forces every reader to canonicalize; overlay.py:1307 reads raw (inverted-clip bug class). Canonicalize in _save_clip_framing_data; duration onto working_clips row if needed; migration rewrites existing rows (tuple row-factory gotcha); then readers stop normalizing (loud log if non-canonical seen). Pure render-correctness, not sync-durability. |
| T4350 | ↳ [Re-Export Re-Transforms Carried Highlights](tasks/write-correctness/T4350-reexport-retransform-highlights.md) | 6 | 4 | 1.5 | TODO | [ ] | framing.py:234 copies old highlights verbatim onto a re-timed video — regions point at wrong moments after trim/speed changes. Map through old→new timeline (transform_all_regions_to_working) or drop-and-notify; never carry verbatim across a timing change. After T4340. |
| T4360 | ↳ [Explicit Orderings: BEGIN IMMEDIATE + Invariants](tasks/write-correctness/T4360-explicit-orderings-invariants.md) | 6 | 3 | 2.0 | TODO | [ ] | Action-endpoint RMW atomicity is an accident of "no await between read and commit"; games activation ordering is comment-hardened only (bug26p). Explicit write transactions + a race-detector test (injected await must fail it) + activation invariants as executable tests. Prerequisite for T4640. |
|  | **[Export Write-Path Unification](tasks/export-write-path/EPIC.md)** | 9 | 7 | -- |  |  | One implementation per export/publish operation, behind a characterization harness. STRICT order. The T4010/T4020/T4110/rank-sweep incident class, removed structurally. |
| T4370 | ↳ [Export Golden-Output Test Harness](tasks/export-write-path/T4370-export-golden-harness.md) | 7 | 4 | 1.8 | TODO | [ ] | Before anything moves: DB-delta snapshots for all 6 export triggers (render stubbed at ffmpeg/Modal boundary) + small real render goldens (local path, tolerance-based frame compare). The parity oracle for T4380-T4430. One re-bless command with reviewable diffs. |
| T4380 | ↳ [ExportJobRepository](tasks/export-write-path/T4380-export-job-repository.md) | 8 | 4 | 2.0 | TODO | [ ] | Two competing create-helpers (one swallows insert failure — job runs unrecorded), 14 raw status-write sites in 5 modules, worker imports from router. One repository owns every transition (ExportStatus enum); insert failure aborts loudly; grep proves single ownership. |
| T4390 | ↳ [finalize_export + publish_final_video Single Writers](tasks/export-write-path/T4390-finalize-publish-single-writers.md) | 9 | 5 | 1.8 | TODO | [ ] | The finalize transaction is hand-copied 5× (one omits version/duration); final_videos has 3 writers — the sweep one caused the raw-clips-in-ranking incident (T4160). One finalize; one publish writer where sweep semantics are explicit parameters; T4010/T4160 protections become properties of the shared code. |
| T4400 | ↳ [Backend-Authoritative Export (mark-exported)](tasks/export-write-path/T4400-backend-authoritative-export.md) | 9 | 6 | 1.5 | TODO | [ ] | Export still trusts CLIENT state: full-state PUT can clobber newer surgical edits (two tabs; T4020 class — guarded only by frontend conventions incl. an always-false predicate); multi-clip renders the payload then stamps DB exported WITHOUT reconciling (DB ≠ rendered video). Backend snapshots its own blobs; client sends no keyframes. Gate: verify no hook state exists that gestures never persisted. |
| T4410 | ↳ [Export Pipelines → Services + Sweep Unification](tasks/export-write-path/T4410-export-pipelines-to-services.md) | 9 | 8 | 1.1 | TODO | [ ] | routers/export/ = 5,878 L (YOLO, ffmpeg, a 640-line _export_clips in a router); duplicate send_progress/callback/sync pairs (only one sync is T4110-durable); sweep auto-export is a parallel universe (no job record, own ffmpeg/R2/status literals). Slices: merge helper pairs → code motion → dispatcher shape → sweep onto shared rails (T4160 semantics preserved as parameters). |
| T4420 | [One Crop-Interpolation Module (4 Catmull-Rom Copies)](tasks/T4420-single-interpolation-module.md) | 7 | 4 | 1.8 | TODO | [ ] | Rendering-identity math ×4 (app/interpolation.py canonical + upscaler + 2 Modal copies) — divergence = local vs Modal exports crop differently. Package canonical into the Modal image; parity fixtures captured from all copies FIRST (existing divergence = a finding to surface). Also: GPU-param on process_framing_ai (kills the L4 200-line copy) + delete the _optimized benchmark file. Modal redeploy (ask). Depends on T4370. |
| T4430 | [FFmpeg Encode-Params + Probe Consolidation](tasks/T4430-ffmpeg-params-probe-consolidation.md) | 8 | 7 | 1.1 | TODO | [ ] | ~55 hand-built ffmpeg arg lists in 13 modules; libx264 block ×15 in one file that ignores its own helper; `-shortest` truncation fix documented in one path, still live in two (output-correctness split); CRF drift 18/23/32 unnamed; 6+ ffprobe implementations each defaulting "30/1". Named encode profiles + one probe (raises on failure); -shortest migration is an isolated, golden-verified behavior commit. Depends on T4370. |
|  | **[Keyframe System Unification](tasks/keyframe-unification/EPIC.md)** | 8 | 6 | -- |  |  | Finish migrating overlay onto framing's keyframe systems (controller, resolveTargetFrame, flat-list rules). STRICT order. Absorbs T3810 + T3820. |
| T4440 | ↳ [Dead Keyframe/Timeline Code Deletion Sweep](tasks/keyframe-unification/T4440-dead-code-deletion-sweep.md) | 6 | 2 | 3.0 | TODO | [ ] | Delete the dead parallel stack that still absorbs fixes: OverlayTimeline + HighlightLayer + components/Timeline (no importers), useHighlight (T3810), name-colliding container wrappers, framingStore corpses (clipStates/videoFile/markExported), annotateHasSelectedClip + its reactive writer, ReferenceError-if-rendered OverlayVideoOverlays. ~800+ lines; grep-prove each. |
| T4450 | ↳ [Shared KeyframeTrack Rendering](tasks/keyframe-unification/T4450-shared-keyframe-track.md) | 7 | 4 | 1.8 | TODO | [ ] | CropLayer vs RegionLayer keyframe rendering forked; delete gating diverged into THE "can't delete first keyframe" bug (highlight path still enforces the dead permanent-keyframe model). One KeyframeTrack; flat-list delete rule uniform; the gating change is the single intended diff. |
| T4460 | ↳ [Overlay onto Keyframe Controller](tasks/keyframe-unification/T4460-overlay-onto-keyframe-controller.md) | 9 | 7 | 1.3 | TODO | [ ] | **Stage 2 gate.** useHighlightRegions re-implements the whole lifecycle (own snap window 5-vs-10 + own parallel fix for the resolveTargetFrame identity bug + fps=30). Controller grows region-scoped tracks; T3820's snap direction decided by user here; payload-parity tests per gesture (persistence semantics = the T350-class risk). |
|  | **[Editor State Decoupling](tasks/editor-decoupling/EPIC.md)** | 8 | 6 | -- |  |  | Kill cross-screen timing contracts + duplicate state (T1670-family breeding ground). Independent except T4530 last. |
| T4470 | ↳ [FramingContainer: One Next-State per Gesture](tasks/editor-decoupling/T4470-framing-single-nextstate.md) | 8 | 4 | 2.0 | TODO | [ ] | 8 hand-mirror sites rebuild segments/crop payloads to fight React batch timing ("won't have the new value yet"); trim handler mirrors a stale value today. Pure computeNext per gesture → hook + store + API from the same object; batching workarounds deleted; surgical payloads unchanged. |
| T4480 | ↳ [Kill clipMetadata Event-Bus + One Overlay Loader](tasks/editor-decoupling/T4480-clipmetadata-bus-removal.md) | 7 | 4 | 1.8 | TODO | [ ] | clipMetadata = stored derived data AND a cross-screen message consumed by an effect whose first act is a store write; fires "fresh export" on ordinary loads. Plus two duplicate 60-line overlay-data loaders (5 setters already wired twice). Explicit navigation payload (pendingNavigation pattern); one loadOverlayData; clipMetadata derived via selector. |
| T4490 | ↳ [Working-Video State Machine](tasks/editor-decoupling/T4490-working-video-state-machine.md) | 8 | 6 | 1.3 | TODO | [ ] | Working-video truth spread across 2 stores + project fields + 4 OverlayScreen guard refs + a 65-line reconcile effect with reactive recovery — the T1670/stuck-loading locus. One {status,video} machine in projectDataStore, one loadWorkingVideo action (dedup + bounded retry); FramingScreen stops writing overlayStore; all refs deleted. |
| T4500 | ↳ [selectedProject → id + Selector](tasks/editor-decoupling/T4500-selectedproject-selector.md) | 6 | 4 | 1.5 | TODO | [ ] | selectedProject is an independently-fetched snapshot of projects[]; rename patches only the list → ProjectContext (both editors) serves stale name/ratio/URL. Store selectedProjectId; derive via selector; detail fields merge into the list entry (or the extra fetch dies). |
| T4510 | ↳ [Annotate API Data → gamesDataStore](tasks/editor-decoupling/T4510-annotate-data-to-store.md) | 7 | 5 | 1.4 | TODO | [ ] | gameVideos/tags/share state in container useState with two mount-restore effects re-syncing parallel copies (T1540/T4060 bug class, onboarding surface). Store keyed by gameId, raw shape + selectors + deduped fetch actions; restore effects deleted; upload-completion writes at the upload-event site. |
| T4520 | ↳ [Reactive-Effect Cleanup Batch](tasks/editor-decoupling/T4520-reactive-effect-cleanups.md) | 6 | 4 | 1.5 | TODO | [ ] | Ten verified small violations, one commit each: audio auto-toggle race → gesture; toast dismissal via identity-watching effects ×2 → gesture wrappers; clip-selection triple-owner; getFilteredKeyframesForExport duplicated verbatim; FramingScreen triple load/restore paths → one loadClipIntoEditor; gamesDataStore stored derivations + version counter; hasFramingEdits ×3; setGlobalAspectRatio stale transform path; useState-initializer side effect; export-dirty slice factory. |
| T4530 | ↳ [Editor-Mode Isolation Harness](tasks/editor-decoupling/T4530-mode-isolation-harness.md) | 6 | 4 | 1.5 | TODO | [ ] | LAST in epic. Per-mode mount helpers (real captured API fixtures) proving each mode loads with zero sibling-mode state + regression checks that keep the timing-contract table empty. Overlay mounting alone IS the epic's proof. |
| T4540 | [frameMath + Canonical Framerate](tasks/T4540-framemath-canonical-framerate.md) | 7 | 3 | 2.3 | TODO | [ ] | getFramerate() stub always returns 30; `\ |
| T4560 | [Frontend Primitives Sweep](tasks/T4560-frontend-primitives-sweep.md) | 6 | 4 | 1.5 | TODO | [ ] | Five mechanical consolidations: formatTime ×9 (divergent null/rounding); EDITOR_MODES + KeyframeOrigin constants exist with ZERO imports while raw literals circulate (~15 files / 31 sites — absorbs T301/T302/T303; flips T4290 lint to error); createStrictContext (Crop≡Highlight contexts); Modal primitive that enforces no-backdrop-close + Spinner (31/48 hand-rolled); apiJson helper (exemplar store migration). |
| T4570 | [useEditorScreenShell + Shared Shortcuts](tasks/T4570-editor-screen-shell.md) | 6 | 5 | 1.2 | TODO | [ ] | Framing/Overlay screens are 1,100-line siblings with character-identical shell blocks; Overlay re-implements keyboard inline with DIFFERENT arrow-key semantics than framing's shared hook. Shell hook for the identical 20%; Overlay onto useKeyboardShortcuts (arrow behavior = user decision, applied to both). No full screen merge. After the decoupling epic. |
| T4580 | [usePlaylistPlayback](tasks/T4580-useplaylistplayback.md) | 5 | 4 | 1.3 | TODO | [ ] | Recap + highlights playback hooks return the same 15-key interface with line-identical cores; story playback repeats the rAF core a third time. One engine with an advanceStrategy seam (virtual vs per-clip); ended-advance + pendingSeek race pinned by tests; manual playback pass on all three surfaces. |
| T4590 | [historyStack Extraction](tasks/T4590-historystack-extraction.md) | 5 | 4 | 1.3 | TODO | [ ] | The only undo (useSegments trimHistory, bespoke, already patched once) gets its stack mechanics extracted to a pure historyStack module BEFORE keyframe undo is built from scratch as a second system. Behavior byte-identical; undo-vs-persistence semantics documented for the next consumer. |
| T4600 | [regionTrack Interval Engine](tasks/T4600-regiontrack-engine.md) | 8 | 7 | 1.1 | TODO | [ ] | **Stage 2 gate.** Sorted-non-overlapping-intervals model ×3 (annotate clips / overlay regions / framing segments): same id-generation, overlap, clamping, percent layout. Waves: layout → overlap/creation → boundary drag (characterize per-mode semantics first) → segments adoption depth decided in design (T3120 precedent: partial adoption is a valid outcome). After keyframe epic. |
| T4610 | [require_admin Router Dependency](tasks/T4610-admin-router-dependency.md) | 7 | 2 | 3.5 | TODO | [ ] | ~25 imperative _require_admin() calls — one forgotten = open admin endpoint. Router-level Depends + a meta-test iterating router.routes so FUTURE handlers are covered automatically. Security-shaped, tiny. **Do this BEFORE T5940** — its meta-test becomes the safety net for the split, and doing it after means applying the dependency 8 times instead of once. |
| T5950 | [Verify cross-machine CAS conflict (blocked: 1 box)](tasks/T5950-cross-machine-cas-conflict-verification.md) | 6 | 3 | 2.0 | TODO | [ ] | **BLOCKED until we run more than one backend machine — not actionable today.** Prod runs a SINGLE Fly machine, so a CAS conflict cannot occur as deployed, and the single-container dev stack cannot produce one either. User call 2026-07-26: "we are still on 1 box so I'm not worried about it." The durability epic's guards (T4310 CAS upload, T4315 restore-if-newer, T5870 pending/failed/conflict + honest Retry) are all unit-tested and red-verified, but the human-facing sequence has never run against a REAL conflict: two machines write the same user DB -> second refused -> conflict banner -> Retry does restore-if-newer -> **user is TOLD their local changes were replaced** -> page reloads so render matches disk. That last mile is exactly what T5870 round 1 got wrong (silently discarded the edit and reported success); now fixed + unit-covered both sides, never exercised for real. Pick up when we scale out. |
| T5940 | [Split admin.py into cohesive sub-routers](tasks/T5940-split-admin-router-cohesion.md) | 5 | 5 | 1.0 | TODO | [ ] | Divergent Change, measured: admin.py is 1880 LOC / 34 endpoints / ~10 unrelated domains, changed by **25 distinct task ids across 50 commits** (T525…T5760). The frontend ALREADY found these seams (components/admin/ is split into AnalyticsDashboard, RevenueReconciliation, CreditGrantModal, UserTable, …) — only the backend is monolithic. Analytics alone is ~700 LOC of aggregation logic inline in the router with no services/analytics.py. **Honest scoping: this would NOT have prevented the T5840×T4315 conflict that prompted it** (both edited the credit-grant code specifically — same feature, not accidental adjacency); justify it on Divergent Change, not on that conflict. What weak cohesion DID cost there was blast radius + one real trap: tests/test_persistence_risk_coverage.py mixes middleware-sync tests with admin-refresh tests, so a whole-file resolution silently drops the other task's durability work — splitting that test file is the part with genuine safety value. Sub-routers under routers/admin/ mounted on the same prefix so every URL is byte-identical. BINDING process rules: route-table snapshot test BEFORE any motion; one sub-router per mechanical commit <200 lines; motion never mixed with behavior change (the analytics service extraction is a separate later commit); no dynamic router auto-discovery (greppability). Sequence AFTER T4610 and AFTER T5840 lands. |
| T4620 | [fetch_or_404 + Enums + queries Straggler](tasks/T4620-fetch-or-404-enums.md) | 6 | 4 | 1.5 | TODO | [ ] | ~45 hand-rolled existence checks / 34 "not found" wordings → per-entity helpers (frontend error-string grep first); finish constants.py adoption (games.py imports GameStatus then writes literals; missing enums for storage/export-type/mode/share; absorbs T304/T305); projects.py:338 hand-rewrites latest_working_clips_subquery with a divergent tiebreak — decide correct ordering with a test (T1532 class). |
| T4630 | [R2StreamProxy Service](tasks/T4630-r2streamproxy-service.md) | 7 | 5 | 1.4 | TODO | [ ] | Streaming proxy ×4; the pooled-httpx TTFB fix reached 1 of 4 (games/clips/projects still pay per-request client latency); retry generator duplicated verbatim; must carry T1690's don't-commit-before-R2 fix everywhere. One service; TTFB measured before/after; scrubbing verified on all four surfaces. |
| T4640 | [games.py Services: Activation + One Share Flow](tasks/T4640-games-services-extraction.md) | 6 | 6 | 1.0 | TODO | [ ] | activate_game = 185-LOC handler orchestrating 3 datastores with a deliberate mid-handler commit, guarded only by bug26p comments; share vs share_playback copy-paste ~130 lines incl. except-pass revoke paths. GameActivationService (invariants from T4360 enforced between named steps) + one share_game_flow. Depends on T4360. |
| T4650 | [raw_clips Write-Path Consolidation](tasks/T4650-rawclips-write-path-consolidation.md) | 7 | 5 | 1.4 | TODO | [ ] | Bulk save_annotations_to_db (full-table UPDATE/INSERT/DELETE, own boundaries-version copy) coexists with the gesture path (clips.py save/update). Caller census → legit-bulk keeps a renamed import path using the ONE extracted version logic; illegitimate callers route through gestures. Unclear callers escalate, not guessed. After T4270. |
| T4660 | [open_sqlite Factory + game_display Service](tasks/T4660-sqlite-factory-game-display.md) | 5 | 2 | 2.5 | TODO | [ ] | Connection PRAGMA recipe copy-pasted 4 ways — privacy.py connects with NO busy_timeout/PRAGMAs (lock errors under sync load); game display naming ~100 lines byte-identical in projects.py + downloads.py (naming rules just churned in T4160/T4190). One factory + one game_display module. |

### Keyframe Identity Cleanup (follow-ups from crop/overlay duplicate-keyframe fix)

From the [code quality audit](audit-2026-07-03-code-quality.md) (directives: DRY / sync model / dependence minimization), user-approved 2026-07-03. Bug tier is in the Bugs section above (T4200-T4280). Guardrails land first; epics run in listed order internally; standalone tasks are independent. Epics with **Stage 2 gates** (T4460, T4600) need design approval before implementation.

| ID | Task | Impact | Cmplx | Pri | Status | Migr | Description |
|------|------|------|------|------|------|------|------|
| T3810 | [Delete Dead useHighlight Hook](tasks/T3810-delete-dead-usehighlight-hook.md) | 2 | 1 | 2.0 | TODO | [ ] | **ABSORBED into T4440** (keyframe-unification epic dead-code sweep). `useHighlight.js` is exported from overlay/index.js but never instantiated (live system is useHighlightRegions). A third keyframe implementation that adds confusion. Confirm no callers, delete hook + re-export + dead test. |
| T3820 | [Reconcile Keyframe Snap Directions](tasks/T3820-reconcile-keyframe-snap-directions.md) | 4 | 4 | 1.0 | TODO | [ ] | **ABSORBED into T4460** (overlay onto keyframe controller — the snap decision is made there). Crop snaps by KEEPING the old frame (10-frame window); overlay snaps by MOVING to the clicked frame (5-frame window). Opposite UX for the same gesture. Needs a UX decision, then unify direction + window across both modes (guard permanent boundaries). |

Surfaced while fixing the keyframe-identity divergence (display snaps an edit to a nearby keyframe, but the surgical persistence sent the raw clicked frame/time, so the backend accumulated near-duplicate keyframes that, on delete, stripped a permanent boundary). Root fix + profile_db v014 heal shipped on branch `fix/crop-keyframe-dup-snap`; these are the remaining DRY/UX cleanups.

### Upscale Quality

| ID | Task | Impact | Cmplx | Pri | Status | Migr | Description |
|------|------|------|------|------|------|------|------|
|  | **[Upscale Quality](tasks/upscale-quality/EPIC.md)** | 9 | 7 | 1.3 |  |  | Testbed-proven upscale improvements for far-side action: quick wins → model A/B → temporal VSR → routed integration → (conditional) domain fine-tuning. |
| T4700 | ↳ [SR Quality Testbed](tasks/upscale-quality/T4700-sr-quality-testbed.md) | 8 | 5 | 1.6 | TODO | [ ] | Standalone eval harness at `src/backend/experiments/sr_testbed/`: fixed 10-12 clip set (far/mid/near, day/night, manifest-pinned crop keyframes), `Pipeline` plug-in interface, exact prod-replica + lanczos baselines, synthetic (PSNR/SSIM/LPIPS vs ground truth) + real (MUSIQ/NIQE + flicker ratio) tracks, blind-A/B HTML report with zoomed focus-box stills + jersey/face hallucination checklist, generic Modal GPU runner. Blocks the whole epic. |
| T4710 | ↳ [Encode + Denoise Quick Wins](tasks/upscale-quality/T4710-encode-denoise-quick-wins.md) | 6 | 2 | 3.0 | TODO | [ ] | Three cheap prod losses: final encode is CRF 23/fast while chunks are CRF 18 (last step = lowest quality — GPU detail thrown away at encode); Modal path missing bt709 color tags (local path sets them; some players render washed out); `dni_weight` denoise blend unused (`realesr-general-wdn-x4v3` companion model) while far crops are block-noise the GAN sharpens. Prove in testbed, then flip flags in video_processing.py + bake wdn weight into the image. |
| T4720 | ↳ [GAN Model A/B Evaluation](tasks/upscale-quality/T4720-gan-model-ab-evaluation.md) | 6 | 3 | 2.0 | TODO | [ ] | Measure (not estimate) RealESRGAN_x4plus / SwinIR-L GAN / Real-HAT vs the compact prod model on our clips, via `spandrel` auto-loading (sidesteps the dead arch-file backends in ai_upscaler). Full matrix on Modal T4 + L4 timing; recommendation table with LPIPS/MUSIQ/flicker/blind-win-%/s-per-frame/$-per-clip/VRAM. Eval only; "wait for VSR" is a valid verdict. |
| T4730 | ↳ [Temporal VSR Prototype (FlashVSR / SeedVR2)](tasks/upscale-quality/T4730-temporal-vsr-prototype.md) | 9 | 6 | 1.5 | TODO | [ ] | The ceiling-changer: one-step diffusion VSR fuses sub-pixel detail across frames — the only real fix for 30-80px far-side players. FlashVSR v1.1 primary (~17fps @ 768x1408 on A100, ~10x our current throughput), SeedVR2-3B secondary. Modal L40S, weights on a Volume. Hard gates: >=70% far-side blind wins, ZERO jersey/face hallucinations, <=$0.15 + <=3x wall-clock per 10s clip, license review. Go/no-go for T4740. |
| T4740 | ↳ [Production Integration + Crop-Size Routing](tasks/upscale-quality/T4740-production-integration-routing.md) | 8 | 5 | 1.6 | TODO | [ ] | BLOCKED by T4730 go. New Modal function (same progress-generator contract as process_clips_ai) + routing in modal_client: min-crop < FAR_SIDE_CROP_THRESHOLD (300) AND VSR_ENABLED → heavy pipeline on L40S; near-side stays on cheap T4 path. Loud deliberate fallback to standard pipeline on heavy failure (never a failed paid export); `[UPSCALE_COST]` telemetry; comparison endpoint gets the new option; staging-first. |
| T4750 | ↳ [Domain Fine-Tuning (conditional)](tasks/upscale-quality/T4750-domain-finetune.md) | 7 | 8 | 0.9 | TODO | [ ] | CONDITIONAL — only if T4730/T4740 leave a named gap. HR/LR dataset from OWN-account near-side footage (YOLO-harvested player crops; consent boundary is hard); Veo/Trace-matched synthetic degradation validated by blind-sort vs real far crops; fine-tune the shipped model (Real-ESRGAN recipe or VSR LoRA); same testbed gate — hallucination checklist especially (plausible wrong jersey numbers are worse than blur). |

### Movement Tracking (Activity Timeline for Annotate)

Make far-side-of-field action look good — the #1 quality ceiling for Trace/Veo footage (far-side crops are ~206x366 px of compressed mush; per-frame Real-ESRGAN can't recover detail a single frame doesn't contain). Strategy: build a quality testbed FIRST (no upscale change ships without a testbed run proving it), harvest cheap encode/denoise wins, A/B heavier GAN models, then the headline bet — temporal video super-resolution (FlashVSR/SeedVR2), which aggregates detail across frames. Ships behind crop-size routing so near-side clips stay on the cheap pipeline. Epic tasks are STRICTLY ordered. Research basis + decision gates: [tasks/upscale-quality/EPIC.md](tasks/upscale-quality/EPIC.md).

| ID | Task | Impact | Cmplx | Pri | Status | Migr | Description |
|------|------|------|------|------|------|------|------|
|  | **[Movement Tracking](tasks/movement-tracking/EPIC.md)** | 8 | 7 | 1.1 |  |  | Testbed-proven dead-time detection: labeling env + ground truth → classical signal bake-off → detection/fusion tuning to KPI gates → Modal job + profile artifact → Annotate activity layer → smart playback → paid opt-in. |
| T5430 | ↳ [Motion Testbed + Labeling Env + Dataset](tasks/movement-tracking/T5430-motion-testbed-labeling.md) | 8 | 5 | 1.6 | TODO | [ ] | The instrument: `experiments/motion_testbed/` (sr_testbed pattern), keyboard-driven HTML labeler (A/D/E state stamping at 4x), 8 games labeled across veo-static/follow/phone cameras (5 dev / 3 held-out, labels committed, media gitignored), G1-G6 metrics harness + HTML report, inter-annotator kappa, trivial baselines. Blocks the whole epic. |
| T5440 | ↳ [Classical Signal Bake-off](tasks/movement-tracking/T5440-classical-signal-bakeoff.md) | 7 | 5 | 1.4 | TODO | [ ] | CPU-only signals measured on labeled games: bitstream motion vectors (mv-extractor/PyAV, near-free), frame diff/MOG2, DIS optical flow — each with camera-motion compensation on/off per camera class. ROC-AUC comparison, cost curve vs sampling rate, per-second feature cache for T5450. |
| T5450 | ↳ [Detection + Fusion + Smoothing → Gate](tasks/movement-tracking/T5450-detection-fusion-tuning.md) | 9 | 7 | 1.3 | TODO | [ ] | The go/no-go: YOLO+ByteTrack semantic features (player count nails EMPTY/halftime) fused with classical features via tiny classifier + hysteresis/HMM decode; tune G1 first (>=99.5% play preservation) then maximize dead capture; leave-one-game-out on dev, single frozen-recipe run on held-out; error analysis + Modal cost projection <=$0.50/game. |
| T5460 | ↳ [Modal Movement Job + Profile Persistence](tasks/movement-tracking/T5460-modal-movement-job.md) | 8 | 6 | 1.3 | TODO | [ ] | BLOCKED by T5450 go. `analyze_movement` Modal fn (reuses baked yolov8x, generator progress, presigned-URL sampled decode of 90-min input) + `call_modal_movement` unified dispatch + local engine; msgpack profile artifact in R2 with storage ref; profile_db `movement_profiles` table + migration; admin/dev trigger + GET endpoint; testbed-parity test so prod recipe can't drift. |
| T5470 | ↳ [Annotate Activity Layer](tasks/movement-tracking/T5470-annotate-activity-layer.md) | 7 | 4 | 1.8 | TODO | [ ] | Canvas area-sparkline over the Annotate timeline (y = normalized score, max-pool downsampling so goal spikes survive), DEAD tinted / EMPTY hatched, session toggle. Read-only derived display — zero persistence; no profile = no layer, no errors. UI Designer pass required. |
| T5480 | ↳ [Smart Playback](tasks/movement-tracking/T5480-smart-playback.md) | 8 | 5 | 1.6 | TODO | [ ] | Pure client playbackRate state machine: 1x through ACTIVE, configurable 2-8x skim through DEAD (2s lead-in back to 1x), hard-skip EMPTY >2min with toast + undo; protected zones ±10s around top-decile spikes (never skip a goal); manual interaction suspends smart control. Exhaustively unit-tested pure hook + real-browser verify. |
| T5490 | ↳ [Upload Opt-in + Paid Add-on Gating](tasks/movement-tracking/T5490-upload-optin-pricing.md) | 6 | 5 | 1.2 | TODO | [ ] | LAST deliberately (dogfood free internally first): opt-in checkbox at Add Game with price, entitlement on existing credits rails (no parallel payment path), dispatch-on-upload-complete, auto credit-back on hard failure, admin free-flag for beta accounts, funnel analytics. |

### Dual-Camera Shared Games

Annotating a full game is the product's biggest time sink — only ~55-65% of a match is effective playing time; the rest is throw-in walks, resets, subs, and halftime. Paid opt-in at upload runs a Modal job producing a per-game movement profile (per-second activity score + ACTIVE/DEAD/EMPTY states); Annotate renders it as an activity layer (y proportional to movement) and smart playback skims dead time / skips halftime. Science project first: testbed + human-labeled ground truth + KPI gates (headline: >=99.5% play preservation — never speed past a goal) BEFORE anything touches the app. Signals: compressed-domain motion vectors, optical flow with camera-motion compensation, YOLO player tracking (weights already in our Modal image), tiny learned fusion + temporal smoothing. Epic tasks are STRICTLY ordered. Full research basis, library survey, label taxonomy, and decision gates: [tasks/movement-tracking/EPIC.md](tasks/movement-tracking/EPIC.md).

| ID | Task | Impact | Cmplx | Pri | Status | Migr | Description |
|------|------|------|------|------|------|------|------|
|  | **[Dual-Camera Shared Games](tasks/dual-camera/EPIC.md)** | 9 | 8 | 1.1 |  |  | Cross-account shared game: invite link → both parents upload → audio-aligned camera toggle in Annotate → clips extract from the camera you were watching → movement-profile-powered auto best-camera. |
| T5500 | ↳ [Shared Game Entity + Invite/Join Backend](tasks/dual-camera/T5500-shared-game-backend.md) | 7 | 5 | 1.4 | TODO | [x] | Postgres coordination object: `shared_games` + `shared_game_members` + `shared_game_videos` (wall_offset column), invite-token endpoints (create/token-info/join/revoke), idempotent join materializing a local game row with `shared_by` provenance (T5330), deferred no-account join via the pending-shares pattern. Coordinate token/claim plumbing with the Share the Game epic (T5720/T5730 — supersedes T4910). |
| T5510 | ↳ [Create + Join UX](tasks/dual-camera/T5510-create-join-ux.md) | 8 | 6 | 1.3 | TODO | [ ] | Create modal (two entry points: Add Game + game-card menu), navigator.share/copy link, `/shared-game/{token}` landing card (signed-in join, signup-deferred join, revoked state), two-row camera-slot status block on shared game cards. UI Designer pass; first-impression surface for invited strangers. |
| T5520 | ↳ [Upload Binding + Camera Propagation](tasks/dual-camera/T5520-upload-binding-propagation.md) | 8 | 7 | 1.1 | TODO | [x] | profile_db migration: `game_videos.camera` (uploader's member slot, unique (game,camera,seq)) + `games.shared_game_id`; upload finalize binds into the existing shared game + registers in Postgres; other member's `/load` materializes reference rows + storage refs (head-guarded, T4820) — no byte copies; sweep audited for multi-user refs; Annotate filtered to primary camera until T5540. |
| T5530 | ↳ [Camera Time Alignment](tasks/dual-camera/T5530-time-alignment.md) | 7 | 6 | 1.2 | TODO | [ ] | Audio cross-correlation auto-suggest (100 Hz RMS envelopes from R2 sources, confidence-scored, validated ±0.2 s on known-offset splits) + side-by-side confirm/nudge modal; per-video wall_offsets persisted only on the Confirm gesture; NULL offsets stay NULL (never fabricated 0s). |
| T5540 | ↳ [Annotate Camera Toggle](tasks/dual-camera/T5540-annotate-camera-toggle.md) | 9 | 6 | 1.5 | TODO | [ ] | The payoff: per-camera virtual timelines, pure `cameraTimeMap` (unit-tested first — pins the time model), flip-camera button + `C` shortcut preserving the game moment, coverage-gap/unaligned states, clips authored on camera B stored in primary time. Session state only; real-browser verified. |
| T5550 | ↳ [Clip Extraction From Active Camera](tasks/dual-camera/T5550-clip-from-active-camera.md) | 7 | 7 | 1.0 | TODO | [x] | `raw_clips.camera` (profile_db migration): clip what you were looking at — export resolves camera-B clips through a Python `camera_time_map` twin (shared test vectors) to camera B's source; loud failure on coverage/offset inconsistency (never silent wrong-camera export); characterization test pins camera-0 behavior; `_export_brilliant_clip` honors camera. |
| T5560 | ↳ [Auto Best-Camera Suggestions](tasks/dual-camera/T5560-auto-best-camera.md) | 6 | 6 | 1.0 | BLOCKED | [ ] | BLOCKED by T5460 + T5540. Per-source (blake3) profile keying agreed with T5460 NOW; testbed-validated activity-score proxy (fallback: YOLO bbox height); pure `useBestCamera` hook with conservative hysteresis (never strobe, never switch away from a goal); "better angle" badge + opt-in auto-camera mode; missing profile/alignment degrades to nothing. |

Two cameras film every game (one parent per team, standing next to each other) but each family only ever sees their own footage — far-side action stays mush. A Shared Game (name/date/time/location) coordinates two accounts via an invite link: each parent uploads their camera, both see both angles, and Annotate gains a playhead-preserving camera toggle. Sources stay content-addressed references (`games/{blake3}.mp4` — no copies); annotations are never shared; time alignment = shared wall-clock with per-video offsets, auto-suggested by audio cross-correlation (adjacent cameras hear the same game). Every link is also organic acquisition — the other parent signs up to join. Auto best-camera rides the Movement Tracking profiles (per-source keying agreed with T5460 up front). Epic tasks are STRICTLY ordered. Architecture decisions, UX flows, and time model: [tasks/dual-camera/EPIC.md](tasks/dual-camera/EPIC.md).

### Milestone: Framing/Overlay Clarity

Outcome-first UX pass on the two steps where non-technical parents fall off. P0 guarantees a zero-effort valid export and kills keyframe jargon; P1 fixes button naming, hides pro readouts, makes hints replayable; P2 adds presets + clearer manual affordances. Each phase is independently shippable.

*All tasks in this section are complete — rows archived to [PLAN-archive.md](PLAN-archive.md).*

### SEO & Organic Discovery

Growth work on the marketing site (`src/landing`), which shipped as static Astro on 2026-07-24. Not a bug milestone -- per the Priority Policy below it sits behind open infrastructure bugs, but it is genuinely actionable and the feedback loop is measured in weeks, so starting it early matters more than finishing it fast.

Ordered: instrumentation first so we can measure what we fix; then the two user-visible stalls we already traced; then the structural infra wins.

| ID | Task | Impact | Cmplx | Pri | Status | Migr | Description |
|------|------|------|------|------|------|------|------|
| T6370 | [SEO - get the landing pages actually indexed (GSC coverage cleanup + page value)](tasks/T6370-seo-indexing-gsc-cleanup.md) | 6 | 4 | 1.5 | TODO | [ ] | **Triggered by a Google Search Console email, 2026-08-02.** GSC names 4 new non-indexing reasons; the full report is 6 buckets / 40 URLs, and the number that matters is the 34 in "Discovered/Crawled - currently not indexed" against a **30-URL sitemap** -- i.e. effectively the whole marketing site is seen and declined. Live diagnosis done in the task file (curl against prod): the 3 reportable "Website" reasons are **working as designed** -- the 2 redirects are `www`->apex 301 + `/index.html` and `/soccer/` 307 canonicalization; the noindex page is `/guides`, correctly noindexed while `PUBLISHED_GUIDES` is empty (and correctly sitemap-filtered off the same data) though it is still **linked in the nav on every page**; the "alternate page with proper canonical" pair is most likely the homepage `https://reelballers.com` vs `.../` split that `astro.config.ts` already documents as cosmetic. The **401 could not be reproduced externally** (every probed app/api path returns 200 as Googlebot) -- needs the GSC export before anything is touched. The one genuine defect is unreported by the email: **`app.reelballers.com` is a soft-404 catch-all** -- no robots.txt (serves the SPA shell with HTTP 200), no robots meta, no canonical, 200 HTML for `/api/*` and for invented paths, and a `<title>Reel Ballers</title>` that violates `site.ts`'s "never Reel Ballers" entity rule; if the GSC property is a domain property it is feeding junk URLs into the report. Fixing it must not break share-link unfurls -- verify, do not assume. **Honest framing: the site was 9 days old when Google sent this**, so non-indexing is expected, not proof of breakage; the technical hygiene (Parts A/B) is cheap and bounded but will NOT by itself move indexing. The leverage is Part C -- measured template similarity is **109 of 156 identical rendered-text lines between `/soccer` and `/volleyball`** (~70% boilerplate across the 11-page sport family, which is the textbook cause of crawled-not-indexed), plus publishing the first guide, intentional internal linking toward `/soccer` (~75% of the audience), GSC index requests (IndexNow already fires on deploy but Google does not consume it), and new `verify-seo.mjs` invariants so the class cannot silently return. Success is measured as indexed-page count in GSC ~4 weeks after deploy, recorded back in the task file -- not "we shipped the fixes". |

#### Video Load Latency (from HAR 2026-06-17)

Traced from `Downloads/localhost.har`: the framing clip *appeared* to take ~4.3s due to a cold over-fetch from R2. **Debunked by the T3760 spike (2026-06-18):** that 4.3s was a HAR misread (`receive` time = playback-stream duration, not TTFF). Measured cold TTFF is 266 ms; the over-fetch is harmless. T3760 closed WON'T-FIX. The per-file tag/JS "downloads" the user noticed are a dev-mode Vite artifact (bundled+gzipped in prod) and are NOT a bottleneck — no task created for them.

> **Coordination:** T3760 + T3770 ship on branch `feature/perf-page-load` (separate conversations, disjoint files). They're part of the wider perf batch with the quest tasks — see [perf-batch-har-2026-06-17.md](tasks/perf-batch-har-2026-06-17.md).

*All tasks in this section are complete — rows archived to [PLAN-archive.md](PLAN-archive.md).*

---

## Priority Policy

**Bugs are always the first priority, especially infrastructure bugs (sync, data integrity, schema).** New features and structural changes should not begin until known bugs are resolved. The order is:

1. **Infrastructure bugs** - Sync failures, data loss, orphaned records, schema issues
2. **Test failures** - Broken tests indicate regressions; fix before adding more code
3. **UI/UX bugs** - Visible issues that affect the user experience
4. **Pre-deployment blockers** - Structural changes (T85) that must happen before real users
5. **New features** - Only after the above are clear

**Bug prioritization within a tier:** Rank by **infrastructure depth** — the deeper the bug (more systems depend on the affected layer), the higher the priority. A silent sync failure is worse than a slow sync, which is worse than a broken test.

When suggesting the next task, always check the Bug Fix Sprint section first. Do not recommend feature work while bugs remain open.

### Epic Prioritization

**Epics compete with other epics and standalone tasks at the milestone level.** Each epic gets aggregate Impact/Complexity/Priority scores based on the collective value and effort of its tasks.

- **Milestone level:** Epics and standalone tasks are ordered by Priority (Impact / Complexity). Higher priority = do first.
- **Within-epic level:** Tasks are ordered by **dependency** — foundational layers first, since all tasks in an epic touch similar code. DB/model changes before API, API before UI.
- **Impact over complexity:** When prioritizing within a milestone, favor high-impact work even if it's harder. The priority formula (Impact / Complexity) naturally rewards this.

---

## Completed Tasks

See [DONE.md](DONE.md) for all completed, superseded, and won't-do tasks.

---

### Epic: Auth Integrity (IN_PROGRESS) -- BUG FIX
[tasks/auth-integrity/EPIC.md](tasks/auth-integrity/EPIC.md)

Goal: Eliminate orphaned accounts by removing guest accounts entirely. Users must sign in (Google or OTP) before using the app.

*All tasks in this section are complete — rows archived to [PLAN-archive.md](PLAN-archive.md).*

### For Alpha - Video Streaming

Fix playback stalls observed on staging (2026-06-02). Video proxy bottleneck (~590 KB/s) causes buffering during game playback. Must fix before alpha testers hit this.

*All tasks in this section are complete — rows archived to [PLAN-archive.md](PLAN-archive.md).*

### For Alpha - Polish (IN_PROGRESS)
[tasks/for-alpha/EPIC.md](tasks/for-alpha/EPIC.md)

Goal: Get user feedback. Core functionality works, performance is acceptable, onboarding doesn't block users.

| ID | Task | Impact | Cmplx | Pri | Status | Migr | Description |
|------|------|------|------|------|------|------|------|
| T3750 | [Redo All E2E Tests](tasks/T3750-rewrite-new-user-flow-e2e.md) | 7 | 4 | 1.8 | TODO | [ ] | (Moved from Framing/Overlay Clarity; hold until now.) Audit + redo the full E2E suite, not just onboarding: rewrite new-user-flow.spec.js to the 3-quest flow + new event gestures + Export Highlight/Add Spotlight selectors; expect framing->overlay auto-advance; fix gallery/My Reels specs for Collections + ranking-game changes; sweep regression-tests.spec.js label selectors. |
| T1970 | [Annotate Mehdi Source Files](tasks/alpha-marketing/T1970-annotate-mehdi-source-files.md) | 8 | 2 | 4.0 | TODO | [ ] | Annotate, frame, and export Mehdi's game footage end-to-end to produce demo clips and before/after examples |
|  | **[Landing Page Polish](tasks/landing-page-redesign/EPIC.md)** | 7 | 4 | 1.8 |  |  | Before/after examples from Mehdi footage + tutorial video |
| T2330 | ↳ [Before/After Examples](tasks/landing-page-redesign/T2330-before-after-section.md) | 10 | 5 | 2.0 | TODO | [ ] | Add more before/after examples to existing section: diverse positions (keepers, defenders), synced loops |
| T3300 | ↳ [Build Tutorial Video for Landing Page](tasks/T3300-tutorial-video-landing-page.md) | 8 | 3 | 2.7 | TODO | [ ] | Build tutorial video and add to landing page |
|  | **[Analytics: Attribution & Access Visibility](tasks/analytics-attribution-viz/EPIC.md)** | 7 | 5 | 1.4 |  |  | Surface origination & access from existing tables: split games uploaded vs accessible, and a full attribution graph (who invited whom + ad campaign roots). |
| T3550 | ↳ [Games Uploaded vs Accessible](tasks/analytics-attribution-viz/T3550-games-uploaded-vs-accessible.md) | 6 | 3 | 2.0 | TODO | [ ] | Split admin "Games" into games uploaded vs games accessible (differ due to sharing). Add accessible count via share_games/shares join; show both in UserTable + UserDetailPanel. |
| T3560 | ↳ [User Attribution Graph](tasks/analytics-attribution-viz/T3560-attribution-graph.md) | 7 | 6 | 1.2 | TODO | [ ] | Node-link attribution graph: users + ad-campaign/origin roots as nodes, referrals as edges, colored by origin. Own lazy-loaded page (code-split) linked from main analytics so the graph lib/payload never slows the main page. New /attribution-graph endpoint + AttributionGraph.jsx (adds first graph viz lib). |
| T3570 | [Track Annotation Playback Frequency](tasks/T3570-annotation-playback-frequency-event.md) | 5 | 2 | 2.5 | TODO | [ ] | Add non-achievement `annotation_playback_started` flow event (once per playback session) so user_actions counts rewatch frequency. Existing `annotations_played` achievement only fires once ever. Feeds lifecycle-email classifier. |
|  | **[Lifecycle Onboarding Emails](tasks/lifecycle-emails/EPIC.md)** | 8 | 6 | 1.3 |  |  | Day 7/14/30 emails: thank, help-by-funnel-stage (no-games -> upload/teammate-share help; has-games-no-annotation -> annotate value; etc.), and beg for feedback w/ free credits. Personalized from activity log. Replies -> imankh@gmail.com. |
| T3580 | ↳ [Lifecycle Email Engine](tasks/lifecycle-emails/T3580-lifecycle-email-engine.md) | 8 | 6 | 1.3 | TODO | [x] | Resend reply_to support + email_sends dedup table (migration) + self-scheduling day-7/14/30 loop (sweep_scheduler pattern) + classify_user_state funnel-stage classifier + admin preview/force-send. |
| T3590 | ↳ [Day 7/14/30 Content & Personalization](tasks/lifecycle-emails/T3590-lifecycle-email-content.md) | 8 | 5 | 1.6 | TODO | [ ] | render_lifecycle_email: appreciation + help block per stuck_at stage + feedback ask (annotate/playback/create/frame/spotlight/share clarity, scoped to steps reached) + free-credit offer. Reuses _build_share_email design system. |
| T3595 | [Share Viewer Opt-In & Viewer Bucket](tasks/T3595-share-viewer-optin-bucket.md) | 7 | 6 | 1.2 | TODO | [x] | Opt-in CTA on shared link viewer pages ("learn how to make clips like this?"); opted-in viewers stored as leads + one-time how-to email. Passive viewers never emailed. share_viewer_leads table buckets viewers separately from users; converted_user_id links viewer->signup. Today share_viewed is sharer-attributed only. |
| T3290 | [Tune NUF for Returning Users](tasks/T3290-tune-nuf-returning-users.md) | 8 | 4 | 2.0 | TODO | [ ] | Differentiate new user flow for returning users vs first-timers. Conversation needed to scope. |
| T5080 | [JIT per-user migration (retire bulk sweep)](tasks/T5080-jit-migration.md) | 7 | 7 | 1.0 | TODO | [ ] | User direction 2026-07-13 (split from T5070). Move DB migrations from the admin-triggered bulk sweep (`run_all_migrations` loops EVERY user; missed/partial sweeps caused T4820/T4830 corruption) to just-in-time per-user migration hooked into T5070's refresh flow (step 5). Key enabler: the single-user primitive `_migrate_user(user_id)` ALREADY exists (bulk runner just loops it); seam = ensure_user_database/session-init; Postgres stays deploy-triggered, user_db/profile_db go JIT; preserve T4830 fail-loud/verify-at-head + define concurrency/idempotency. CUTOVER (user directive): run ONE last full batch migration across dev/staging/prod, THEN delete the bulk-migration code (run_all_migrations + admin migrate endpoint) — keep _migrate_user + versioned files; rewrite CLAUDE.md Migration System for JIT; ensure background jobs (expiry sweep) migrate-before-touch. Depends on T5070. L-tier, Architect gate. |

### Alpha Marketing

Final pre-alpha polish: source material, analytics review, returning-user experience, and landing page refinement.

| ID | Task | Impact | Cmplx | Pri | Status | Migr | Description |
|------|------|------|------|------|------|------|------|
| T1980 | [Alpha Outreach Email](tasks/alpha-marketing/T1980-alpha-outreach-email.md) | 9 | 3 | 3.0 | TODO | [ ] | "Save your best moments before you lose the videos" — email with demo clip link, feature screenshots, CTA to app |
| T1990 | [Alpha List](tasks/alpha-marketing/T1990-alpha-list.md) | 9 | 1 | 9.0 | TODO | [ ] | Contact list for alpha outreach: Zack, Arshia, Chris Choie, WhatsApp group, John Gleaves, Jack's dad, Jett's dad, current team, Shannon |

### For Launch — Infrastructure

Outreach to our network once alpha milestone is complete. Ordered: create source material first, then compose email, then send.

| ID | Task | Impact | Cmplx | Pri | Status | Migr | Description |
|------|------|------|------|------|------|------|------|
|  | **[Session Scaling Epic](tasks/session-scaling/EPIC.md)** | 8 | 5 | 1.6 |  |  | Session pinning + write-back R2 sync + data loss recovery. Makes per-user SQLite correct and fast at multi-machine scale. |
| T2250 | ↳ [Write-Back R2 Sync](tasks/session-scaling/T2250-write-back-r2-sync.md) | 8 | 5 | 1.6 | TODO | [ ] | Move R2 sync from blocking-per-gesture to periodic background (~3 min). Sync on sign-out, export, session invalidation. Writes respond in <5ms instead of ~200ms. |
| T40 | ↳ [Single Active Session Handoff](tasks/session-scaling/T40-single-active-session-handoff.md) | 8 | 5 | 1.6 | TODO | [ ] | Auto-signout old device on new login, sync R2 before 401, retry on failure, "signed in elsewhere" UX. Orchestrates device handoff end-to-end. |
| T2260 | ↳ [Data Loss Detection & Recovery](tasks/session-scaling/T2260-data-loss-detection-recovery.md) | 7 | 4 | 1.8 | TODO | [ ] | Detect version gaps on reconnect after crash. Auto-grant goodwill credits, notify user with clear explanation. |
| T1537 | ↳ [Consolidate Achievement POSTs (fire-and-forget analytics)](tasks/session-scaling/T1537-consolidate-achievement-posts.md) | 5 | 3 | 1.7 | BLOCKED | [ ] | Depends on T2250 (write-back). The achievement POST's ~610ms is synchronous `record_milestone` (Postgres + user.sqlite); it's already FE fire-and-forget so the user feels none of it. Fold the achievement INSERT into the action handler AND make the milestone emit fire-and-forget — a persistence-model change deferred until sessions are single-machine + write-back. Design ready: [T1537-design.md](tasks/T1537-design.md). Moved here from the Quests Latency epic. |
|  | **[Analytics System (OpenPanel)](tasks/analytics/EPIC.md)** |  |  |  |  |  | Replace CF Web Analytics with self-hosted OpenPanel: 41 events, 4-tier dashboards, credit-economy metrics, computed intelligence (churn/LTV/tiers), alerts |
| T1700 | ↳ [Foundation](tasks/analytics/T1700-foundation.md) | 6 | 5 | 1.2 | TODO | [ ] | Deploy OpenPanel VPS, SDK integration (frontend + backend), 8 activation events, L1 Daily Pulse dashboard, remove CF Web Analytics |
| T1701 | ↳ [Core Analytics](tasks/analytics/T1701-core-analytics.md) | 6 | 5 | 1.2 | TODO | [ ] | Full 41-event taxonomy, L2 Weekly Health dashboard (7 sections), session replay, quest funnels, admin panel -> OpenPanel links |
| T1702 | ↳ [Monetization + Intelligence](tasks/analytics/T1702-monetization-intelligence.md) | 6 | 5 | 1.2 | TODO | [ ] | Credit events, Stripe revenue tracking, nightly analytics engine (churn risk, engagement tiers, credit health, LTV), hourly/weekly alerts, viral attribution |
| T1703 | ↳ [Optimization](tasks/analytics/T1703-optimization.md) | 5 | 4 | 1.3 | TODO | [ ] | L3 deep-dive template, feature release protocol, aha moment regression, magic number testing (requires 200+ users) |
|  | **[R2 CDN Video Serving](tasks/r2-cdn/EPIC.md)** | 4 | 5 | 0.8 |  |  | Custom domain + HMAC auth + HTTP/2 + CDN caching. Presigned URL streaming done in T3250; this epic adds edge infrastructure. Re-scoped 2026-06-18 (T3760): latency justification debunked (measured TTFF 266 ms, seeks don't stall even saturated); surviving value = caching + egress-at-scale + auth = low-urgency infra. Impact 9->4. Depends on T3250. |
| T2550 | ↳ [CDN + Auth Worker](tasks/r2-cdn/T2550-r2-custom-domain-cdn.md) | 4 | 4 | 1.0 | TODO | [ ] | Custom domain (`cdn.reelballers.com`) + HMAC auth Worker + HTTP/2 + CDN caching. Auth-only Worker (no byte proxying). **Re-scoped 2026-06-18 (T3760):** HTTP/2 6-socket-cap latency story is empirically weak (seeks don't stall even saturated; R2 TTFB ~150 ms). Surviving value = CDN caching + egress-at-scale + HMAC auth, not a latency fix. Impact 8->4. |
| T2570 | ↳ [Remove Fly.io Video Proxy](tasks/r2-cdn/T2570-remove-flyio-video-proxy.md) | 4 | 3 | 1.3 | TODO | [ ] | Delete proxy endpoints made redundant by T3250 + CDN path. After CDN stable 2+ weeks. |
| T2580 | ↳ [Faststart Upload Validation](tasks/r2-cdn/T2580-faststart-upload-validation.md) | 6 | 2 | 3.0 | TODO | [x] | Validate faststart on upload, auto-remux if needed, store is_faststart flag. Prevents non-faststart regression after T3250 drops proxy moov windows. |
| T2270 | [Session Inactivity TTL](tasks/T2270-session-inactivity-ttl.md) | 5 | 2 | 2.5 | TODO | [ ] | Expire sessions after N days of inactivity using last_seen_at. Absorbs T420 inactivity portion. Depends on T1190. |
| T3440 | [Cache CORS Preflight Responses](tasks/for-launch/T3440-cors-preflight-caching.md) | 5 | 1 | 5.0 | TODO | [ ] | No Access-Control-Max-Age header -- browser fires OPTIONS preflight on every cross-origin request. 826ms overhead (17.8% of page time). One-line fix: add max_age=7200 to CORSMiddleware. |
| T1730 | [Performance Optimization Pass](tasks/for-launch/T1730-performance-optimization-pass.md) | 7 | 5 | 1.4 | TODO | [ ] | Pre-launch audit: slow endpoints, UI jank, bundle size, slow queries, unnecessary R2 round-trips |
| T2650 | [Move Sweep Auto-Export to Modal](tasks/T2650-sweep-to-modal.md) | 7 | 4 | 1.8 | TODO | [ ] | Sweep runs FFmpeg/recap on Fly.io via asyncio.to_thread — violates fast-server principle. Move auto-export compute to Modal; server becomes lightweight orchestrator (DB queries + Modal RPC). |

### Epic: Video Load Reliability (IN_PROGRESS) -- BUG FIX
[tasks/video-load-reliability/EPIC.md](tasks/video-load-reliability/EPIC.md)

Goal: Robust video loading — no misleading format errors, no oversized preloads, no CORS spam. Ordered by severity to user experience. Orchestrator-driven; each task gets its own branch and merges only after its before/after test proves effectiveness.

*All tasks in this section are complete — rows archived to [PLAN-archive.md](PLAN-archive.md).*

### Epic: For Launch (IN_PROGRESS)
[tasks/for-launch/EPIC.md](tasks/for-launch/EPIC.md)

Goal: Make money, virality, super polished. Most tasks here are yet to be generated based on alpha feedback.

#### Features

Scale, performance, and reliability — must be solid before feature work.

| ID | Task | Impact | Cmplx | Pri | Status | Migr | Description |
|------|------|------|------|------|------|------|------|
|  | **[Overlay 2](tasks/overlay-2/EPIC.md)** |  |  |  |  |  |  |
| T2180 | ↳ [Manual Telestration](tasks/overlay-v2/T2180-manual-telestration.md) | 6 | 5 | 1.2 | TODO | [ ] | Phase 2: freeze frame + draw arrow/circle/line, hold 1-2s, resume. Recruiting use case. CPU-only. |
| T2130 | ↳ [Player Label Overlay](tasks/overlay-v2/T2130-player-label-overlay.md) | 8 | 5 | 1.6 | TODO | [ ] | Name/number text tag following player tracker. Auto-positions, "minimal" and "broadcast" style presets. |
| T2140 | ↳ [Screen-Anchored Event Overlays](tasks/overlay-v2/T2140-screen-anchored-event-overlays.md) | 7 | 4 | 1.8 | TODO | [ ] | Score bug, GOAL/ASSIST badge, match metadata, time of play. Timestamp-triggered, corner-anchored. |

#### Marketing

| ID | Task | Impact | Cmplx | Pri | Status | Migr | Description |
|------|------|------|------|------|------|------|------|
| T445 | [Vehicle Window Cards](tasks/T445-business-cards.md) | 6 | 2 | 3.0 | TODO | [ ] | Design + print cards to place on vehicle windows at games promoting reelballers.com with QR code. Targets parents already at the field. |
| T1930 | [Influencer Marketing](tasks/marketing/T1930-influencer-marketing.md) | 8 | 4 | 2.0 | TODO | [ ] | Identify top influencers that youth soccer parents follow who align with video technology use. Outreach strategy + partnership plan. |

#### Completed

- T1150 Fix Pending Sync Retry No-Op — DONE
- T1152 Persist Sync-Failed State — DONE
- T1160 Clean Up Unused DB Rows — DONE
- T1170 Size-Based VACUUM on Init — DONE
- T1180 Fix NULL video_filename Root Cause — DONE
- T1200 Modal Job ID Logging & Retry — DONE
- T1380 Recover Orphaned Jobs Per-User at Startup — DONE
- T1390 Process Modal Queue Per-User at Startup — DONE
- T1130 Multi-Clip Stream Not Download — DONE
- T1120 Framing Video Cold Cache — DONE
- T1020 Fast R2 Sync — DONE
- T1010 Slow fetchProgress Response -- DONE

### Post Launch

| ID | Task | Impact | Cmplx | Pri | Status | Migr | Description |
|------|------|------|------|------|------|------|------|
| T6490 | [Pause during overlay text - hold a frame while text is on screen](tasks/T6490-pause-during-overlay-text.md) | 7 | 6 | 1.2 | TODO | [ ] | **DEFERRED 2026-08-04 by user - do this way later, NOT part of the Player Intro epic.** User request from staging: a per-block option holding the frame at the block's start so a callout can actually be READ. **Design already decided, do not re-litigate** (see task file): control stays in Overlay on the text block; the hold is a render-time expansion driven by an explicit piecewise time-remap; NOTHING is materialised. Freezing in Framing was rejected on GPU cost (frames duplicated before the upscaler cost ~20.4s of T4 each second held; a 5s pause = +102s wall-clock for identical frames). Writing the paused result back as a new working_videos version was rejected as DATA-DESTRUCTIVE - cleanup_database_bloat keeps only the latest version, so the un-paused original is pruned and pauses then compound (T4020's failure one table over). |

Target audience: highly engaged soccer parents with enough technical ability to use the app. Reach them where they already spend attention.

Landing Page Redesign core tasks (hero, nav, visual foundation) in For Alpha -- polish tasks (how-it-works, features cut, sample reels, FAQ) deferred to For Launch. Pricing dropped (freemium model).

| ID | Task | Impact | Cmplx | Pri | Status | Migr | Description |
|------|------|------|------|------|------|------|------|
| T710 | [Share with Coach](tasks/post-launch/T710-share-with-coach.md) |  |  | 1.2 | TODO | [x] | Coach account type + sharing: roster uploads, assign annotations to players, clip ratings, notes, send-back flow. Absorbs T1060 (Coaches View) |
| T720 | [Art Frames](tasks/T720-art-frames.md) |  |  | 1.1 | TODO | [x] | Draw on frozen clip frames (like a telestrator); shown during Play Annotations with a pause |
| T2170 | [Glow & Arrow Primitives](tasks/overlay-v2/T2170-glow-arrow-primitives.md) |  |  | 1.5 | TODO | [ ] | Soft radial glow aura + floating arrow pointer for wide shots. |
| T2190 | [Extended Presets](tasks/overlay-v2/T2190-extended-presets.md) |  |  | 2.5 | TODO | [ ] | "Recruiting" (minimal + persistent label) and "Social" (pulse + glow + broadcast name) presets. |
| T2200 | [Outline Trace Primitive](tasks/overlay-v2/T2200-outline-trace-primitive.md) |  |  | 0.7 | TODO | [ ] | Edge-detect player silhouette outline. Expensive to compute, premium-feel. |
| T2210 | [Spotlight Cone Primitive](tasks/overlay-v2/T2210-spotlight-cone-primitive.md) |  |  | 1.2 | TODO | [ ] | Darken/desaturate everything outside player region. High-drama cinematic effect. |
|  | **[Overlay 3](tasks/overlay-v2/EPIC.md)** |  |  |  |  |  | Composable overlay system: player labels, pulse rings, score bugs, event badges, presets. Clips look like pro TikTok/IG edits. |
| T2100 | ↳ [Composable Overlay Architecture](tasks/overlay-v2/T2100-composable-overlay-architecture.md) |  |  | 1.3 | TODO | [ ] | Refactor single ellipse into composable primitive system with common config, composition engine, stacking rules |
| T2120 | ↳ [Pulse Ring Primitive](tasks/overlay-v2/T2120-pulse-ring-primitive.md) |  |  | 2.3 | TODO | [ ] | Animated scale + opacity loop for dramatic moments (goals, big saves). 1-2s duration. |
| T2150 | ↳ [Overlay Presets System](tasks/overlay-v2/T2150-overlay-presets-system.md) |  |  | 2.0 | TODO | [ ] | One-click templates: "Spotlight", "Goal", "Custom". Wire up multiple primitives at once. |
|  | **[PWA Epic](tasks/pwa/EPIC.md)** |  |  |  |  |  | Background export + push notifications + background uploads + share target + offline playback |
| T443 | ↳ [Background Export Tracking](tasks/pwa/T443-background-sync.md) |  |  | 1.4 | TODO | [ ] | Export survives app close -- service worker tracks Modal job, notifies on completion. |
| T444 | ↳ [Push Notifications & Badges](tasks/pwa/T444-push-notifications-badges.md) |  |  | 1.6 | TODO | [ ] | Push for export complete + shared clips received. Badge count on app icon for pending items. |
| T447 | ↳ [Background Fetch for Uploads](tasks/pwa/T447-background-fetch-uploads.md) |  |  | 2.0 | TODO | [ ] | Multi-GB game uploads survive app close/switch. Parents upload at the field on cellular. THE differentiator. |
| T448 | ↳ [Share Target API](tasks/pwa/T448-share-target-api.md) |  |  | 2.3 | TODO | [ ] | Receive videos FROM camera roll directly into Reel Ballers upload flow. Eliminates file picker friction. |
| T449 | ↳ [Offline Reel Playback](tasks/pwa/T449-offline-reel-playback.md) |  |  | 2.0 | TODO | [ ] | Cache exported reels for offline viewing + persistent storage. Show reels without cell signal. |
| T1910 | ↳ [Tutorial Video](tasks/for-launch/T1910-tutorial-video.md) |  |  | 2.7 | TODO | [ ] | Record walkthrough video: upload game, annotate clips, frame, overlay, export. Embeddable on landing page and in-app onboarding. |

### Epic: Post Launch (TODO)
[tasks/post-launch/EPIC.md](tasks/post-launch/EPIC.md)

Improvements after real user traffic.

| ID | Task | Status | Pri | Migr | Description |
|------|------|------|------|------|------|
| T710 | [Share with Coach](tasks/post-launch/T710-share-with-coach.md) | TODO | 1.2 | [x] | Coach account type + sharing: roster uploads, assign annotations to players, clip ratings, notes, send-back flow. Absorbs T1060 (Coaches View) |
| T720 | [Art Frames](tasks/T720-art-frames.md) | TODO | 1.1 | [x] | Draw on frozen clip frames (like a telestrator); shown during Play Annotations with a pause |
| T2170 | [Glow & Arrow Primitives](tasks/overlay-v2/T2170-glow-arrow-primitives.md) | TODO | 1.5 | [ ] | Soft radial glow aura + floating arrow pointer for wide shots. |
| T2190 | [Extended Presets](tasks/overlay-v2/T2190-extended-presets.md) | TODO | 2.5 | [ ] | "Recruiting" (minimal + persistent label) and "Social" (pulse + glow + broadcast name) presets. |
| T2200 | [Outline Trace Primitive](tasks/overlay-v2/T2200-outline-trace-primitive.md) | TODO | 0.7 | [ ] | Edge-detect player silhouette outline. Expensive to compute, premium-feel. |
| T2210 | [Spotlight Cone Primitive](tasks/overlay-v2/T2210-spotlight-cone-primitive.md) | TODO | 1.2 | [ ] | Darken/desaturate everything outside player region. High-drama cinematic effect. |
|  | **[Overlay 3](tasks/overlay-v2/EPIC.md)** |  |  |  | Composable overlay system: player labels, pulse rings, score bugs, event badges, presets. Clips look like pro TikTok/IG edits. |
| T2100 | ↳ [Composable Overlay Architecture](tasks/overlay-v2/T2100-composable-overlay-architecture.md) | TODO | 1.3 | [ ] | Refactor single ellipse into composable primitive system with common config, composition engine, stacking rules |
| T2120 | ↳ [Pulse Ring Primitive](tasks/overlay-v2/T2120-pulse-ring-primitive.md) | TODO | 2.3 | [ ] | Animated scale + opacity loop for dramatic moments (goals, big saves). 1-2s duration. |
| T2150 | ↳ [Overlay Presets System](tasks/overlay-v2/T2150-overlay-presets-system.md) | TODO | 2.0 | [ ] | One-click templates: "Spotlight", "Goal", "Custom". Wire up multiple primitives at once. |
|  | **[PWA Epic](tasks/pwa/EPIC.md)** |  |  |  | Background export + push notifications + background uploads + share target + offline playback |
| T443 | ↳ [Background Export Tracking](tasks/pwa/T443-background-sync.md) | TODO | 1.4 | [ ] | Export survives app close -- service worker tracks Modal job, notifies on completion. |
| T444 | ↳ [Push Notifications & Badges](tasks/pwa/T444-push-notifications-badges.md) | TODO | 1.6 | [ ] | Push for export complete + shared clips received. Badge count on app icon for pending items. |
| T447 | ↳ [Background Fetch for Uploads](tasks/pwa/T447-background-fetch-uploads.md) | TODO | 2.0 | [ ] | Multi-GB game uploads survive app close/switch. Parents upload at the field on cellular. THE differentiator. |
| T448 | ↳ [Share Target API](tasks/pwa/T448-share-target-api.md) | TODO | 2.3 | [ ] | Receive videos FROM camera roll directly into Reel Ballers upload flow. Eliminates file picker friction. |
| T449 | ↳ [Offline Reel Playback](tasks/pwa/T449-offline-reel-playback.md) | TODO | 2.0 | [ ] | Cache exported reels for offline viewing + persistent storage. Show reels without cell signal. |
| T1910 | ↳ [Tutorial Video](tasks/for-launch/T1910-tutorial-video.md) | TODO | 2.7 | [ ] | Record walkthrough video: upload game, annotate clips, frame, overlay, export. Embeddable on landing page and in-app onboarding. |

---

## Environment Configuration

### Credentials (Found)

**R2 Storage** (in `.env`):
```
R2_ENABLED=true
R2_ACCESS_KEY_ID=4f5febce8beb63be044414984aa7a3b4
R2_SECRET_ACCESS_KEY=***
R2_ENDPOINT=https://e41331ed286b9433ed5b8a9fb5ac8a72.r2.cloudflarestorage.com
R2_BUCKET=reel-ballers-users
```

**Modal GPU** (in `~/.modal.toml`):
```
token_id=ak-Gr72Vz5gr7MYVpcUowSeDB
token_secret=***
```

### Fly.io Secrets (for T100)

```bash
fly secrets set --app reel-ballers-api-staging \
  R2_ENABLED=true \
  R2_ACCESS_KEY_ID=4f5febce8beb63be044414984aa7a3b4 \
  R2_SECRET_ACCESS_KEY=<from .env> \
  R2_ENDPOINT=https://e41331ed286b9433ed5b8a9fb5ac8a72.r2.cloudflarestorage.com \
  R2_BUCKET=reel-ballers-users \
  MODAL_ENABLED=true \
  MODAL_TOKEN_ID=ak-Gr72Vz5gr7MYVpcUowSeDB \
  MODAL_TOKEN_SECRET=<from ~/.modal.toml> \
  ENV=staging
```

---

## Task ID Reference

IDs use gaps of 10 to allow insertions:
- `T10-T79` - Feature tasks (complete)
- `T80-T99` - Pre-deployment blockers + bug fix sprint
- `T100-T199` - Deployment epic
- `T200-T299` - Post-launch features + polish
- `T400-T430` - User Auth epic (T400=Google, T401=OTP, T405=D1, T420=sessions, T430=settings)
- `T500-T525` - Monetization epic
- `T1700-T1705` - Open Panel Analytics epic
- `T2100-T2220` - Overlay System v2 epic
- `T2250-T2260` - Session Scaling epic
- `T2300-T2380` - Landing Page Redesign epic
- `T2400` - Grace Period for Expired Games
- `T2410-T2430` - Expired Game Experience epic
- `T2450-T2470` - Auto-Export Reliability epic
- `T2480` - Modal Spline Interpolation
- `T2550-T2570` - R2 CDN Video Serving epic
- `T2670` - Upload Slow Connection Optimization
- `T2680` - Remove Video Link Import (legal)
- `T2750` - Unified Multi-Video Experience
- `T2800-T2860` - Team Sharing Alpha epic
- `T2880-T2885` - Games List Performance epic
- `T2890` - Cache Warming Efficiency (standalone, warming system upgrade)
- `T2900-T2910` - Invite & Referral epic
- `T2915` - Sport Inheritance Through Invite (link snapshot: referrals.inherited_sport v017 + shares.sharer_default_sport v018; NOT a users.default_sport mirror)
- `T2920` - Migration System Infrastructure (standalone)
- `T2930` - Postgres Data Locality Audit (standalone)
- `T3000-T3020` - Analytics 1 epic (CF Web Analytics + Postgres event log + admin migration)
- `T3030` - Cross-Origin Fetch Credentials (bug fix)
- `T3080` - Sync User Activity to SQLite (dual-write activity to per-user SQLite)
- `T3050` - Multi-Video Blank Video (bug fix, no error handling on dual video elements)
- `T3060` - Make It Load Fast (Playwright perf benchmarks against prod)
- `T3260` - Edit Game Metadata Post-Upload
- `T3270` - Clip Boundary Visual Indicator (Annotate mode)
- `T3450-T3490` - Analytics Power-Up epic (normalize schema, action log, fill tracking gaps, admin redesign)
- `T3290` - Tune NUF for Returning Users (differentiate returning vs first-time)
- `T3300` - Build Tutorial Video for Landing Page
- `T3070` - Brand Messaging Audit (emails, preloading, landing page high concept)
- `T3420` - Profile Critical-Path Endpoints (375ms per-request baseline, auth/me 1774ms, bootstrap 741ms)
- `T3430` - Parallelize Game Load (4 sequential requests -> 1 game bootstrap endpoint)
- `T446-T449` - PWA new tasks (Screen Wake Lock, Background Fetch, Share Target, Offline Playback)
- `T3540` - Framing "In Progress" Visual Ambiguity (progress strip half-fill + wording)
- `T3550-T3560` - Analytics: Attribution & Access Visibility epic (games uploaded vs accessible, user attribution graph)
- `T3570` - Track Annotation Playback Frequency (recurring usage event)
- `T3580-T3590` - Lifecycle Onboarding Emails epic (day 7/14/30 engine + content/personalization)
- `T1516` - Suppress Export-Job Analytics During Impersonation (split from T1515; stamp impersonated flag on export job, skip completion milestone)
- `T3595` - Share Viewer Opt-In & Viewer Analytics Bucket (consent-based how-to email for opted-in viewers; viewer leads bucketed separately from users)
- `T3600-T3640, T3670` - Season Highlights & Collections epic, DONE (metadata freeze, collections tab, live shares, ranking, "Top Plays" + dynamic smart collections). T3635 (reel order editor) moved to For Alpha - Polish. T3650/T3660/T3680 dropped (custom mix rename, quest rework, stitch).
- `T6190-T6200` - **Project-open latency (HAR 2026-07-28)** — T6190 removes redundant frontend fetches on reel open; T6200 investigates the backend request serialization the same HAR exposed
- `T1536, T1537, T3760, T3770` - **Perf batch (HAR 2026-06-17)** — coordinated rollout across 2 branches / 3 conversations. See [perf-batch-har-2026-06-17.md](tasks/perf-batch-har-2026-06-17.md). Branch `feature/perf-quests-latency`: **T1536 DONE (deployed 2026-06-18)** as a correctness/DRY cleanup — HAR re-attribution showed `/progress` had no above-baseline server cost; **T1537 moved to the [Session Scaling epic](tasks/session-scaling/EPIC.md)** (BLOCKED on T2250 write-back, since its fix needs fire-and-forget analytics). Branch `feature/perf-page-load`: T3760 (framing clip cold-load over-fetch, re-opens T2560 clamp on latency grounds) + T3770 (StrictMode duplicate page-load fetch confirm, disjoint files).

See [task-management skill](../../.claude/skills/task-management/SKILL.md) for guidelines.
