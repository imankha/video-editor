# T9770: Persist Spotlight selection, settings and timing

**Status:** STAGING  
**Created:** 2026-09-13  
**Source task:** T01 / EP01  
**Source priority / type:** P1 / Fix  
**Readiness:** READY  
**Dependencies:** None

## Execution brief

Use the [full task brief](source/tasks/T01.md) or its [standalone visual HTML](source/tasks/T01.html), which contains the complete requirements, acceptance criteria, screenshots and schematics. These are alternative formats of one task. All assets are copied into this repository. Source task IDs are namespaced to this handoff; translate dependencies with the [project mapping](EPIC.md).

Before implementation, verify the current checkout and deployed version, reproduce the applicable observation, and reconcile existing work: **T9700, T9620**. These are related work candidates, not proof that the new finding is fixed or shares a root cause. Reuse verified implementations; record remaining scope rather than duplicating or undoing completed work. Source findings came from the September 12–13 evaluation and are not repository diagnoses.

The source readiness and dependency contracts remain binding even though the task board uses TODO for pending work. GATED_DISCOVERY authorizes a bounded investigation only; any production build needs a recorded decision and separately scoped tasks. Research execution requires real inputs. No implementation, tests, deployment or research completion is claimed by importing this task.

Apply the [integration rules](EPIC.md#integration-rules), especially reconciliation of existing naming/status decisions before changing product copy. Preserve this task's full acceptance criteria and evidence limits.

## Outcome record

- Status and verified remaining scope: fixed and merged to master (STAGING) for the confirmed
  mechanism (boundary-keyframe collision dropping `fromDetection`). Remaining scope: the source
  task's broader wording ("style and interval survive... a new signed-in session") is satisfied by
  this fix insofar as the ONLY identified failure mode was the `fromDetection` drop — no other
  persistence gap was found in the region/keyframe round-trip during investigation. A real e2e
  six-second-fixture journey (select -> export -> save -> reopen) is deferred to staging
  verification (no media fixtures available in the container) — not a merge blocker, noted for the
  next staging pass. T9780 (export-byte correctness) remains separately scoped and unaffected by
  this fix.
- Root cause or capability: Overlay's "selected player" is derived entirely from whether a
  highlight region's keyframes carry `fromDetection: true` at a detected player's timestamp
  (`detectionAssignment.js`). Backend `add_keyframe`'s UPDATE branch (existing keyframe found
  within `_find_keyframe_index`'s 0.02s tolerance — the common case when a selection click lands
  on a region's boundary scaffold keyframe) updated position/style but never set `fromDetection`,
  unlike the CREATE branch. On reload, the boundary-only assignment check read the keyframe as
  unassigned, resurfacing "Pick your player" (E40/E55) despite the position data being intact.
- Changed implementation paths / commit: `src/backend/app/routers/export/overlay.py` (`add_keyframe`
  UPDATE branch now sets `fromDetection` when present in the payload) + new
  `tests/test_t9770_spotlight_selection_persistence.py` + `.claude/knowledge/keyframes-framing.md`.
  Merged via PR #428, commit `5ca510aa` on `feature/T9770-fix-spotlight-selection-persistence`
  (merge commit `1b206423`).
- Tests and screenshot/media evidence: 51 relevant tests (4 new backend + 28 existing backend
  overlay-action regression + 19 frontend detectionAssignment/playerSelection), all passing.
  Supervisor-produced red/green proof: reverted `overlay.py` to pre-fix and reran the 4 new
  tests -> 1 failed (the exact boundary-collision reproduction), 3 passed as negative controls
  (CREATE branch unaffected, legacy no-`fromDetection` data still reads unassigned, a plain UPDATE
  without `fromDetection` never clears an existing true value); restored the fix -> 4/4 pass.
  Fresh-context Reviewer: APPROVED, 0 blocking/major/minor.
- Dependency-contract changes: none — additive JSON-blob field write, no schema change.
- Blockers, migration and rollback limits: none. Branch CI green (backend layer only; frontend
  correctly skipped for this diff — no known-flake attribution needed).

Update this record and the main PLAN.md row together. The source package is retained unchanged as provenance; current execution status belongs here, not in its original manifest or duplicate task formats.
