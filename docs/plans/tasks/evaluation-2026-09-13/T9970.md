# T9970: Establish highlight quality benchmark and calibrated fallback

**Status:** STAGING  
**Created:** 2026-09-13  
**Source task:** T21 / EP05  
**Source priority / type:** P2 / Investigation / quality gate  
**Readiness:** READY  
**Dependencies:** None

## Execution brief

Use the [full task brief](source/tasks/T21.md) or its [standalone visual HTML](source/tasks/T21.html), which contains the complete requirements, acceptance criteria, screenshots and schematics. These are alternative formats of one task. All assets are copied into this repository. Source task IDs are namespaced to this handoff; translate dependencies with the [project mapping](EPIC.md).

Before implementation, verify the current checkout and deployed version, reproduce the applicable observation, and reconcile existing work: **T9700**. These are related work candidates, not proof that the new finding is fixed or shares a root cause. Reuse verified implementations; record remaining scope rather than duplicating or undoing completed work. Source findings came from the September 12–13 evaluation and are not repository diagnoses.

The source readiness and dependency contracts remain binding even though the task board uses TODO for pending work. GATED_DISCOVERY authorizes a bounded investigation only; any production build needs a recorded decision and separately scoped tasks. Research execution requires real inputs. No implementation, tests, deployment or research completion is claimed by importing this task.

Apply the [integration rules](EPIC.md#integration-rules), especially reconciliation of existing naming/status decisions before changing product copy. Preserve this task's full acceptance criteria and evidence limits.

## Outcome record

- **Status and verified remaining scope:** Investigation deliverable COMPLETE
  (objective/measurable half). Delivered: fixture inventory, reproducible
  benchmark script + procedure, testable rubric, and a wider-frame / warning
  recommendation. BLOCKED sub-parts (marked, not fabricated): GAN-path quality
  (no CUDA/torch in container → no-GAN lower bound only), parent "Would you send
  this?" feedback, real child-identity confirmation, multi-fixture
  lighting/distance/camera calibration set. No product code shipped (research
  task) — the default-crop-box change, "Use a wider frame" affordance, and any
  conditional softness warning are flagged for future separately-scoped,
  evidence-gated tasks.
- **Root cause or capability (observation, not renderer diagnosis):** Measured
  on the authorized fixture, softness scales with the crop **enlargement factor**,
  not source content — the two source crops are comparably sharp (295 vs 279
  `lap_var`) and the ~33× quality gap opens only after enlargement (tight default
  205×365 → 810×1440 enlarged `lap_var` 3.18 vs wide 608×1080 105.0). The tight
  crop is the **product default** (`DEFAULT_CROP_SIZES["9:16"]=(205,365)`,
  `app/services/default_crop.py`), so the softest case is the zero-effort path.
  This is CONSISTENT WITH (not proof of) crop-enlargement being R3's dominant
  softness contributor; the Real-ESRGAN GAN was not exercised and is NOT claimed
  defective.
- **Deliverable paths / commit:** `scripts/quality_benchmark.py` (benchmark);
  `docs/plans/tasks/evaluation-2026-09-13/T9970-benchmark/{FIXTURE-INVENTORY,
  RUBRIC,FINDINGS}.md`; knowledge pointer added to `.claude/knowledge/modal-gpu.md`.
  Commit: see branch `feature/T9970-quality-benchmark`.
- **Tests and screenshot/media evidence:** Benchmark run on
  `formal annotations/test.short/wcfc-carlsbad-trimmed.mp4` (1920×1080, 1:29.322 —
  matches evaluator's supplied MP4); outputs = `results.json` + per-timestamp
  source|crop|enlarged montages + encoded mp4 per variant. `black_frac=0.0`
  (no black gaps). Not a CI-shipping task (no app behaviour changed); the
  benchmark run IS the test evidence.
- **Dependency-contract changes:** none. T9980/T9990 (GATED_DISCOVERY, depend on
  T9970) can now consume this evidence base.
- **Blockers, migration and rollback limits:** No schema/migration (tooling+docs
  only) → nothing to roll back. Key blocker on shipping any quality claim:
  GAN-inclusive multi-fixture calibration run is still required (FINDINGS.md §D/§F).

Update this record and the main PLAN.md row together. The source package is retained unchanged as provenance; current execution status belongs here, not in its original manifest or duplicate task formats.
