# T9800: Remove false export-required loading warning

**Status:** STAGING  
**Created:** 2026-09-13  
**Source task:** T04 / EP01  
**Source priority / type:** P1 / Fix  
**Readiness:** READY  
**Dependencies:** None

## Execution brief

Use the [full task brief](source/tasks/T04.md) or its [standalone visual HTML](source/tasks/T04.html), which contains the complete requirements, acceptance criteria, screenshots and schematics. These are alternative formats of one task. All assets are copied into this repository. Source task IDs are namespaced to this handoff; translate dependencies with the [project mapping](EPIC.md).

Before implementation, verify the current checkout and deployed version, reproduce the applicable observation, and reconcile existing work: **T9510, T9540, T9600**. These are related work candidates, not proof that the new finding is fixed or shares a root cause. Reuse verified implementations; record remaining scope rather than duplicating or undoing completed work. Source findings came from the September 12–13 evaluation and are not repository diagnoses.

The source readiness and dependency contracts remain binding even though the task board uses TODO for pending work. GATED_DISCOVERY authorizes a bounded investigation only; any production build needs a recorded decision and separately scoped tasks. Research execution requires real inputs. No implementation, tests, deployment or research completion is claimed by importing this task.

Apply the [integration rules](EPIC.md#integration-rules), especially reconciliation of existing naming/status decisions before changing product copy. Preserve this task's full acceptance criteria and evidence limits.

## Outcome record

- Status and verified remaining scope: fixed and merged to master (STAGING); the reported flash
  (E24/E25) is fixed by construction; the genuine-dirty case is preserved. Real-browser live-drive
  on staging still owed (the container had no backend/Postgres for a full paid-render pass) — not
  a merge blocker.
- Root cause or capability: `OverlayModeView.jsx`'s `showExportRequired` predicate
  (`!effectiveOverlayVideoUrl && framingVideoUrl && (hasFramingEdits || hasMultipleClips)`) could
  not distinguish "waiting for a just-exported working video to hydrate" from "genuinely never
  exported" — both leave `effectiveOverlayVideoUrl` falsy, and `hasFramingEdits` is a static
  persisted fact that never resets after a successful export. `OverlayScreen.jsx`'s
  `deriveOverlayVideoSource` (T9150) already computed `shouldWaitForWorkingVideo` for exactly this
  distinction but never threaded it into this one predicate.
- Changed implementation paths / commit: `src/frontend/src/modes/OverlayModeView.jsx` (added
  `shouldWaitForWorkingVideo` prop, gated the predicate on it), `src/frontend/src/screens/
  OverlayScreen.jsx` (threaded the existing value through) + a new
  `OverlayModeView.exportRequiredRace.test.jsx` + `.claude/knowledge/export-pipeline.md`. Merged
  via PR #427, commit `26bfbb65` on `feature/T9800-fix-export-required-race` (merge commit
  `85b21807`).
- Tests and screenshot/media evidence: 4 new regression tests (wait-window suppression x2,
  genuine-dirty preserved, default-omitted-prop behavior) + 20 existing regression tests (resetPill,
  playerSelection, focusOverlayTransition) all passing. Supervisor-produced red/green proof:
  reverted the 2 source files to pre-fix and reran the 4 new tests -> 2 failed (the exact
  suppression cases); restored the fix -> 4/4 pass. Fresh-context Reviewer: APPROVED, 0
  blocking/major, 1 minor (no action needed) — confirmed the predicate change is a pure
  AND-narrowing that can only remove banner displays, never add false ones.
- Dependency-contract changes: none.
- Blockers, migration and rollback limits: none. Branch CI's sole failure was the pre-existing
  `uploadManager.attachVideo.test.js` flake (12th confirmed hit, attributed in
  `docs/testing/known-failures.md`), unrelated to this diff — T10040 already tracks fixing it.

Update this record and the main PLAN.md row together. The source package is retained unchanged as provenance; current execution status belongs here, not in its original manifest or duplicate task formats.
