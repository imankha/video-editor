# T9820: Correct rating and clip-creation helper immediately

**Status:** STAGING  
**Created:** 2026-09-13  
**Source task:** T06 / EP02  
**Source priority / type:** P1 / Fix  
**Readiness:** READY  
**Dependencies:** None

## Execution brief

Use the [full task brief](source/tasks/T06.md) or its [standalone visual HTML](source/tasks/T06.html), which contains the complete requirements, acceptance criteria, screenshots and schematics. These are alternative formats of one task. All assets are copied into this repository. Source task IDs are namespaced to this handoff; translate dependencies with the [project mapping](EPIC.md).

Before implementation, verify the current checkout and deployed version, reproduce the applicable observation, and reconcile existing work: **T9450, T9520, T9630**. These are related work candidates, not proof that the new finding is fixed or shares a root cause. Reuse verified implementations; record remaining scope rather than duplicating or undoing completed work. Source findings came from the September 12–13 evaluation and are not repository diagnoses.

The source readiness and dependency contracts remain binding even though the task board uses TODO for pending work. GATED_DISCOVERY authorizes a bounded investigation only; any production build needs a recorded decision and separately scoped tasks. Research execution requires real inputs. No implementation, tests, deployment or research completion is claimed by importing this task.

Apply the [integration rules](EPIC.md#integration-rules), especially reconciliation of existing naming/status decisions before changing product copy. Preserve this task's full acceptance criteria and evidence limits.

## Outcome record

- Status and verified remaining scope: fixed and merged to master (STAGING); all 3 acceptance
  criteria verified with evidence, no remaining scope.
- Root cause or capability: `getRatingCaption`/`getEditRatingCaption` (`clipConstants.js`) claimed
  a false star-count threshold ("one more star creates a clip") at rating 4, independent of the
  actual creation-intent state (`createProject` toggle in create mode, `hasReel` in edit mode).
  Neither function was told the real boolean, so the copy could contradict the toggle (E47).
- Changed implementation paths / commit: `src/frontend/src/components/shared/clipConstants.js`,
  `src/frontend/src/modes/annotate/components/AnnotateFullscreenOverlay.jsx`,
  `src/frontend/src/modes/annotate/components/ClipDetailsEditor.jsx` (+ their tests, an e2e spec,
  and `.claude/knowledge/annotate.md`). Merged via PR #424, commit `768ed342` on
  `feature/T9820-fix-rating-clip-helper` (merge commit `b188a9bd`).
- Tests and screenshot/media evidence: 48 relevant unit tests (pure-function caption coverage +
  the two component layer tests) + e2e `T8490-star-semantics-caption.qa.spec.js` updated with an
  E47-pinning case. Supervisor-produced red/green proof: reverted the 3 source files to pre-fix
  and reran the 48 tests -> 15 failed (E47 test among them); restored the fix -> 48/48 pass.
  Fresh-context Reviewer: APPROVED, 0 BLOCKING/0 MAJOR, 2 MINOR (addressed in `768ed342`).
- Dependency-contract changes: none.
- Blockers, migration and rollback limits: none. Branch CI's sole failure was the pre-existing
  `uploadManager.attachVideo.test.js` flake (9th confirmed hit, attributed in
  `docs/testing/known-failures.md`), unrelated to this diff.

Update this record and the main PLAN.md row together. The source package is retained unchanged as provenance; current execution status belongs here, not in its original manifest or duplicate task formats.
