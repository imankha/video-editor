# T9960: Make single-athlete Spotlight a clear optional step

**Status:** STAGING  
**Created:** 2026-09-13  
**Source task:** T20 / EP05  
**Source priority / type:** P2 / UX implementation  
**Readiness:** READY (T9770, T9780 merged/STAGING)  
**Dependencies:** T9770, T9780

## Execution brief

Use the [full task brief](source/tasks/T20.md) or its [standalone visual HTML](source/tasks/T20.html), which contains the complete requirements, acceptance criteria, screenshots and schematics. These are alternative formats of one task. All assets are copied into this repository. Source task IDs are namespaced to this handoff; translate dependencies with the [project mapping](EPIC.md).

Before implementation, verify the current checkout and deployed version, reproduce the applicable observation, and reconcile existing work: **T9620, T9740**. These are related work candidates, not proof that the new finding is fixed or shares a root cause. Reuse verified implementations; record remaining scope rather than duplicating or undoing completed work. Source findings came from the September 12–13 evaluation and are not repository diagnoses.

The source readiness and dependency contracts remain binding even though the task board uses TODO for pending work. GATED_DISCOVERY authorizes a bounded investigation only; any production build needs a recorded decision and separately scoped tasks. Research execution requires real inputs. No implementation, tests, deployment or research completion is claimed by importing this task.

Apply the [integration rules](EPIC.md#integration-rules), especially reconciliation of existing naming/status decisions before changing product copy. Preserve this task's full acceptance criteria and evidence limits.

## Outcome record

- Status and verified remaining scope: implemented (branch `feature/T9960-spotlight-optional-clear`).
  Verified first: the literal "select the remaining players" instruction is GONE from master
  (T9620) — grep = 0 hits. The RESIDUAL all-player implication survived as a templated progress
  line in `OverlaySpotlightPanel.jsx` ("N of M players selected — click the remaining players to
  spotlight each too"), which the literal grep missed. That was the real remaining gap and is now
  fixed. The reason sentence (`STAGE_REASONS.SPOTLIGHT`, "Twenty-two kids in the same kit…") already
  existed (T9860) and was left as-is.
- Root cause or capability: AC1 defect was purely presentational (a copy line implying all players
  must be selected). The effect interval was already adjustable (timeline levers, T5649) and
  previewable (ordinary playback + T5250 always-on reveal envelope, so preview == export); this task
  only NAMED/surfaced it as a "Spotlight duration" readout. Selection persistence across reopen is
  owned by T9770 (backend `fromDetection` on both add_keyframe branches) + T9780 (frontend
  `restoreRegions` carries `fromDetection`) — both merged, not re-solved here.
- Changed implementation paths / commit: `src/frontend/src/config/displayNames.js` (EDITOR_PANELS:
  SELECT_PLAYER_DONE/ADD_MORE/OPTIONAL, SPOTLIGHT_DURATION/_HINT), `.../components/settings/
  OverlaySpotlightPanel.jsx` (affirmation replaces all-player imperative; pre-selection optionality
  line; duration readout as primary, styling secondary), `.../modes/OverlayModeView.jsx` (derive +
  pass `spotlightDurationSeconds` from the existing `spotlightSpan` prop). Tests updated in
  `OverlaySpotlightPanel.test.jsx`. Commit subject `T9960:`.
- Tests and screenshot/media evidence: `npx vitest run` on OverlaySpotlightPanel.test.jsx +
  OverlayModeView.playerSelection.test.jsx + useHighlightRegions.persistence.test.js → 3 files / 27
  tests passed. eslint clean on all changed files. Frontend production build clean. LIVE-DRIVE not
  run: this worker container has no backend venv, no shared dev Postgres, and no R2 player-detection
  fixtures, so the Overlay Spotlight flow (which needs a real working video + detections) cannot be
  exercised end-to-end here — documented as an environment limit per the brief's fixture-absent
  clause. The reopen/preview==export ACs are covered by the merged T9770/T9780/T5250 test suites.
- Dependency-contract changes: none. Builds on T9770/T9780 (merged) without modifying them.
- Blockers, migration and rollback limits: no schema change, no migration, no new default timing
  (AC4 satisfied by construction — nothing writes/rewrites a saved effect's interval). Rollback is a
  pure copy/derivation revert of 4 frontend files; no persisted user data is touched.

Update this record and the main PLAN.md row together. The source package is retained unchanged as provenance; current execution status belongs here, not in its original manifest or duplicate task formats.
