# T9900: Make progress and saving status readable and persistent

**Status:** STAGING  
**Created:** 2026-09-13  
**Source task:** T14 / EP04  
**Source priority / type:** P2 / UX implementation  
**Readiness:** READY (T9800 merged/STAGING)  
**Dependencies:** T9800

## Execution brief

Use the [full task brief](source/tasks/T14.md) or its [standalone visual HTML](source/tasks/T14.html), which contains the complete requirements, acceptance criteria, screenshots and schematics. These are alternative formats of one task. All assets are copied into this repository. Source task IDs are namespaced to this handoff; translate dependencies with the [project mapping](EPIC.md).

Before implementation, verify the current checkout and deployed version, reproduce the applicable observation, and reconcile existing work: **T9430, T9540, T9600**. These are related work candidates, not proof that the new finding is fixed or shares a root cause. Reuse verified implementations; record remaining scope rather than duplicating or undoing completed work. Source findings came from the September 12–13 evaluation and are not repository diagnoses.

The source readiness and dependency contracts remain binding even though the task board uses TODO for pending work. GATED_DISCOVERY authorizes a bounded investigation only; any production build needs a recorded decision and separately scoped tasks. Research execution requires real inputs. No implementation, tests, deployment or research completion is claimed by importing this task.

Apply the [integration rules](EPIC.md#integration-rules), especially reconciliation of existing naming/status decisions before changing product copy. Preserve this task's full acceptance criteria and evidence limits.

## Outcome record

- **Status and verified remaining scope:** implemented on `feature/T9900-progress-status-readable`.
  A Code-Expert audit found the task RECONCILIATION-heavy — three of four acceptance criteria were
  ALREADY satisfied by prior work, so only two genuine residual gaps needed code.
  - **AC1 (no speculative saved/ready): already satisfied.** `exportStore` COMPLETE is only ever set
    from a real backend ack; the share action is gated on `outputVideoId` (`GlobalExportIndicator:209`)
    so no dead share link is offered. Upload "Saved"/"Game ready!" fires on server ack
    (`uploadStore.onEntryComplete`); `UploadPreviewNotice` never shows "Saved" speculatively (T9430).
  - **AC2 (stage/estimate readable @699px + 200% zoom after toast dismissal): FIXED (evidence E19).**
    `GlobalExportIndicator` no longer crams name+stage+percent+ETA into one `truncate max-w-[180px]`
    line — each gets its own wrapping line, the fixed `max-w-[180px]` clamp is removed, and the card
    gained `max-w-[calc(100vw-2rem)]`. New `etaSlotText` + `EXPORT_PROGRESS.ETA_VARIES`
    ("Time remaining varies.") make the estimate slot honest and never blank/frozen.
  - **AC3 (no fake countdown / no duplicate job on retry): already satisfied.** T8510 ETA-honesty
    preserved; percent is always real; upload retry reuses the same entry; export "Retry connection"
    only re-checks status (never re-POSTs); `inFlightRef` + 409 + `completionFiredRef` guard dispatch.
  - **AC4 (no unsupported background-continuation promise): satisfied by omission.** No user-facing
    copy promises leaving the page; exports genuinely survive reload, uploads don't and don't claim to.
  - **Work item 3 (explain temporary preparation states): FIXED (evidence E09).** The create-in-flight
    disabled "Frame this clip" CTA now shows a "Preparing your clip..." caption
    (`ANNOTATE.PREPARING_CLIP`, `AnnotateFullscreenOverlay.jsx`).
  - **Work item 5 ("Export required" flash race): investigated, no change — T9800's fix is holding**
    (`OverlayModeView.jsx` `showExportRequired` gates on `!shouldWaitForWorkingVideo`, well-tested).
- **Root cause or capability:** the persistent surfaces already existed and were honest; the residual
  defect was purely presentational (fixed-width truncation of the estimate at narrow/zoomed viewports)
  plus one unexplained transient disabled state.
- **Changed implementation paths / commit:** `src/frontend/src/components/GlobalExportIndicator.jsx`,
  `src/frontend/src/config/displayNames.js`,
  `src/frontend/src/modes/annotate/components/AnnotateFullscreenOverlay.jsx` (+ two test files).
  Commit subject starts `T9900:`.
- **Tests and screenshot/media evidence:** relevant frontend unit set green — `GlobalExportIndicator`
  (17, incl. new etaSlotText + persistent-readability), `AnnotateFullscreenOverlay` invitation/mobile/
  stayOpen/strip (30), and the upload/export progress family (`UploadProgressIndicator`,
  `UploadPreviewNotice`, `ExportButtonView`, `exportProgressPresentation`, `uploadPresentation` — 61).
  No live-drive: this container has no browser/backend (same limit as prior wave workers); the
  699px/200%-zoom readability is asserted structurally (the fixed-width clamp is gone) and owed a
  staging live-drive.
- **Dependency-contract changes:** none. Reused existing job identity per the brief's T04/T9800
  contract; introduced no second job/progress store.
- **Blockers, migration and rollback limits:** none. Frontend-only, no schema/migration; rollback is a
  pure revert of the changed presentational files (no persisted data touched).

Update this record and the main PLAN.md row together. The source package is retained unchanged as provenance; current execution status belongs here, not in its original manifest or duplicate task formats.
