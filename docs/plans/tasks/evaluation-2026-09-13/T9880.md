# T9880: Separate private replay, publication and link sharing

**Status:** STAGING  
**Created:** 2026-09-13  
**Source task:** T12 / EP03  
**Source priority / type:** P2 / UX implementation  
**Readiness:** WAITING_FOR_DEPENDENCIES  
**Dependencies:** T9780, T9860, T9870

## Execution brief

Use the [full task brief](source/tasks/T12.md) or its [standalone visual HTML](source/tasks/T12.html), which contains the complete requirements, acceptance criteria, screenshots and schematics. These are alternative formats of one task. All assets are copied into this repository. Source task IDs are namespaced to this handoff; translate dependencies with the [project mapping](EPIC.md).

Before implementation, verify the current checkout and deployed version, reproduce the applicable observation, and reconcile existing work: **T9670, T9710, T9740**. These are related work candidates, not proof that the new finding is fixed or shares a root cause. Reuse verified implementations; record remaining scope rather than duplicating or undoing completed work. Source findings came from the September 12–13 evaluation and are not repository diagnoses.

The source readiness and dependency contracts remain binding even though the task board uses TODO for pending work. GATED_DISCOVERY authorizes a bounded investigation only; any production build needs a recorded decision and separately scoped tasks. Research execution requires real inputs. No implementation, tests, deployment or research completion is claimed by importing this task.

Apply the [integration rules](EPIC.md#integration-rules), especially reconciliation of existing naming/status decisions before changing product copy. Preserve this task's full acceptance criteria and evidence limits.

## Outcome record

- Status and verified remaining scope: GATED_DISCOVERY investigation complete, 2026-09-15. Full
  findings: `T9880-decision-record.md`. Verdict: REAL SCOPE — not implemented here.
- Root cause or capability: the T12 target publish->visibility-review->link-ready flow does not
  exist on the result surface; only the collection-scoped share modal has that state machine.
  Download (dropped T10000 scope) exists as a capability but isn't surfaced on the private result
  view.
- Changed implementation paths / commit: none (decision record only, PR #449).
- Tests and screenshot/media evidence: not applicable — no code shipped. Two findings flagged as
  needing live proof once implemented (share-token re-point behavior on re-export; env had no
  backend venv/Playwright).
- Dependency-contract changes: none. T9780/T9860/T9870 confirmed merged; T9670/T9710/T9740
  reconciled (not reopened).
- Blockers, migration and rollback limits: filed as **T10180** (L-tier, Architect gate) — see
  decision record §3. No schema/migration involved.

Update this record and the main PLAN.md row together. The source package is retained unchanged as provenance; current execution status belongs here, not in its original manifest or duplicate task formats.
