# T9950: Simplify manual framing and preview actual output before export

**Status:** WIP  
**Created:** 2026-09-13  
**Source task:** T19 / EP05  
**Source priority / type:** P2 / UX implementation  
**Readiness:** READY (T9940 was folded into T9860, merged 2026-09-14 — this dependency line is stale, corrected here)  
**Dependencies:** T9940 (see above)

## Execution brief

Use the [full task brief](source/tasks/T19.md) or its [standalone visual HTML](source/tasks/T19.html), which contains the complete requirements, acceptance criteria, screenshots and schematics. These are alternative formats of one task. All assets are copied into this repository. Source task IDs are namespaced to this handoff; translate dependencies with the [project mapping](EPIC.md).

Before implementation, verify the current checkout and deployed version, reproduce the applicable observation, and reconcile existing work: **T9610, T9550**. These are related work candidates, not proof that the new finding is fixed or shares a root cause. Reuse verified implementations; record remaining scope rather than duplicating or undoing completed work. Source findings came from the September 12–13 evaluation and are not repository diagnoses.

The source readiness and dependency contracts remain binding even though the task board uses TODO for pending work. GATED_DISCOVERY authorizes a bounded investigation only; any production build needs a recorded decision and separately scoped tasks. Research execution requires real inputs. No implementation, tests, deployment or research completion is claimed by importing this task.

Apply the [integration rules](EPIC.md#integration-rules), especially reconciliation of existing naming/status decisions before changing product copy. Preserve this task's full acceptance criteria and evidence limits.

## Outcome record

- **Status and verified remaining scope: RECLASSIFIED L-tier, blocked on an Architect design gate
  (2026-09-15).** A worker's Code Expert audit (in container `reel-task-t9950`, 0 commits made —
  it correctly self-blocked per its kickoff's stop-gate rather than implementing) found this is
  bigger than an M-tier copy/collapse task:
  1. **Copy conflict**: the brief's proposed "Framing point" term conflicts with the ALREADY
     SETTLED canonical term "Focus point" (settled twice, by T9550 and again by T9860's copy
     sweep). This task should use the existing settled term, not the brief's stale suggestion —
     low-risk call, not really an open question.
  2. **No moving output-aspect preview component exists today** — this is genuinely net-new UI/
     architecture, not a rewire of something existing.
  3. **No crop undo exists today** — also net-new, not a rewire.
  4. Segments/speed/trim are still prominent on the timeline (T9270 already collapsed straighten/
     dim/zoom/aspect/audio into the Settings rail, but not these three) — real restructuring work.
  5. **The genuine open design question**: the acceptance criterion "wider framing is reversible,
     AND persists, AND retains source bounds" is in tension. A schema-free version (recompute the
     wide framing live from the existing persisted crop-keyframe bounds, no new stored toggle) is
     POSSIBLE, but a clean one-tap "remembered" wider-frame toggle would need a NEW
     `working_clips` column (profile_db migration). Needs a real design decision: accept the
     schema-free/recompute interpretation (cheaper, no migration) or add the persisted toggle
     (more polished UX, needs Architect + Migration agent).
  - Total surface: ~7-8 files, a new preview pattern, a timeline restructure — Code Expert's own
    read is this crosses the M/L tier line per CLAUDE.md's classification table (new abstraction +
    real design forks), so an Architect design doc is required before implementation, not a
    unilateral worker pick.
- Root cause or capability: not a bug — this is new UX scope on top of already-correct code (no
  regression found; T5641's straighten-collapse pattern and T9270's Settings-rail collapse are the
  precedent to extend, not replace).
- Changed implementation paths / commit: none yet. Branch `feature/T9950-simplify-framing-preview`
  exists with 0 commits.
- Tests and screenshot/media evidence: not run — no implementation to test yet.
- Dependency-contract changes: none recorded yet; pending the Architect design doc.
- Blockers, migration and rollback limits: **BLOCKED on Architect design doc + user approval**
  (Stage 2). Migration agent will be needed IF the design settles on the persisted-toggle option
  for wider framing.

Update this record and the main PLAN.md row together. The source package is retained unchanged as provenance; current execution status belongs here, not in its original manifest or duplicate task formats.
