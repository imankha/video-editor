# T9930: Simplify upload and remove fresh-home distractions

**Status:** STAGING  
**Created:** 2026-09-13  
**Source task:** T17 / EP04  
**Source priority / type:** P2 / UX implementation  
**Readiness:** READY  
**Dependencies:** None

## Execution brief

Use the [full task brief](source/tasks/T17.md) or its [standalone visual HTML](source/tasks/T17.html), which contains the complete requirements, acceptance criteria, screenshots and schematics. These are alternative formats of one task. All assets are copied into this repository. Source task IDs are namespaced to this handoff; translate dependencies with the [project mapping](EPIC.md).

Before implementation, verify the current checkout and deployed version, reproduce the applicable observation, and reconcile existing work: **T9640, T8700, T8500, T9390**. These are related work candidates, not proof that the new finding is fixed or shares a root cause. Reuse verified implementations; record remaining scope rather than duplicating or undoing completed work. Source findings came from the September 12–13 evaluation and are not repository diagnoses.

The source readiness and dependency contracts remain binding even though the task board uses TODO for pending work. GATED_DISCOVERY authorizes a bounded investigation only; any production build needs a recorded decision and separately scoped tasks. Research execution requires real inputs. No implementation, tests, deployment or research completion is claimed by importing this task.

Apply the [integration rules](EPIC.md#integration-rules), especially reconciliation of existing naming/status decisions before changing product copy. Preserve this task's full acceptance criteria and evidence limits.

## Outcome record

- Status and verified remaining scope: implemented on `feature/T9930-simplify-upload-fresh-home`
  (branch awaiting supervisor push/CI/merge). All four acceptance criteria addressed:
  1. **Upload needs no metadata; cost/storage stay visible** — `GameDetailsModal.jsx` collapses
     Opponent/Date/Game Type/Tournament behind a closed `<details>` "Game details (optional)";
     the cost line ("N credits - keeps your video for 30 days" + Balance) and the file picker stay
     above it, always visible (T8500 pre-commit disclosure untouched). Submit still gated on the
     video alone.
  2. **Fresh home emphasizes upload over Invite/disabled destinations** — the front-and-center
     top-right "Invite" button was removed from the home controls cluster. Disabled Reels/Published
     tabs were already de-emphasized (disabled state + T9390 caption) and left as-is.
  3. **Invite reachable in a scope-appropriate secondary location, purpose correctly stated** —
     verified (code-expert + reviewer) that the global Invite is a REFERRAL/growth control
     (`shareInvite` -> `/api/me/invite-code` -> `?ref=` product link), NOT a game-scoped
     collaboration share. Moved into `ProfileDropdown`'s account menu as "Invite a friend".
  4. **Honest fallback title; existing highlight names preserved** — date no longer defaults to
     today and the `'Unnamed opponent'` placeholder is gone; an untouched opponent/date submits
     empty, and the backend titles the game "Game uploaded <upload date>" (never claiming a match
     opponent/date). Editing game metadata later regenerates only `games.name`, never clip/highlight
     names (already structurally isolated — no code needed).
- Root cause or capability: S11 metadata-before-value + dishonest default title; E03 fresh-home
  Invite distraction. Global Invite scope resolved to REFERRAL (not collaboration).
- Changed implementation paths / commit: `GameDetailsModal.jsx`, `ProfileDropdown.jsx`,
  `ProjectManager.jsx`, `games.py` (+ `format_short_date`/`upload_fallback_name` helpers); tests:
  new `ProfileDropdown.t9930.test.jsx`, `tests/test_t9930_game_upload_fallback_name.py`; updated
  `GameDetailsModal.videoFirst`/`.t8700`, `ProjectManager.homeTabDefaults`. Commit: see branch.
- Tests and screenshot/media evidence: 112 frontend unit tests pass; eslint 0 errors; backend
  pure-fn helpers verified with system python3 (+ a CI pytest added). Live-drive NOT possible — this
  container has no backend venv and no browser (same limit noted for T9630/T9810/T9850); staging
  live-drive owed after the supervisor merges.
- Dependency-contract changes: reverses T8700/T8955's always-visible metadata treatment on the
  create modal (documented in `annotate.md`); no schema change; no migration.
- Blockers, migration and rollback limits: none. Backward-compatible (empty opponent/date already a
  supported edge case per T7290). Rollback = revert the branch; no user data touched.

Update this record and the main PLAN.md row together. The source package is retained unchanged as provenance; current execution status belongs here, not in its original manifest or duplicate task formats.
