# T9810: Repair both game-invitation entry points

**Status:** STAGING  
**Created:** 2026-09-13  
**Source task:** T05 / EP01  
**Source priority / type:** P1 / Fix  
**Readiness:** READY  
**Dependencies:** None

## Execution brief

Use the [full task brief](source/tasks/T05.md) or its [standalone visual HTML](source/tasks/T05.html), which contains the complete requirements, acceptance criteria, screenshots and schematics. These are alternative formats of one task. All assets are copied into this repository. Source task IDs are namespaced to this handoff; translate dependencies with the [project mapping](EPIC.md).

Before implementation, verify the current checkout and deployed version, reproduce the applicable observation, and reconcile existing work: **T9560**. These are related work candidates, not proof that the new finding is fixed or shares a root cause. Reuse verified implementations; record remaining scope rather than duplicating or undoing completed work. Source findings came from the September 12–13 evaluation and are not repository diagnoses.

The source readiness and dependency contracts remain binding even though the task board uses TODO for pending work. GATED_DISCOVERY authorizes a bounded investigation only; any production build needs a recorded decision and separately scoped tasks. Research execution requires real inputs. No implementation, tests, deployment or research completion is claimed by importing this task.

Apply the [integration rules](EPIC.md#integration-rules), especially reconciliation of existing naming/status decisions before changing product copy. Preserve this task's full acceptance criteria and evidence limits.

## Outcome record

- Status and verified remaining scope: fixed and merged to master (STAGING); all 4 acceptance
  criteria verified with evidence. Real-browser live-drive on staging still owed (the container
  had no backend venv/Postgres for a full live pass) — not a merge blocker, noted for the next
  staging verification pass.
- Root cause or capability: the normal-view "Sharing settings"/"Share plays" buttons in
  `AnnotateModeView.jsx` called `onShare` (T2820 tagged-teammates sharing, gated on
  `hasTaggedClips` at the RENDER level, not the click handler) instead of `onSharePlayback` (the
  actual game-invitation `SharePlaybackDialog`, T2905) — clicking with zero tagged clips silently
  set state that rendered nothing, matching the reported "three clicks, no visible response"
  exactly. The fullscreen exit bar and `RecapPlayerModal`'s "Preview plays -> Share plays" path
  were already wired correctly, which is why that alternate path worked once.
- Changed implementation paths / commit: `src/frontend/src/components/SharePlaybackDialog.jsx`,
  `src/frontend/src/config/displayNames.js`, `src/frontend/src/modes/AnnotateModeView.jsx`,
  `src/frontend/src/screens/AnnotateScreen.jsx` (+ their tests and
  `.claude/knowledge/annotate.md`). Both buttons repointed to `onSharePlayback`; tagged-player
  sharing preserved via its own `hasTaggedClips`-gated affordance so no capability was silently
  dropped. Added an accurate permission-scope disclosure line and an actionable open-failure error
  state ("Game invitations couldn't open. Try again.") to the dialog. Merged via PR #425, commit
  `4371e6b0` on `feature/T9810-fix-game-invitation-entry-points` (merge commit `70130ce0`).
- Tests and screenshot/media evidence: 50 relevant tests (SharePlaybackDialog + the new
  AnnotateModeView wiring test + CTA/beacon-surface/tagged-teammates regression coverage).
  Supervisor-produced red/green proof: reverted the 4 source files to pre-fix and reran the 25
  directly-affected tests -> 12 failed; restored the fix -> 25/25 pass. Fresh-context Reviewer:
  APPROVED, 0 BLOCKING/0 MAJOR, 2 MINOR.
- Dependency-contract changes: none.
- Blockers, migration and rollback limits: none. Branch CI's sole failure was the pre-existing
  `uploadManager.attachVideo.test.js` flake (10th confirmed hit, attributed in
  `docs/testing/known-failures.md`), unrelated to this diff.

Update this record and the main PLAN.md row together. The source package is retained unchanged as provenance; current execution status belongs here, not in its original manifest or duplicate task formats.
