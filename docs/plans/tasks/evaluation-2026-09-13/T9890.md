# T9890: Make all result entry points recover and replay consistently

**Status:** STAGING  
**Created:** 2026-09-13  
**Source task:** T13 / EP03  
**Source priority / type:** P2 / Integration implementation  
**Readiness:** WAITING_FOR_DEPENDENCIES  
**Dependencies:** T9780, T9860, T9870

## Execution brief

Use the [full task brief](source/tasks/T13.md) or its [standalone visual HTML](source/tasks/T13.html), which contains the complete requirements, acceptance criteria, screenshots and schematics. These are alternative formats of one task. All assets are copied into this repository. Source task IDs are namespaced to this handoff; translate dependencies with the [project mapping](EPIC.md).

Before implementation, verify the current checkout and deployed version, reproduce the applicable observation, and reconcile existing work: **T9470, T9710**. These are related work candidates, not proof that the new finding is fixed or shares a root cause. Reuse verified implementations; record remaining scope rather than duplicating or undoing completed work. Source findings came from the September 12–13 evaluation and are not repository diagnoses.

The source readiness and dependency contracts remain binding even though the task board uses TODO for pending work. GATED_DISCOVERY authorizes a bounded investigation only; any production build needs a recorded decision and separately scoped tasks. Research execution requires real inputs. No implementation, tests, deployment or research completion is claimed by importing this task.

Apply the [integration rules](EPIC.md#integration-rules), especially reconciliation of existing naming/status decisions before changing product copy. Preserve this task's full acceptance criteria and evidence limits.

## Outcome record

- Status and verified remaining scope: GATED_DISCOVERY investigation complete, 2026-09-15. Full
  findings: `T9890-decision-record.md`. Verdict: REAL SCOPE, smaller than feared — not
  implemented here.
- Root cause or capability: entry points already funnel to one `CollectionPlayer` via four
  independent hand-built payloads (drift vector); E51 ("source substituted for finished") does
  NOT reproduce (naming collision, not a bug); loading/recovery already met by T9470. Real gaps:
  result title diverges (clip name vs game name) across paths, no back-to-game backlink, 5 copy
  strings uncentralized.
- Changed implementation paths / commit: none (decision record only, PR #449).
- Tests and screenshot/media evidence: not applicable — no code shipped. T13's own live
  cross-entry validation (reload/back/forward, artifact-ID compare, playback) could not run in
  this container (no backend venv/Playwright) and is mandatory for any future fix.
- Dependency-contract changes: none. T9780/T9860/T9870 confirmed merged; T9470/T9710 reconciled
  (not reopened); T9740 filed separately (unrelated FocusScreen bug).
- Blockers, migration and rollback limits: filed as **T10190** (M-L, coordinate with T9860's
  copy conventions). No schema/migration involved.

Update this record and the main PLAN.md row together. The source package is retained unchanged as provenance; current execution status belongs here, not in its original manifest or duplicate task formats.
