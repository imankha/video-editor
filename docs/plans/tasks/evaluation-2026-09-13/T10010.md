# T10010: Define activation metrics and instrument the missing funnel

**Status:** STAGING  
**Created:** 2026-09-13  
**Source task:** T25 / EP06  
**Source priority / type:** P2 / Analytics implementation  
**Readiness:** READY  
**Dependencies:** None

## Execution brief

Use the [full task brief](source/tasks/T25.md) or its [standalone visual HTML](source/tasks/T25.html), which contains the complete requirements, acceptance criteria, screenshots and schematics. These are alternative formats of one task. All assets are copied into this repository. Source task IDs are namespaced to this handoff; translate dependencies with the [project mapping](EPIC.md).

Before implementation, verify the current checkout and deployed version, reproduce the applicable observation, and reconcile existing work: **T7515, T8110**. These are related work candidates, not proof that the new finding is fixed or shares a root cause. Reuse verified implementations; record remaining scope rather than duplicating or undoing completed work. Source findings came from the September 12–13 evaluation and are not repository diagnoses.

The source readiness and dependency contracts remain binding even though the task board uses TODO for pending work. GATED_DISCOVERY authorizes a bounded investigation only; any production build needs a recorded decision and separately scoped tasks. Research execution requires real inputs. No implementation, tests, deployment or research completion is claimed by importing this task.

Apply the [integration rules](EPIC.md#integration-rules), especially reconciliation of existing naming/status decisions before changing product copy. Preserve this task's full acceptance criteria and evidence limits.

## Outcome record

- **Status and verified remaining scope:** implemented (M-tier). Missing funnel
  events instrumented using the EXISTING aggregates-only mechanism
  (`record_milestone` / `user_actions` / `user_action_log`); NO new Postgres
  table/column, NO external vendor, NO Migration agent needed (Code Expert
  confirmed the dedup/join requirement is a READ convention, not new persisted
  state — so no BLOCK). Measurement deliverable (activation defs + cohort queries
  + privacy guardrails) in `T10010-activation-metrics.md`.
- **Capability:** person-level, playback-validated activation is now measurable.
  `result_viewed` (a real watch, server-re-validated ≥2s/≥50%) is a DIFFERENT
  event from render validation (`export_completed`) and from publish
  (`share_completed`). Dedup = `COUNT(DISTINCT user_id)` over `result_viewed`.
- **Changed implementation paths:**
  - Backend: `app/analytics.py` (7 new `FLOW_EVENTS` engagement dims,
    `is_playback_viewed`, `CLIENT_FUNNEL_EVENTS`, `FUNNEL_CONTEXT_ALLOWED_KEYS`,
    `_sanitize_funnel_context`, `record_funnel_event`);
    `app/routers/telemetry.py` (`POST /api/telemetry/funnel-event`).
  - Frontend: NEW `utils/funnelEvents.js`; wired in `PublishedReelsPanel.jsx`,
    `containers/FocusContainer.jsx`, `components/DraftTile.jsx`,
    `screens/FocusScreen.jsx`, `screens/OverlayScreen.jsx`.
  - Docs: `T10010-activation-metrics.md`; knowledge `annotate.md` +
    `export-pipeline.md`.
- **Tests:** `tests/test_t10010_activation_funnel.py` (viewed-threshold table,
  PII-sanitizer drop, beacon 204/never-500, registration, and the DB-backed
  AC1 dedup + AC3 distinctness + impersonation-zero-footprint — the last group
  runs green in CI where Postgres is available; container has no Postgres).
  `utils/funnelEvents.test.js` (15 tests, all pass locally). 189 related
  frontend tests pass; eslint/ruff clean; backend import OK.
- **Dependency-contract changes:** none (reused existing infra).
- **Blockers / rollback limits:** additive instrumentation only — reverting any
  call site loses that event but touches no user data or persisted schema. NOTE:
  T9950 (Focus simplification) had NOT landed at implementation time; the two
  framing call sites are flagged in-code + in the metrics doc for re-verify.

Update this record and the main PLAN.md row together. The source package is retained unchanged as provenance; current execution status belongs here, not in its original manifest or duplicate task formats.
