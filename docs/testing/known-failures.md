# Known Test Failures Baseline (master)

Purpose: stop re-litigating pre-existing failures. Workers and CI compare
against THIS list instead of arguing "it was already broken" per task.

Rules:
1. An entry may be added only with evidence it fails on **master** (run it on
   a clean master checkout, paste the failure line).
2. Every entry is debt: each should eventually become a task and be burned
   down. Delete the row when fixed.
3. `branch-ci.yml`'s `--deselect` list must stay in sync with the rows marked
   "deselected in CI".

Note (fresh throwaway Postgres): the conftest `pg_conn` fixture requires the
`schema_migrations` table to exist before the migration RUNNER runs. It handles
this by executing `_SCHEMA_DDL` (idempotent `CREATE TABLE IF NOT EXISTS`) first,
so a brand-new throwaway PG (`docker run postgres:16`, or a local
`initdb`-created cluster) is schema-first safe out of the box — no manual
`init_pg_schema()` call needed before the first pg-backed test.

| Layer | Test | Failure | Root cause | Handling |
|-------|------|---------|-----------|----------|
| Backend | `test_collection_metadata.py::test_stamps_aspect_ratio_and_tags` | ffprobe not found | Needs ffmpeg on PATH | CI installs ffmpeg; local Windows devs need it on PATH |
| Backend | `test_t4050_durable_sync.py::<PATCH /watched R2-async timing test>` | `AssertionError: watched blocked on R2 (1672ms) — should be async, assert 1672ms < 500ms` | CI-runner network flake on a real R2 round-trip timing assertion (500ms budget), not a code regression | Confirmed via same-SHA rerun: T6630 branch commit `06c9111a` failed this ONE test 2026-08-08 04:45 UTC, then passed clean (all tests green) on an identical rerun of the same commit minutes later. T6630's diff touches neither `db_sync.py` nor `downloads.py` at all, so the branch cannot be the cause. Re-run isolated/rerun-the-job if seen again. |
| Backend | `test_vacuum_on_signout.py::test_logout_fires_vacuum_when_user_archived` | `AssertionError: Expected '_vacuum_user_dbs' to be called once. Called 0 times.` — underlying cause: `app/analytics.py::close_session` raised inside `with get_pg() as conn:` ("Failed to close session for user-abc"), a Postgres connection-pool hiccup on the CI runner, not application logic | CI-runner Postgres connectivity flake, not a regression | Hit once on T5230's Branch CI (run 31282360965, commit `fa6fab34`); confirmed via `gh run rerun --failed` on the SAME commit/run — green on retry with zero code changes, proving non-determinism rather than a real failure. T5230's diff never touches `analytics.py`, `vacuum`, or session-close code (grep-confirmed). Re-run isolated if seen again. 2nd confirmation: hit on T4947's Branch CI (run 31854920755, commit `f1c78a78`) 2026-08-15; T4947's diff (`collections.py`, `serve_time_video.py`, `storage.py`) also never touches `analytics.py`/vacuum/session-close (grep-confirmed) — rerun triggered |
| Frontend | `src/stores/gamesDataStore.test.js::returns cached in-flight promise for same gameId` (and, seen on a later same-day rerun of the full suite: `gamesDataStore.test.js::fires fresh request after previous completes`, `src/stores/profileStore.test.js::switchProfile > calls API and updates currentProfileId`, `src/utils/cacheWarming.test.js::deduplicates same URL across tier1 and games queues`) | Hook/test timeout (5000-10010ms) in the full parallel unit suite; count varies per run (1 to 4 of these fail depending on machine load) | Load-related flake under full-suite parallelism, not a real regression: every one of these passed in an isolated re-run (`npx vitest run <file...>`, 47/47 passed in 6.98s) | Re-run isolated if seen in CI; none of these files are touched by T5700's diff |
| Frontend | `src/stores/profileStore.test.js::createProfile > calls API with name and color, returns new profile` (and other cases in the same file) | `Error: [vitest] No "useIntroCardStore" export is defined on the "./index" mock` inside `profileStore.js`'s `_resetDataStores`, caught and logged as `[ProfileStore] Failed to switch profile`/`Failed to fetch profiles`, not re-thrown | Same class as the row above: a `./index` (stores barrel) mock built by one test file leaks into `profileStore.test.js` under full-suite parallelism because the mock doesn't return every store the barrel now exports (`useIntroCardStore` added by the intro-card work). Confirmed via isolated re-run: T8838 branch commit `21eb3b88`, `npx vitest run src/stores/profileStore.test.js` -> 17/17 passed in 1.08s, same error logged internally but non-fatal to the test | Re-run isolated if seen in CI; T8838's diff touches `uploadManager.js`/`shrinkCapability.js`/`analytics.py` only, never `profileStore.js`/`introCardStore.js`/`stores/index.js` (grep-confirmed) |
| Backend | `test_background_sync.py::TestWriteLockDoesNotBlockOnSync::<gather 5 concurrent deletes, assert response time>` | `AssertionError: syncs should still be running when responses return — assert 2 == 0` | Wall-clock timing assertion (5 concurrent 10ms-lock-held requests must return in <250ms while their 200ms background R2 syncs are still in flight) is CI-runner-load sensitive, not a code regression | Confirmed via same-SHA rerun: T4340 branch commit `744a5ddc` failed this ONE test 2026-08-21 ~21:24 UTC (full suite also ran 3x slower than the prior run on the same branch, 13:30 vs 4:44, pointing at a loaded runner), then passed clean (`gh run rerun --failed` on the identical run/commit, conclusion `success`) minutes later with zero code changes. T4340's diff touches `clips.py`/`highlight_transform.py`/migrations/tests only — never `db_sync.py` or `test_background_sync.py` (grep-confirmed). Re-run isolated if seen again. |
| Backend | `test_t6200_concurrency.py::test_authed_burst_larger_than_pool_does_not_503` | `sqlite3.OperationalError: database is locked` | Burst-concurrency test racing SQLite locking is CI-runner-load sensitive, not a code regression | Confirmed via same-SHA rerun: T7910 branch (`feature/T7910-signup-referrer-attribution`) run 33147172811 failed this ONE test 2026-08-28 06:19 UTC, then passed clean on `gh run rerun --failed` of the identical run/commit minutes later. Also passed in an isolated local run. T7910's diff touches `analytics.py`/`auth.py`/`App.jsx`/`PageLayout.astro` only — never SQLite connection/locking code (grep-confirmed). Re-run isolated if seen again. |
| Frontend | `src/services/uploadManager.attachVideo.test.js::attachVideoToExistingGame (T8700, new helper) > runs hash -> upload -> addVideosToGame in order, then reloads the game` | `Error: Unexpected status: undefined` thrown from `ensureVideoInR2` (`uploadManager.js:697`), reached via `attachVideoToExistingGame` (`uploadManager.js:1202`) | The test's mocked `prepare-upload` response no longer matches an `UPLOAD_STATUS` shape `ensureVideoInR2` expects — a real drift between the test's fixture and the current upload-status contract, not something either T8910 or T8960 touched | Confirmed on a clean current-master checkout (commit `cccaadde`, after T8910 merged): `npx vitest run src/services/uploadManager.attachVideo.test.js` from `src/frontend` reproduces the identical failure in isolation (1 failed, 2 passed), independent of any branch. First hit on T8910's Branch CI (run 34166349241) and T8960's Branch CI (run 34166361508) the same day; neither branch's diff touches `uploadManager.js`'s `ensureVideoInR2`/`attachVideoToExistingGame` upload-status handling (T8910 only extends the function to accept a file list; T8960 doesn't touch the file at all — grep-confirmed both). Needs a real fix to the test's mock fixture (or the contract it asserts), tracked as debt here in the meantime. Also hit on T9740's Branch CI (run 34669205336) 2026-09-12; T9740's diff touches only `FocusScreen.jsx`, `scheduleExportWhenReady.js`(`.test.js`), `focusPublishExit.test.jsx`, and `.claude/knowledge/export-pipeline.md` — never `uploadManager.js` (grep-confirmed). Hit AGAIN on T9740 fix v3 Branch CI (run 34679428740) 2026-09-12, sole failure with 343/344 test files passing; that diff touches `App.jsx`, `ExportButtonContainer.jsx`, `usePublishProject.js`, `handleOverlayExportCompletion.js` and their tests - never `uploadManager.js` (grep-confirmed). Hit AGAIN on T9750's Branch CI (run 34681974281) 2026-09-12, sole failure with 343/344 test files passing (3351/3352 tests); that diff touches `highlight_transform.py`, `routers/exports.py`, `creditStore.js`, `BuyCreditsModal.jsx`, `ExportButtonContainer.jsx` (the credit-estimate branch only) and their tests, plus copy files - never `uploadManager.js`'s upload-status handling (grep-confirmed). Hit AGAIN on T9575's Branch CI (run 34705447938) 2026-09-12, sole failure with 343/344 test files passing (3357/3358 tests); that diff touches only `quest_config.py`, `QuestPanel.jsx`, `questDefinitions.jsx`/`.js`/`.test.jsx`, and `.claude/knowledge/backend-services.md` - never `uploadManager.js` (grep-confirmed). Hit AGAIN on T9285's Branch CI (run 34732711197) 2026-09-13, sole failure with 348/349 test files passing (3403/3404 tests); that diff touches `App.jsx`, `FocusScreen.jsx`, `FocusCompletionRecovery.jsx`, `focusCompletionStore.js`, `useExportRecovery.js`, `recoveredExportCompletion.js`, `resumeFocusCompletion.js`, `focusCompletionOffer.js` and their tests, plus `.claude/knowledge/export-pipeline.md` - never `uploadManager.js` (grep-confirmed). Hit AGAIN on T9480's Branch CI (run 34734739229) 2026-09-13, sole failure with 358/359 test files passing (3756/3757 tests); that diff touches `utils/timeFormat.js`, `modes/annotate/trimBounds.js`, `modes/annotate/components/TrimTimeField.jsx`/`ClipScrubRegion.jsx`/`ClipDetailsEditor.jsx`, `modes/FocusModeView.jsx`, `BuyCreditsModal.jsx`, `ExportButtonView.jsx`, `config/displayNames.js` and their tests, plus knowledge docs - never `uploadManager.js` (grep-confirmed). Hit AGAIN on T9820's Branch CI (run 34797518826) 2026-09-14, sole failure with 363/364 test files passing (3806/3807 tests); that diff touches `components/shared/clipConstants.js`, `modes/annotate/components/AnnotateFullscreenOverlay.jsx`/`ClipDetailsEditor.jsx` (rating-caption create-intent wiring only) and their tests, plus `.claude/knowledge/annotate.md` - never `uploadManager.js` (grep-confirmed). Hit AGAIN on T9810's Branch CI (run 34798090473) 2026-09-14; that diff touches `components/SharePlaybackDialog.jsx`, `config/displayNames.js`, `modes/AnnotateModeView.jsx`, `screens/AnnotateScreen.jsx` (game-invitation button wiring + disclosure/error copy only) and their tests, plus `.claude/knowledge/annotate.md` - never `uploadManager.js` (grep-confirmed). Hit AGAIN on T9840's Branch CI (run 34798897909) 2026-09-14, sole failure with 364/365 test files passing (3812/3813 tests); that diff touches `components/shared/clipConstants.js`, `config/displayNames.js`, `modes/AnnotateModeView.jsx`, `modes/annotate/components/AnnotateFullscreenOverlay.jsx`, `modes/annotate/hooks/useAnnotate.js` (capture-window constants + copy only) and their tests, plus `.claude/knowledge/annotate.md` - never `uploadManager.js` (grep-confirmed). **FIXED by T10040 (2026-09-13) - do NOT attribute any new hit of this test to this row; a failure after this fix is a REAL regression.** Root cause was not an upload-status contract drift at all: T8838 added a fire-and-forget `probeAndReport(...)` inside `ensureVideoInR2` that posts to `/api/telemetry/impression` BEFORE `prepare-upload`, and the test queued its responses positionally with `mockResolvedValueOnce`, so the beacon consumed the `prepare-upload` fixture and `prepare-upload` received the addVideosToGame shape (no `status` field) -> `Unexpected status: undefined`. The beacon is never awaited, which is why the shift only landed under some schedulings (flaky in CI, deterministic locally by the time it was investigated). Fixed test-side by routing mock responses BY URL instead of positionally (matching the already-stable sibling `uploadManager.attachFootage.test.js`), so a future added call cannot shift the queue and an unrouted call fails loudly. **This row is retained only until 2-3 consecutive green Branch CI runs confirm the fix, then delete it.** Hit AGAIN on T9800's Branch CI (run 34807846922) 2026-09-14, sole failure with 366/367 test files passing (3825/3826 tests); that diff touches `modes/OverlayModeView.jsx`, `screens/OverlayScreen.jsx` (threading `shouldWaitForWorkingVideo` into the export-required predicate only) and their tests, plus `.claude/knowledge/export-pipeline.md` - never `uploadManager.js` (grep-confirmed). Hit AGAIN on T9790's Branch CI (run 34808511078) 2026-09-14, sole failure with 366/367 test files passing (3829/3830 tests); that diff touches `components/FocusCompletionRecovery.jsx`, `containers/ExportButtonContainer.jsx`, `screens/FocusScreen.jsx`, `screens/focusCompletionOffer.js`, `stores/focusCompletionStore.js`, `utils/resumeFocusCompletion.js`, new `utils/acknowledgeExportJob.js` (live-completion acknowledge-on-gesture + Option C auto-navigate removal) and their tests, plus `.claude/knowledge/export-pipeline.md` - never `uploadManager.js` (grep-confirmed). |
| Frontend | Branch CI's `Unit tests (vitest, retry absorbs known flakes)` step overall — reported "originating in" `src/components/UploadPreviewNotice.test.jsx` via `src/utils/clipDisplayName.js` / `src/modes/annotate/constants/soccerTags.js` | `Vitest caught 1 unhandled error during the test run` -> `Unhandled Rejection: [vitest-worker]: Closing rpc while "fetch" was pending` (a Vitest module-runner RPC race on worker teardown); step exits 1 even though every reported Test File and Test passed (`368 passed (368)` / `3834 passed (3834)`) | GitHub Actions runner-load-sensitive Vitest module-runner race, not a code regression: the module named as the "origin" is incidental (whichever dynamic import happens to be resolving when a worker's RPC channel closes), not a real failure in that file | Confirmed on T9920's Branch CI (run 34818652529) 2026-09-14, TWICE (original run + `gh run rerun --failed` on the identical commit `c9bd1f5c`) — both times zero actual test failures, same "Closing rpc" signature. Confirmed NOT reproducible locally: `npx vitest run` (full suite) inside the task container on the same commit -> 368/368 files, 3834/3834 tests, exit 0, no unhandled rejection at all (less resource contention than the CI runner). Confirmed absent from the merge-base's own clean run (Master CI on `bd4f88fb`, and T9780's Branch CI on the same base) — not a general always-on flake, but load/timing-dependent. T9920's diff never touches `UploadPreviewNotice.jsx`/`clipDisplayName.js`/`soccerTags.js`, and `vite.config.js` excludes `e2e/**` from vitest entirely, so T9920's e2e-only additions cannot affect this. Re-run (`gh run rerun --failed`) if seen again; if it starts reliably reproducing locally too, escalate — that would mean an actual Vitest/dependency version issue, not CI-load noise. Hit AGAIN on T10300's Branch CI (run 35301302705) 2026-09-18, identical signature (`385 passed (385)` / `3969 passed (3969)`, 1 unhandled rejection); T10300's diff (`clips.py`, `games.py`, `projects.py`, `DraftTile.jsx`, `LinkClipToGameModal.jsx`, `useRawClipSave.js`, migration v053) never touches `UploadPreviewNotice.jsx`/`clipDisplayName.js`/`soccerTags.js` (grep-confirmed) — re-ran the failed job, no code change. Hit AGAIN on T10820's Branch CI (run 35566236547) 2026-09-21, identical signature (`409 passed (409)` / `4150 passed (4150)`, 1 unhandled rejection originating in `UploadPreviewNotice.test.jsx`); T10820's diff (`SettingsRail.jsx`, `FocusSettingsPanel.jsx`, `OverlayModeView.jsx`, `FocusModeView.jsx` and their tests) never touches `UploadPreviewNotice.jsx`/`clipDisplayName.js`/`soccerTags.js` (grep-confirmed) — `gh run rerun --failed` on the identical commit went green, no code change. |
| Frontend (e2e, local-only — not run in Branch CI) | `e2e/T4880-mobile-editor-reachable.spec.js::Framing: Export control reachable + clickable (portrait & landscape)` | `Test timeout of 180000ms exceeded` waiting on `getByRole('button', { name: /^Export( \(\d+\/\d+\))?$/ })` | The primary Framing CTA's accessible name was renamed from "Export" to `` `Generate ${MODE_NAMES.FRAMING}` `` = "Generate Framing" (`displayNames.js:359`, tagged N19, "was 'Export Focused Video'" — an earlier, unrelated copy task). The spec's locator regex was never updated to match, so it polls to the full 3-minute timeout instead of failing fast | Confirmed on T10630's Branch (`feature/T10630-action-band-mobile-stack`, commit `3d0693b9`) 2026-09-19, reproduced 3x; T10630's diff is `ActionBand.jsx` + `ActionBand.test.jsx` only (grep-confirmed no touch of `ExportButtonView.jsx`/`displayNames.js`/the spec itself) and the spec file is unchanged on master. Page snapshot at timeout (`error-context.md`) shows the Framing screen fully loaded with the ActionBand rendering correctly (status caption + "Generate Framing" button + cost, no wrapping) — the app works, only the locator is stale. Not a CI blocker (this spec isn't in `branch-ci.yml`), but blocks local reachability verification for anyone touching Focus/Overlay. Needs a one-line locator fix (`/^(Export\|Generate Framing)( \(\d+\/\d+\))?$/` or similar); out of scope for T10630 (single-file layout task) to fix inline. |
Observed 2026-07-04 during the first /dotask wave (bug27p, T4190, T4100, T3980
workers all independently hit subsets of these).

`gamesDataStore.test.js` row observed 2026-08-01 during the T5700 branch's full
frontend unit run (`npm test`, 148 files); confirmed as a parallel-suite timing
flake via an isolated re-run rather than assumed.

Burned down: the `keyframe-integrity.spec.js` row (stale T340 permanent-boundary
expectation, `g1a_frame0` expected `0` received `50`) was re-pinned to the flat-list
model and removed by T6050 (2026-07-27). The spec now guards the current invariants
(restore round-trips exactly / no manufactured entries; origins normalized to
user/trim; dedup is spatial-identity-based not proximity-based; empty list legal; any
keyframe deletable including the first; min-spacing snap; resolveTargetFrame identity
SSOT). It was never `--deselect`ed in `branch-ci.yml` (that list is pytest-only), so no
CI change was needed.

Burned down: the two `e2e/T9270-mobile-drawer.qa.spec.js::Focus` rows (CTA y-shift on
drawer open; a 28px drawer control under the 44px touch floor) were removed by T10820
(2026-09-20), which rewrote the whole mobile settings surface. Row 1 (CTA y-shift):
NOT a product bug, on either the old or new drawer — root-caused (expert agent,
live-drive confirmed) to the spec itself measuring the "closed" CTA baseline before
`row.click()`'s implicit auto-scroll and the "open" CTA after it. Focus's action band
is `sticky bottom-0`, and App's mobile-only `pb-48` (192px) content padding means the
band only pins to the viewport bottom once scrolled within 192px of the page end —
short of that, `sticky` sits at its static in-flow position, `up to 192px` higher. The
spec's two measurements straddled that un-stick boundary; the settings panel (always
`position:absolute`, zero in-flow height, present in both open and closed states) was
never the mechanism, on this branch or before it. Fixed at the spec level: `row.click()`
now runs before either CTA measurement (`row.scrollIntoViewIfNeeded()` settles the
scroll first, matching `T10820-mobile-settings-anchor.qa.spec.js`), plus a new
assertion that opening the panel does not itself scroll the page. Live-drive confirmed
green on `imankh@gmail.com`/`9fa7378c` (2026-09-20) — this is not a "re-verify later"
row. The 192px mobile dead-zone below the CTA at max scroll is real and pre-existing
(predates T9270/T10820, from T8790's original mobile bar), out of scope here; worth a
separate S-tier task to trim `pb-48`. Row 2 (28px control):
root-caused to the default `Toggle` (`components/shared/Button.jsx`) `md` size track
(`h-7 w-14` = 28x56, matching the failure's `{h:28,w:56}` exactly) used un-gated by
Focus's "Include audio" row (`FocusSettingsPanel.jsx`) in the mobile-safe subset;
fixed with `coarse-pointer:min-h-11` on that one `Toggle` instance (grows the
touch-only hit area without resizing the desktop track or any other `Toggle` caller).
