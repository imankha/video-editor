---
domain: backend-services
updated: 2026-09-01 (T8190 + T5087: the JIT seam self-deadlocked the API process on any migration reaching `get_db_connection` from its own `up()` (same-thread lock re-entry, no timeout) -- fixed with a same-thread pass-through + `SEAM_LOCK_TIMEOUT_S` acquire-with-timeout raising `MigrationBlocked`, and v017/v047 fixed at the source (Postgres-only helpers, never re-enter the seam). With JIT now fully proven, **T5087 deleted the bulk sweep entirely** (`run_all_migrations`, `_migrate_user`, `_migrate_user_db`, `_migrate_profile_db`, and the old `POST /api/admin/migrate`) -- `user_db`/`profile_db` migrate ONLY via the JIT seam now, no backstop. `POST /api/admin/migrate` was renamed `POST /api/admin/migrate-postgres` (`migrate_postgres()`), Postgres-only. See §"Migration system (3 tracks)" below for the updated invariant.)
updated: 2026-08-31 (T5083: **migrations now ALSO run just-in-time at the per-user DB-load seam** (`ensure_database`/`ensure_user_database`), not only via the admin-triggered `POST /api/admin/migrate` bulk sweep — the sweep is unchanged and still the backstop for non-login-writer paths (share materialization, background workers) until T5085 covers them and T5087 deletes the bulk path. Full mechanism, locking, CAS-retry, and fail-loud-503 details: persistence-sync.md §T5083. See §"Migration system (3 tracks)" below for the updated invariant.)
updated: 2026-08-29 (T8020: **admin-dashboard fetch consolidation + `list_users` joins the sync-def set.** (1) `list_users` (`GET /api/admin/users`, admin.py) was `async def` with blocking psycopg2 inline — the SAME event-loop-blocking bug T8000/T8010 fixed on the analytics handlers, missed by both sweeps despite being the heaviest-hit admin endpoint (every admin page load, multiple psycopg2 queries + `credit_ledger.stats_for_admin`). Converted to plain `def` (threadpooled off the loop). The sync-def regression guard is now `test_all_nine_admin_dashboard_handlers_are_sync_def` (was `_eight_`), covering the 8 analytics handlers + `list_users` + the new `get_admin_dashboard`. `list_users` binds `get_pg` at MODULE import time (top-of-file import, used at `with get_pg()`), so a concurrency probe must patch `app.routers.admin.get_pg` (not just `app.services.pg.get_pg`). **Landmine for direct-call tests:** flipping this to `def` broke `tests/test_t5770_usage_daily.py` and `test_t4970_admin_segmentless_enumeration.py`, which called `asyncio.run(admin.list_users(...))` — now a sync call (`admin.list_users(...)`); any future direct caller of an admin handler must check its def/async shape first. (2) New `GET /api/admin/dashboard` (`get_admin_dashboard`, sync `def`) composes the 5 individual admin reads (`list_users` + `analytics_pulse/channels/cohorts/platforms`) into ONE response `{users,pulse,channels,cohorts,platforms}`, mirroring `/api/bootstrap`: reuses the handler FUNCTIONS (no duplicate SQL), sync all the way down (do NOT make it `async def` calling these inline = re-block the loop), NO per-section try/except (any callee raises -> full 500, bootstrap precedent), individual routes KEPT for their other callers (pagination, campaign click-through). **Query-sentinel trap (critical):** these callees are FastAPI handlers whose defaults are `Query(...)` FieldInfo sentinels, resolved to real values only through DI. Calling them as plain functions and OMITTING an arg leaves the param bound to the truthy `Query(...)` object, NOT `None` — silently behaving as if a filter were supplied. So `get_admin_dashboard` passes EVERY param explicitly. Guard `TestDashboardEndpoint` asserts each section == the individual endpoint's unfiltered default (byte-equality = proof no sentinel leaked). Frontend: `adminStore.fetchDashboard` fans the combined response into the same 5 state fields the individual actions populate; `AdminScreen` mount effect calls it once instead of 5 fetches (`AdminScreen.test.jsx` asserts exactly ONE mount request). Live-HAR request-count re-capture DEFERRED (needs staging/prod). Guards: `tests/test_t8000_admin_analytics_concurrency.py`, `tests/test_analytics_dashboards.py::TestDashboardEndpoint`, `adminStore.dashboard.test.js`, `AdminScreen.test.jsx`.)
updated: 2026-08-28 (T7940: the three owner-facing poster routes (`get_game_poster` games.py, `get_draft_poster` projects.py, `get_reel_poster` downloads.py) now take a `profile_id: str | None = None` QUERY param used purely as a per-owner CACHE-CORRECTNESS TOKEN, NOT authorization. Root cause of the cross-account leak: `games.id`/`projects.id`/`final_videos.id` are per-profile SQLite AUTOINCREMENTs, so `/api/games/1/poster.jpg` is byte-identical across every account with an id=1 row -> any URL-keyed cache (CDN/proxy/browser) can cross-serve one account's poster bytes to another. Fix: frontend appends `?profile_id=<currentProfileId>` (GameTile/DraftTile/DownloadsPanel) so the URLs differ per owner; backend refuses a request whose URL `profile_id` does NOT match the session's `get_current_profile_id()` with 403 BEFORE any DB read or R2 call (defense-in-depth; absent param = no check possible, still fine because the URL already disambiguates). Real scoping is UNCHANGED (X-Profile-ID header -> contextvar -> profile-scoped DB read). Do NOT use `Query(None)` as the default -- a plain `= None` keeps FastAPI treating it as a query param while direct-call unit tests (the poster-test convention) receive real `None` instead of the `Query` sentinel. `Cache-Control` stays `private, max-age=86400` (unchanged); no `Vary` added (a cache that ignores Vary wouldn't honor it anyway; the per-owner URL is the actual fix). DEFERRED follow-up (needs Cloudflare dashboard, not this repo): confirm `api.<domain>` isn't caching authenticated `/api/**` .jpg responses. Guard: `tests/test_t7940_poster_cross_account_leak.py`, `GameTile.posterUrl.test.jsx`, `DraftTile.test.jsx`.)
updated: 2026-08-28 (T7860: read-only per-user clip/reel lifecycle-phase inventory. New admin endpoint `GET /api/admin/analytics/user/{user_id}/clip-phases` -> two FURTHEST-PHASE-EXCLUSIVE tiers (clips: created/focus_started/focused over `raw_clips`; reels: completed/published over `final_videos`, so each tier's buckets sum to its total AND a multi-clip reel counts as 1) + orthogonal reel FLAGS (intro_explicit/intro_inherited per downloads.py:240 `intro_card_id` semantics `0`=opted-out/`NULL`=inherit/`>0`=explicit; downloaded via `user.sqlite` `user_action_log` video_downloaded `video_id` set; shared via PG `share_videos` join scoped by `sharer_profile_id`; watched via `final_videos.watched_at`). ALL read-time derivation, NO persisted state, NO writes: opens each `profile.sqlite` `mode=ro` via `materialization.open_profile_db_readonly` (the stuck-uploads RO pattern) and runs the WHOLE inventory in ONE `asyncio.to_thread` (sqlite3 conns are thread-affine, so open/read/close must share a thread). Canonical clip-phase predicates (exported=>focused, crop/segments/timing=>focus_started) live in `services/clip_phases.py` (single source of truth; projects.py's clip_stats query carries a cross-ref comment). Also RESERVED `clip_uploaded` in `FLOW_EVENTS`+`FUNNEL_STEPS` (label "Clip Uploaded", `daily_col=None`, NOT emitted yet) for a future direct-upload origin dimension — the `daily_counters.clips_uploaded` PG column is DEFERRED (no dead column) until direct-upload ships. Downloaded flag has a documented cross-profile id-collision caveat (`final_videos.id` is per-profile AUTOINCREMENT). Frontend: `UserDetailPanel` ClipPhaseBreakdown band; `adminStore.fetchUserDetail` adds a best-effort 3rd fetch (panel renders without it on failure). Guards: `tests/test_clip_phases.py`, `UserDetailPanel.test.jsx`.)
updated: 2026-08-28 (T7950: **CreateMultipartUpload is NON-IDEMPOTENT and must NEVER be retried** — the double-UploadId leak. `r2_create_multipart_upload` (storage.py) previously wrapped boto3 `create_multipart_upload` in `retry_r2_call(**TIER_3)`; on a large/slow upload the first attempt EXECUTED at R2 but its ack beat the 30s `read_timeout` → `ReadTimeoutError` (transient) → the retry minted a SECOND live multipart, and the handler stored only the second id (stored-id ≠ open-id leak, confirmed 2/2 prod accounts by T7880). FIX (B1): call create ONCE; on exception classify with retry.py's EXISTING `is_transient_error` (do NOT invent a new split) — transient/ack-may-be-lost (ReadTimeout/ConnectTimeout/EndpointConnection/5xx/BotoCore) → `_adopt_live_multipart_after_ack_loss(key)` LISTs open multiparts and ADOPTS the newest by `Initiated`, aborting extras (returns None only if nothing materialized → caller raises a clean 500 and the client re-prepares); non-transient/definitive-rejection (ClientError 403/404, AccessDenied, 400 validation) → return None immediately WITHOUT listing (nothing could have been created). Classification table is inline in storage.py by `r2_create_multipart_upload`. FIX (B2): `prepare_upload` (games_upload.py) now creates FIRST then `r2_abort_orphan_multipart_uploads(r2_key, keep_upload_id=upload_id)` — the abort spares the id it is about to store (old order was abort-all-then-create with no keep, letting a concurrent prepare's unscoped abort strand the stored id). **INVARIANT: prepare-upload must never leave a live multipart whose UploadId ≠ the one written to `pending_uploads.r2_upload_id`.** ACCEPTED RESIDUAL (deferred, Option A only-on-recurrence): B2 spares only the CURRENT prepare's own id and the per-user write lock is per-process — a genuinely-concurrent CROSS-USER/CROSS-MACHINE prepare on the SAME global `games/{hash}.mp4` key can still abort another prepare's in-use multipart; a correct cross-machine fix needs a PG-advisory/idempotency lock (an in-process lock is per-machine on Fly, so useless here). Design: docs/plans/tasks/T7950-design.md. Guard: tests/test_t7950_double_uploadid_leak.py.)
updated: 2026-08-28 (T7890: pre-upload funnel beacons — two frontend gestures (`add_game_opened`, `upload_file_selected`) now bridge through `record_milestone` (impersonation-guarded) into the existing `user_actions` aggregate. Added to `analytics.FLOW_EVENTS` (labels "Add Game Opened"/"File Selected", `daily_col=None` — read side is user_actions per-user first_at+count, so NO daily_counters column and NO migration) and to `FUNNEL_STEPS` between `session_started` and `game_created`; FunnelChart.jsx gets the two matching stages. Bridged like every other frontend flow event: `questStore.recordAchievement(key)` -> `POST /api/quests/achievements/{key}` -> `ACHIEVEMENT_TO_MILESTONE` (quests.py). Both keys are in `KNOWN_ACHIEVEMENT_KEYS` + `ACHIEVEMENT_TO_MILESTONE` but deliberately NOT in `_STEP_ACHIEVEMENT_KEYS` (analytics-only, not quest steps — same shape as `viewed_gallery_video`). Fired from `ProjectManager.handleAddGameClick` (inside the authed `requireAuth` callback, AFTER opening the picker so the beacon can never delay it) and from `GameDetailsModal`'s four file-select gestures (`handleFileSelect`/`handleHalfFileSelect` input change + `handleDrop`/`handleHalfDrop` dropzone) via a shared `recordFileSelected` helper — NOT from `uploadManager.uploadGame` (upload START is post-submit, which collapses the details-form abandonment window this beacon exists to measure; T7890 review finding). The gesture is "a file is actually chosen," which precedes the details form + the prepare POST. **Interpretation contract:** `add_game_opened` WITHOUT `upload_file_selected` = picker/entry failure or bail; `upload_file_selected` WITHOUT `game_created` = pre-prepare death (JS error, validation, navigation). picker-abandoned is NOT instrumented (no reliable browser cancel event) — derive it in reads as `add_game_opened - upload_file_selected`. Guard: tests/test_t7890_pre_upload_funnel_beacons.py, ProjectManager.addGameBeacon.test.jsx, uploadManager.test.js. See Invariants rule 10.)
updated: 2026-08-27 (T6260: boot-set read endpoints now emit a strong content-hash ETag + answer 304. The single Cache-Control chokepoint is `RequestContextMiddleware._sync_aware_flow` (db_sync.py) — it default-stamps `private, no-cache, stale-while-revalidate=5` on ALL GET JSON that don't set their own. T6260 added `ETAG_304_READ_PATHS` (frozenset, EXACT-match, 6 boot-set paths: /api/profiles|projects|games|settings|downloads|downloads/count) gating `_apply_read_etag(request, response)`: buffers the rendered JSON body, SHA-256 -> strong ETag, bodiless 304 on `If-None-Match` match or `*`, else re-emits the 200 with the ETag. These paths get `private, no-cache` (validator, NOT a stale max-age/SWR window — mutable per-user data; a stale window would revert a user's own just-saved edit). Lives ENTIRELY in middleware — no handler signature touched, so the T6200/T6240 no-blocking-on-the-loop invariant holds by construction (the SHA runs on already-in-memory bytes). ONLY runs when R2_ENABLED (reads route through `_sync_aware_flow`; the skip-sync branch at db_sync.py ~864 does NOT stamp Cache-Control/ETag). Poster ETag (T5682, games.py/projects.py) is R2-object-ETag passthrough — structurally different, NOT reusable for JSON. To widen the allowlist (epic follow-up) just add the exact path. No frontend change: apiFetch uses browser-default cache mode. Guard: tests/test_t6260_read_etag_304.py.)
updated: 2026-08-27 (T7515: mid-funnel frustration instrumentation tiers 3+4 — two fire-and-forget telemetry beacons (`POST /api/telemetry/impression`, `POST /api/telemetry/session-breadcrumbs`) extending T7510's aggregates-only taxonomy with NO migration. Tier 3 counts blocking-dialog/error-toast impressions into `user_actions` (stable caller-pinned `impressionKey`, never the interpolated title — PII/cardinality); tier 4 writes session-exit breadcrumbs to per-user `user_action_log` only (PG stays aggregate-only). Both copy `record_milestone`'s impersonation guard. Frontend transport `utils/uiTelemetry.js`. Review-hardened invariants: stable impressionKey, reset-after-flush (no duplicate breadcrumb rows on a resumed tab), anon-beacon log+drop. See Invariants rule 10 addendum.)
updated: 2026-08-27 (T7670: upload-complete return-trigger email. Emitted from `activate_game` (games.py) — the single authoritative pending->ready transition — NOT from `finalize_upload`'s `game_upload_succeeded` (that is the R2-bytes-durable point, BEFORE the game is `ready` and it only has `blake3_hash`, no `game_id`/name for the deep link). Exactly-once per game = the existing already-ready early-return + a conditional `UPDATE games SET status='ready' WHERE id=? AND status!='ready'` gated on `cursor.rowcount==1` (`flipped_to_ready`). Send is best-effort/non-blocking via `_maybe_send_game_ready_email` → `poster_warmer.fire_and_forget(send_game_ready_email(...))`: all eligibility checks (impersonation via `get_current_impersonator_id`, opt-out, address) run SYNC in request context, only the network send is offloaded, whole helper try/except-wrapped so a failed send never fails an activation. No email on failed/pending uploads — a failed activation never flips status, so the trigger is unreached. Opt-out is the FIRST notification-email preference surface: user-level boolean in the existing `user_settings` KV (`get_/set_notification_email_optout`, key `notification_email_optout`), absent=opted-in, no schema/migration, no UI yet. `send_game_ready_email` (email.py) reuses `_build_share_email` CTA shell + `FROM_ADDRESS` noreply@, dev-mode (no RESEND_API_KEY) logs+returns True. Deep link `?game=<id>&profile=<profileId>` (`_get_game_editor_url`) carries the profile because per-profile game ids are NOT globally unique; App.jsx stashes the params to sessionStorage early (survive auth reload) then post-bootstrap `switchProfile` if needed + opens the game in annotate mode. Guard tests: `tests/test_t7670_game_ready_email.py`.)
updated: 2026-08-27 (T6770: `game_ref_counts.ref_count` — the hand-maintained Postgres counter gating IRREVERSIBLE R2 game-video deletion — is RETIRED in favor of a derived ref-set. `game_storage_refs` (dead since the v002/T2930 refactor: v002 aggregated it INTO game_ref_counts and nothing wrote it after) is revived as the LIVE table: one row per `(user_id, profile_id, blake3_hash)`, so `ref_count = COUNT(*)` can never independently drift from the rows that back it. `insert_game_storage_ref`/`delete_ref` now upsert/delete a keyed row instead of incrementing/decrementing an integer; `has_remaining_refs` is `EXISTS(...)`; `get_next_expiry` is `MIN(storage_expires_at)`; `heal_ref_count` is DELETED (a derived count has nothing to heal). Root cause of both prior findings: v002's `INSERT INTO game_ref_counts SELECT COUNT(*), MAX(expiry) FROM game_storage_refs` is why `game_storage_refs` went dead (2026-08-24 prod finding: 8 rows, newest 2026-05-15) — nothing wrote it after that aggregation; the 2026-08-11 missing-`game_ref_counts`-row finding was a structural bug in the OLD `insert_game_storage_ref`, which gated the PG write on SQLite row novelty (`is_new`) — if the PG row was lost independently (crash, purge) while the SQLite row survived, the `else` branch's `UPDATE ... WHERE blake3_hash` matched zero rows and the counter stayed missing forever, with no create-if-absent path. Postgres migration v025 clears the dead pre-T2930 sediment; profile_db migration v047 backfills `game_storage_refs` authoritatively from every profile's real `game_storage` rows (this also serves as the one-time drift reconciliation the 2026-07-23 retrospective flagged as a smaller open item). `game_ref_counts` itself is left declared-but-dead (not dropped) for rollback safety — a follow-up migration drops it once prod is confirmed. **Account-deletion FK must-fix:** `game_storage_refs.user_id REFERENCES users(user_id)` has no cascade; now that the table is LIVE, `_purge_user_data` (auth.py) deletes the user's `game_storage_refs` rows before the caller's `DELETE FROM users`, or that delete 500s for any user who still owns a game ref. Design: `docs/plans/tasks/T6770-design.md`. The Phase-2 sweep's authoritative recount (`_count_refs_all_profiles`, reading SQLite `game_storage` across all profiles) is UNCHANGED — it was already the real safety net; only the drift-prone read-side counter it double-checked against is gone.)
updated: 2026-08-26 (T6780: CLOSED the two write-side migration-window asymmetries T6550 filed — see "Write-side sibling guard (T6550...)" § "Both asymmetries CLOSED (T6780)". Both confirmed REACHABLE (neither write path self-migrates to head). (A) `working_videos.detections_data` v027 write in `upsert_working_video` → COLUMN-OMIT guard (optional cache, render path degrades, mirrors T6030 slowmo), NOT 503. (B) `games.shared_by` v026 + `source_profile_id`/`source_game_id` v030 in the shared `_insert_game_with_videos` copy primitive → REFUSE via new `materialization.RecipientProfileBelowHead` (columns DEFINE the row's meaning, omission would corrupt); share paths DEFER to `create_pending_share`, move-reel returns 503. No new migration, no POST_V023_COLUMNS change — write-side test drivers only.)
updated: 2026-08-26 (T7510: attempt/outcome/failure-reason taxonomy layered onto the EXISTING PG `user_actions`/`daily_counters` aggregates — no `analytics.sqlite`, T7400 not landed yet. See "Attempt/outcome/failure taxonomy (T7510)" below and Invariants rule 10. Postgres migration numbered v024 (`daily_counters` additive columns) — NOT v023, which the sibling T7550 branch already landed on master for an unrelated `pending_teammate_shares` rename; this branch has a v023 gap until it merges with master. See the table below.)
updated: 2026-08-25 (T7500: **a write handler must NOT report success (or fire a side effect) on a zero-row UPDATE/DELETE-by-id** — check `cursor.rowcount == 0` and 404 + no side effect + WARNING log. `finish_annotation` (games.py) did both (returned `{success:true}` AND fired `record_milestone("annotation_completed")`) against a game the failed-upload cascade had already deleted, manufacturing a false activity trail. Full `record_milestone`/`record_achievement` sweep done — see Invariants rule 8; ONLY `finish_annotation` was unguarded, every other milestone site is INSERT / SELECT-404-guarded / atomic-PG-grant-gated / background-job. `save_playhead` sibling deliberately left unchanged (UPDATE-by-id but NO side effect, sendBeacon response unconsumed). Frontend `gamesDataStore.finishAnnotation` swallows the new 404 quietly.)
updated: 2026-08-25 (T7490: `GameStatus` (constants.py) gained `UPLOAD_FAILED='upload_failed'` — free-text `games.status`, NO migration (column already unconstrained). `list_pending_uploads` (games_upload.py) now HONESTLY reaps a stale resume record: aborts the R2 multipart (`r2_abort_multipart_upload`, best-effort/never-blocks) + `UPDATE games SET status='upload_failed' WHERE blake3_hash=? AND status='pending'` + deletes the pending row; idempotent; a WRITE in a GET handler kept deliberately (not restructured). `create_game` pending-reuse now matches `status IN ('pending','upload_failed')` and resets to pending so Retry resumes into the same game id. See annotate.md § "Orphaned pending upload is reaped HONESTLY (T7490)".)
updated: 2026-08-21 (T4340: profile_db migration head is now v045 — canonicalizes `working_clips.segments_data.boundaries` to full-list, DATA-ONLY, no column added; bumped `HEAD_VERSION_AUDITED` 44->45 in test_t6030_migration_window_structural_guard.py, no `POST_V023_COLUMNS` change needed. See the migration table below and annotate.md § segments_data write-time-canonical.)
updated: 2026-08-14 (T6240: `user_session_init` offloaded off the event loop via `run_in_context` (db_sync.py:773) — it is the BOOT hot path, not "rare": `/api/profiles` (session's first request) cannot send `X-Profile-ID`, so it routes into the blocking init and pre-fix serialized the whole boot burst (~22s HAR). Corrected the "rare on the hot path" claim in "Request concurrency model". Guard: tests/test_t6240_session_init_concurrency.py.)
updated: 2026-08-01 (T5770: admin Avg/wk + Last 7d usage columns — new postgres v022 `user_usage_daily` table, single-write-path `analytics.add_usage_seconds` helper, derived weekly average. See "Admin weekly-usage columns: Avg/wk + Last 7d (T5770)" below. Postgres migration head moved v018→v022 in the table below; v019/v022 documented, v020/v021 reserved by unmerged sibling branches.)
updated: 2026-07-31 (T6220: lockstep deploys so the build number stays accurate — see "Deploy + build-number lockstep (T6220)" below. Union `paths:` filter on both staging workflows (not dropped), asymmetric `deploy_production.sh` gate (`--frontend-only` stays allowed, `--backend-only` now refuses without `--accept-build-drift`), and `scripts/verify-build-lockstep.sh` asserts bundle build == backend build after every deploy. Follow-up from T6210.)
updated: 2026-07-30 (T6200: documented the request concurrency model — ONE uvicorn process/loop, blocking I/O must never run on the loop (it serializes concurrent requests AND defers all responses to burst end = the HAR fingerprint), the two threadpools by handler shape (async def -> to_thread/run_in_context; plain def -> anyio pool), the bounded 32-thread default executor set in lifespan(), what /api/health costs, and where blocking work is offloaded. See "Request concurrency model (T6200)". Fix offloaded validate_session + hot list reads.)
updated: 2026-07-27 (T6030: closed the v025 residual — final_videos.slowmo_section_start/end now column_exists-guarded on BOTH the publish SELECT (downloads.py) and the render INSERT (overlay.py `_finalize_overlay_export`, which had blocked ALL exports in the deploy->migrate window). Added a structural regression test that drives every hot-path read against a below-head DB WITH ROWS; `test_registry_head_is_audited` turns the next added migration RED so the class can't silently reopen. See "Migration-window column guard audit" § v025 + Structural guard.)
updated: 2026-07-27 (T5970: migration-window column-guard audit — see "Migration-window column guard audit (T5970)". Result: ONE unguarded hot read fixed (games.shared_by v026 in quests._check_all_steps, hit on bootstrap); everything else either ≤v18 (window closed) or off the app-load path. Added read-only GET /api/admin/migration-status.)
updated: 2026-07-26 (T5840: credits moved from per-user SQLite to Postgres — see "Credits (Postgres, T5840)" below. Shipped as two deploy-safe commits since master auto-deploys staging and migrations don't auto-run: Slice A = additive v019 DDL + `credit_ledger` service + `credit_backfill` tool + admin backfill/gate endpoints, zero behavior change, safe pre-migration; Slice B = cutover flip, all call sites + purge/reset scripts point at Postgres, old `user_db` credit functions + SQLite credit columns removed.)
updated: 2026-07-22 (T5660: engaged-time model — shared SESSION_IDLE_CAP_SECONDS + session_engaged_seconds helper, /api/auth/heartbeat + /session-close endpoints, tab-close beacon, hours-only Usage display. T5600: working_videos.detections_data column + v027 migration; T4860: admin bulk user actions — POST /admin/users/bulk/grant-credits + /bulk/email, plus the `hello@reelballers.com` sender / `send_admin_update_email` path in email.py. T5060: app startup/shutdown now via fastapi `lifespan=` (not `@app.on_event`); pytest.ini added with a scoped filterwarnings guard + `local_disk_timing` marker. T5020: fastapi 0.110.1 / starlette 0.37.2 -- TestClient no longer passes removed `app=` kwarg, test and prod now both use httpx 0.28.1)
---
# Backend Services & Structure — Domain Knowledge

## Scope
FastAPI app layout: routers, services, the three databases, the 3-track migration system, admin endpoints, and test infrastructure. R2 sync mechanics and persistence rules live in persistence-sync.md.

## Entry points
- `src/backend/app/main.py` — app creation, CORS, `RequestContextMiddleware`, `FlyReplayMiddleware`, router registration. **Startup/shutdown run via a fastapi `lifespan=` context manager (T5060)** — `lifespan()` is defined just above `app = FastAPI(..., lifespan=lifespan)` and calls `_startup_event()` (before yield: Postgres pool + schema init, sweep + cleanup loops, SIGTERM handler when not IS_DEV) then `_shutdown_event()` (after yield: stop sweep/cleanup loops, `close_pg_pool()`). The old `@app.on_event("startup"/"shutdown")` decorators are gone (deprecated). Regression test: `tests/test_lifespan.py` drives the lifespan via `with TestClient(app):` and asserts the shutdown branch closes the PG pool.
- Import check after ANY backend edit (required, src/backend/CLAUDE.md): `cd src/backend && .venv/Scripts/python.exe -c "from app.main import app"`.

Router inventory (`app/routers/`; prefix shown; some get an extra `/api` prefix at include time — main.py:154-158):
- `health.py` (no prefix) — health/status. `auth.py` (`/api/auth`) — Google/OTP login, sessions, test-login. `bootstrap.py` (`/api/bootstrap`) — initial app payload.
- `clips.py` (`/api/clips`) — raw/working clip CRUD + framing gesture actions + streaming proxy. `projects.py` (`/api/projects`) — reel projects, rename, rescale, restore. `games.py` (`/api/games`) — game library, activation, sharing, expiry, streaming. `games_upload.py` (`/api/games`) — blake3-keyed upload/dedupe/finalize.
- `export/__init__.py` (`/api/export`) — mounts `framing.py` (crop/AI framing), `overlay.py` (highlight render + overlay gesture actions), `multi_clip.py` (multi-clip reels, 3 routes, ~2,500 lines), `before_after.py`. `exports.py` (`/api/exports`) — export job records, recovery, presigned results. `detection.py` (`/api/detect`) — YOLO player detection.
- `downloads.py` (`/api/downloads`) — gallery/final-video downloads + proxy. `collections.py` (`/api/collections`) — season highlights. **T5880**: `/summary` also returns `game_groups: list[GameGroup]` — DERIVED tournament/month groupings of `games` (`_build_game_groups`), each `{axis:'tournament'|'month', key, label, game_ids, reel_count, unwatched_count}`. Two independent axes over the SAME games (client shows one via a toggle); grouping is server-computed; absent metadata -> NO group (a game with no `tournament_name`/no parseable `game_date` is simply in no such group — never a fabricated bucket). `game_ids` ⊆ `games` (frontend nests the existing per-game buckets under `GameAxisGroup`, no duplicated aggregates). Ordering: tournament groups most-recent-first via dict insertion over the pre-sorted `games`; month groups by `YYYY-MM` key DESC. Cross-profile attribution deferred to the T5800-T5820 epic. `rank.py` (`/api/rank`) — Glicko pairwise ranking. `shares.py` (two routers: `/api/gallery`, `/api/shared`) — share links, public playback. **T4840**: `GET /api/shared/{token}` schedules `record_milestone("share_viewed")` via `BackgroundTasks` (was synchronous, ~400ms on the response path); new `POST /api/shared/{token}/viewed` beacon (no auth, 204, unknown->404, revoked->204 no-record) lets the edge share page record views on edge-cache hits. Response shape unchanged.
- `profiles.py` (`/api/profiles`), `settings.py` (`/api/settings`), `users.py` (`/api/me`), `credits.py` (`/api/credits`), `payments.py` (`/api/payments`, Stripe), `quests.py` (`/api/quests`), `privacy.py` (`/api/privacy`), `storage.py` (`/storage`, presigned URLs + warmup).
- `admin.py` (`/api/admin`) — admin dashboard, migrations, impersonation. **T4860 bulk user actions:** `POST /users/bulk/grant-credits` (`{user_ids[], amount}` -> `{results[], granted, failed}`, loops the single-user `grant_credits` sequentially — per-user SQLite write lock, never `asyncio.gather`) and `POST /users/bulk/email` (`{user_ids[], subject, body, test}` -> `{results[], sent, failed}`). Both `_require_admin()` first line, 100-id cap -> 400, partial failure is first-class (per-user `{ok, error}`, unknown/no-email ids recorded not fatal). **Route-order landmine:** the two `/users/bulk/*` routes are registered ABOVE `/users/{user_id}/grant-credits` — FastAPI matches in definition order, so `bulk` would otherwise be captured as `user_id="bulk"`. Email resolves each recipient from the Postgres users table (not auth.sqlite), sends ONE email per recipient (no To/CC leak), converts plain-text body -> HTML server-side (escape-first, never raw HTML), and awaits real results (no `background_tasks`). `test=true` ignores `user_ids` and sends one email to the caller's own address (`get_current_user_id` -> `get_user_by_id`). `_debug.py` (`/api/_debug`, gated by DEBUG_ENDPOINTS_ENABLED). `test_seams.py` (`/api/test`, mounted only in non-prod, main.py:173-177).

Key services (`app/services/`): `pg.py` (Postgres pool + `_SCHEMA_DDL`), `user_db.py` (user.sqlite + `_USER_DB_SCHEMA`), `auth_db.py` (sessions/users on Postgres), `sharing_db.py`, `export_worker.py` + `export_helpers.py` (job lifecycle), `modal_client.py`/`modal_queue.py`/`processor_modal.py` (GPU), `processor_local.py`/`local_processors.py`/`ffmpeg_service.py` (local FFmpeg), `auto_export.py` + `sweep_scheduler.py` (expiry sweep), `storage_credits.py`, `materialization.py` (share materialization), `project_archive.py` (msgpack archives), `glicko.py`, `email.py` (Resend HTTP API; two senders: `FROM_ADDRESS`=`noreply@` for all transactional mail, and **T4860** `ADMIN_FROM_ADDRESS`=`hello@reelballers.com` for admin update emails via `send_admin_update_email(to, subject, body_html)` + `_build_update_email` branded shell + `body_text_to_html` (escape-first plain-text->HTML). **Dev-mode contract:** the share funcs + `send_admin_update_email` LOG and return truthy when `RESEND_API_KEY` is unset (never raise) — only `send_otp_email` raises. Resend verifies at the domain level so any `@reelballers.com` sender works once the domain is verified).

## Data flow
Three databases, different access patterns:
1. **Fly Postgres** (auth/sharing/sessions/analytics/game storage refs): `from app.services.pg import get_pg`; context manager auto-commits/rolls back. `%s` params, RealDictCursor (rows are dicts). Schema: `_SCHEMA_DDL` in pg.py:21 (`CREATE TABLE IF NOT EXISTS`, run at startup — fresh DBs only). **T4960**: `get_pg()` pre-pings each checkout with `SELECT 1` (rollback after, no txn leak) and discards+refetches stale conns, bounded to `maxconn+1` attempts then re-raise, WARNING `[PG] discarded stale connection` per discard — fixes first-request-after-idle 500s (Fly closes idle sockets while `conn.closed` stays 0; keepalives alone didn't catch it). Gated by `_IDLE_PING_THRESHOLD_S=5s` (conns reused within the window skip the ping → ~0 hot-path overhead; only idle-past-window conns are pinged). A fixed 2-attempt bound is insufficient — after idle ALL pooled conns are dead, so the bound must exceed the free-list size.
2. **Per-user-per-profile SQLite** `profile.sqlite` (clips/projects/games/final videos): `from app.database import get_db_connection` → `TrackedConnection` (write tracking for R2 sync). `?` params, `sqlite3.Row` (bracket access only — `.get()` does not exist). Schema: `ensure_database()` in database.py:479.
3. **Per-user SQLite** `user.sqlite` (profiles list/quests/activity — **credits moved OUT to Postgres, T5840**, see below): `app/services/user_db.py`, schema `_USER_DB_SCHEMA` (user_db.py:39).

Binary blobs are msgpack via `app/utils/encoding.py` (`encode_data`/`decode_data`). Media files live in R2 under `{APP_ENV}/users/{user_id}/...`; game sources under `games/{blake3}.mp4` (no env prefix).

## Request concurrency model (T6200 — measured, not assumed)
**ONE uvicorn process, ONE asyncio event loop.** `Dockerfile:23` runs `uvicorn app.main:app` with **no `--workers`** (the Fly VM is 1 shared vCPU / 1024MB). Single-process is LOAD-BEARING, not laziness: `_USER_WRITE_LOCKS` (db_sync.py:215), the R2 sync version caches (`_user_db_versions`/`_user_sqlite_versions`) and the in-flight/upload locks are all in-process, so they are machine-global ONLY while there is one process. Adding `--workers` makes them per-process → two workers could write the same user's `profile.sqlite` concurrently = the CAS/last-write-wins data-loss hazard those locks exist to prevent (persistence-sync.md §T4310/T4315). Do NOT add workers without first moving the write lock + version cache out of process (PG advisory lock / Redis) AND resizing the VM.

**The cardinal rule: never run blocking I/O (psycopg2 / sqlite3 / boto3-R2) directly on the event loop.** While the loop is blocked it can neither advance another request NOR flush any already-finished response, so a concurrent burst SERIALIZES and all responses drain together — identical durations + simultaneous completion (the T6200 HAR fingerprint: 4 GETs incl. a trivial `/api/health` all ~1460ms, finishing within 2ms). Measured warm on staging pre-fix: authed `/api/health` 358ms(N=1)→1271ms(N=8); anon (allowlisted, no work) stayed flat ~118ms. `/api/health` "paying" the full time did NOT mean it was expensive — it was cheap but stuck behind the burst, and every response flushed together.

**Two threadpools, by handler shape (both get work OFF the loop):**
- **`async def` handlers** run ON the loop. Any blocking call inside one must be offloaded: `await asyncio.to_thread(...)` (bare, only if the fn reads NO request contextvar) or `await run_in_context(fn, *args)` (`app/utils/offload.py` — copies contextvars via `copy_context()`+`ctx.run`, the bootstrap.py precedent, so `get_current_*()` resolve in the thread; the ONE greppable offload primitive). A bare `to_thread` on a fn that calls `get_current_user_id()` raises `RuntimeError: No user context set` inside the thread — the recurring landmine.
- **plain `def` handlers** are run by FastAPI/Starlette in **anyio's worker-thread pool** (default 40 threads) automatically, off the loop, WITH contextvars propagated (starlette 0.37.2 `run_in_threadpool` → `anyio.to_thread.run_sync`). So a handler whose whole body is synchronous blocking sqlite should just be `def`, not `async def` (Option C). Handlers that must `await` (e.g. a presign gather, a fire-and-forget that schedules tasks) stay `async` and offload their blocking prefix via `run_in_context`.
- **The loop's default executor is a bounded `ThreadPoolExecutor(max_workers=32)` set in `main.py lifespan()`** (T6200). asyncio's default is `min(32, cpu+4)` = 5 on 1 vCPU — too few once request-path I/O is offloaded. This 32-pool backs EVERY `to_thread`/`run_in_executor(None,...)` app-wide (bootstrap, `_background_sync`, etc.). psycopg2 and sqlite3 release the GIL during their I/O, so offloaded I/O genuinely overlaps even on 1 vCPU. Do NOT offload CPU-bound work here (it serializes on the GIL anyway). **PG-touching offloads are capped at `maxconn=10` NOT by this executor and NOT by the PG pool itself** — psycopg2's `ThreadedConnectionPool.getconn()` does not block, it RAISES `PoolError` the instant `len(_used) == maxconn`, which `db_sync` turns into a 503 (the T6200 regression the 32-thread bump would have introduced). The real cap is a **`threading.BoundedSemaphore(maxconn)` checkout gate in `pg.py get_pg()`** (T6200): callers WAIT for a free connection instead of racing getconn past the ceiling. Because that gate lives in the single checkout path, it bounds PG demand from BOTH threadpools at once — the 32-thread default executor AND the 40-thread anyio pool — so executor size and pool `maxconn` are decoupled on purpose (32 for I/O concurrency, ≤10 ever inside a PG checkout). Raising `maxconn` is a latency knob (fewer waits), not a correctness fix, and needs a Postgres `max_connections` + VM headroom check first.

**Where blocking work is offloaded (T6200):** `validate_session` (blocking psycopg2, run for EVERY authed request) is `await asyncio.to_thread(validate_session, session_id)` in `db_sync.py:611` — bare-safe because it runs BEFORE `set_current_user_id` and reads no contextvar. The hot list reads are offloaded: `games.list_games` → `_read_games_for_list()` via `run_in_context`; `projects.list_projects` → `_read_projects_list()` via `run_in_context` (its poster-warm fire-and-forget stays on the loop — it schedules async tasks and needs the running loop); `clips.list_project_clips` + `clips.get_clip_playback_url` are plain `def` (no `await` in body). **`user_session_init` (db_sync.py:773, blocking R2+sqlite+PG) is offloaded via `await run_in_context(user_session_init, user_id)` (T6240).** It runs only when `X-Profile-ID` is absent — NOT rare: this is the boot hot path. The FIRST request of a session is `/api/profiles`, whose whole job is to discover which profiles exist, so it structurally CANNOT send `X-Profile-ID` → it routes into `user_session_init`, and pre-T6240 the ~7 requests issued behind it at boot drained together (2026-07-31 HAR: eight boot requests all ~22s, `/api/health` aborting to status 0 throughout). `run_in_context` (not a bare `to_thread`) is required: user context is already set at the call site, and `user_session_init` → `ensure_database()` reads `get_current_*()` in the thread. The profile_id it sets on the copied context does NOT propagate back, so the caller re-applies `set_current_profile_id(init_result["profile_id"])` on the request context. session_init's own pending-share block already runs on a daemon thread, untouched. **Worker-thread landmine (fixed T6240):** session_init's slow path ends with `_schedule_startup_recovery` (orphaned-job recovery + modal-queue drain), which detected "am I on the loop?" via `get_running_loop()`. Offloaded, the worker thread has no loop → the old `except RuntimeError: asyncio.run(...)` fallback would BLOCK the worker (draining the modal queue) and run recovery on an ephemeral loop that cancels sub-tasks at close. Fix: `main.py` lifespan captures the loop via `session_init.set_main_loop()`; the worker-thread branch now `call_soon_threadsafe`→`create_task(..., context=copy_context())` onto the main loop — fire-and-forget, context-preserving. So ANY new offload of a function that schedules loop work from inside must carry the main-loop reference, not rely on `get_running_loop()`. Guards: `tests/test_t6240_session_init_concurrency.py` (overlap property + fire-and-forget recovery, both counterfactual-proven).

**What `/api/health` actually costs:** it is in BOTH `SKIP_SYNC_PATHS` and `SKIP_SESSION_INIT_PATHS`, so an authenticated health request's only real per-request work is `validate_session` (now offloaded); anon health is allowlisted and returns immediately via `call_next`. It is a genuine concurrency discriminator vs a data endpoint — health touches no profile DB. Cold-start (Fly `auto_stop_machines="suspend"`) adds ~600ms on the first hit after idle (staging cold 712ms vs warm 105ms) but prod runs `min_machines_running=1`, so it's a secondary contributor, not the HAR's primary cause.

**Repeatable probes (committed):** `scripts/concurrency_probe.py` (fires N=1,2,4,8, reports per-request duration + finish-spread; loop-probe modes or a real endpoint, dev-header or real cookie) and the non-prod test seam `GET /api/test/loop-probe?mode={block,async,thread}` (`test_seams.py`). `block` mode intentionally blocks the loop and MUST still scale with N (it's the discriminator); `async`/`thread` stay flat. Perf guard: `tests/test_t6200_concurrency.py` asserts the overlap PROPERTY (N concurrent authed requests overlap, not serialize) with a controlled sleeping `validate_session` stub — counterfactual-verified (re-inlining the blocking call fails it).

### Credits (Postgres, T5840)
Credits were per-user SQLite (`user.sqlite`, R2-synced) — a money ledger on eventually-consistent per-machine storage. Now a shared Postgres ledger (`_SCHEMA_DDL` in pg.py: `credits` balance row, `credit_transactions` append-only ledger with `UNIQUE(user_id, idempotency_key)` idempotency (`v019_credits.py`; the old SQLite-era `UNIQUE(user_id, source, reference_id) WHERE reference_id IS NOT NULL` index is gone -- `credit_key()` derives the idempotency_key from source+reference_id itself, so the uniqueness lives on that single column now), `credit_reservations` for in-flight exports incl. `profile_id`, `credit_migration_state` singleton row tracking gate/backfill status + `last_report` JSONB).
- **Service:** `app/services/credit_ledger.py` — `set_balance`/`debit`/`grant`/`reserve_credits`/`confirm_reservation`/`release_reservation`/`recover_orphaned_reservations`. All idempotency-keyed writes use `INSERT ... ON CONFLICT DO NOTHING ... RETURNING` + a following `SELECT ... FOR UPDATE`; `debit`/reservation confirm are atomic conditional `UPDATE ... WHERE balance >= %s`. `reference_id` flows through debit/grant for T970 orphan-recovery correlation (quest rewards pass `reference_id=quest_id`).
- **`credits_ready` gate:** mutations 503 until Postgres balances are confirmed to match the legacy SQLite ledger. Gate consumes a STORED report (`credit_migration_state.last_report`, written by `run_backfill()`) rather than recomputing per-request — unbounded synchronous R2 scans in a gate check risk the Fly proxy timeout. `no_user_db` (purged/guest accounts) is informational only, never blocks; other flags require `acknowledge_flags` or `force` to open.
- **Backfill:** `app/services/credit_backfill.py` `run_backfill(user_ids=None, apply=False, limit=None, offset=0)` — merges each user's local `user.sqlite` ledger against the R2 copy. NULL-`reference_id` rows (admin_grant / new_account_bonus / dev selfgrant — no natural key) are keyed content-only as `legacy:{(source, amount, created_at)}:{ordinal}`, with per-signature multiplicity = the elementwise MAX across the two copies (Counter multiset union) so R2 and local rows coalesce under one key via `ON CONFLICT DO NOTHING`. Content-only is DELIBERATE (never origin- or rowid-tagged): a key that depends on which copies happened to be readable re-keys the same physical row across a machine replacement (the D2 volume wipe) and double-credits every signup bonus and admin grant on the next catch-up run. Re-derive accounts for open PG reservations so re-running backfill can't over-credit a user with an in-flight export. Chunked via `limit`/`offset` for large user counts; full unchunked runs persist their result via `save_report()`/`load_last_report()`.
- **Reservation scoping (T5840 M6):** `recover_orphaned_reservations` is global (crosses machines) — before releasing a candidate it checks `_has_live_export_job(user_id, profile_id, job_id)` against that profile's local `export_jobs` table, defaulting to "conservative" (treat as live, don't release) when `profile_id` is absent or the profile DB isn't locally cached. Never release on ambiguous state — a false release is free money.
- **Purge:** `auth.py` `_purge_user_data` (shared by account delete + test-account reset) deletes `credit_transactions`/`credit_reservations`/`credits` — this MUST stay in the shared helper, not duplicated per caller, or a purged-then-reregistered user_id collides with its old `signup:{user_id}` idempotency key and silently gets no signup bonus. `scripts/reset-test-user.py`/`reset_all_accounts.py`/`copy_user_between_envs.py` clear the same three tables.
- **Cleanup tooling must clear the ledger explicitly (T6090):** the credit tables are keyed by `user_id` in **Postgres**, so they SURVIVE an R2-prefix purge and a `user.sqlite` delete — the mental model "wipe the user's storage = wipe the user" is now wrong. `_purge_user_data` (above) covers every app path (account delete, CCPA, test-account reset), but `scripts/delete_user.py` was missed when T5840 landed and orphaned the ledger; worse, re-creating a user with the SAME `user_id` then inherited the stale balance (observed 2026-07-27 on staging: two prod-copied accounts came back with 395 and 28 leftover credits). Fixed in T6090 (`CREDIT_TABLES` + a `to_regclass` pre-v019 guard so a not-yet-migrated env does not crash + `--sweep-orphans` for rows already orphaned). Any NEW cleanup tool must delete `credit_transactions`/`credit_reservations`/`credits` explicitly AND tolerate an env where they do not exist yet.
- **Deploy sequencing:** schema + tool ship in one commit with zero behavior change (Slice A, safe pre-migration); the cutover (all call sites + admin endpoints reading `applied`) ships separately (Slice B) so an admin can run the `v019` migration + `POST /api/admin/credits/backfill` + open the gate between deploys, never leaving `/api/bootstrap` 500ing on `UndefinedTable`.
- **Fresh-environment footgun:** `credit_migration_state` is empty on a brand-new deploy, so `_is_ready()` is False and the gate treats credits as NOT cut over → **every** credit mutation 503s, including new-signup bonuses and dev selfgrants, until someone runs `GET /api/admin/credits/backfill-report` (which stores a report) then `POST /api/admin/credits/open-gate`. This is correct for the production cutover (fail loud until backfill is verified) but surprising on a fresh staging DB with no legacy data to migrate: the gate still must be explicitly opened before credits work at all.
- **Landmine RESOLVED for credits (T5840):** the "live machine never re-pulls `user.sqlite` from R2, so out-of-band credit edits get clobbered" hazard no longer applies to credits. The old admin-grant path had to download the GRANTEE's `user.sqlite` from R2, read-modify-write the balance, and push it back (a read-modify-write across a network round trip on eventually-consistent storage — the exact hole that lost 400 credits on 2026-07-24). Now `credit_ledger.grant` runs a single atomic `UPDATE credits SET balance = balance + amount` inside Postgres under `UNIQUE(user_id, idempotency_key)`; there is no read of the balance in app code before the write, so a stale snapshot cannot force-push over newer state. This is why the `_refresh_target_user_db`/`_persist_target_user_db` helpers and their `TestRefreshTargetUserDb` coverage were deleted — the invariant they protected is now structurally guaranteed, and the admin grant landing correctly against real Postgres (incl. idempotent retry) is covered by `tests/test_admin_credit_idempotency.py`. **The hazard still applies to everything else in `user.sqlite`** (profiles list, quests, activity) — a live machine reads its cached copy for a machine lifetime and out-of-band edits to those tables can still be clobbered.

## Auth bypasses for automated testing (dev/staging)
- `POST /api/auth/test-login` (auth.py:852) — empty `e2e@test.local` user; new-user flows only. In SKIP_SESSION_INIT_PATHS, so no real data loads. Requires X-Test-Mode; `ENV==production` → 404.
- `POST /api/auth/dev-login` (auth.py:884, T3980) — FAITHFUL impersonation of a REAL user. Body `{email|user_id, profile_id?}` (email also via X-Dev-Login-Email header); user_id wins over email. Resolves via Postgres (`get_user_by_id`/`get_user_by_email`), then runs `user_session_init(user_id, hint_profile_id=profile_id)` (R2 download of user.sqlite + selected/hinted profile.sqlite) BEFORE `_issue_session_cookie`. Payload: `{email,user_id,profile_id,dev_login:true}`. Gating: `APP_ENV=="production"` → 404; non-local-dev (staging) requires X-Test-Mode; local dev {dev,development,local} exempt (back-compat for scripts/dev-verify.sh + realAuth helper). **Init at login is REQUIRED, not redundant**: the sync middleware runs `user_session_init` only when X-Profile-ID header is absent/invalid (db_sync.py:549-559); when the client sends a valid X-Profile-ID (normal case) init is SKIPPED, so the cookie alone never triggers the R2 download → empty local profile (the original 2-blank-games bug). Playwright helper: `loginAsRealUser(pageOrContext, email, profileId?)` in src/frontend/e2e/helpers/realAuth.js (always sends X-Test-Mode). Verify: `bash scripts/dev-verify.sh e2e/T3980-dev-login-real-data.spec.js`.
- Header bypass: middleware accepts `X-User-ID` only when `APP_ENV != production` (db_sync.py:827); sets user context but NOT session_init → no real data. `GET /api/games` returns a `{games:[...]}` envelope (not a bare array). **T8290 (2026-09-01): removed the `or /api/admin/` route carve-out** — that clause let the header authenticate any admin route on production with no cookie/token (`_require_admin()` only checks whether the resolved user_id IS an admin, never whether the caller really IS that user_id). Admin routes are deliberately NOT an exception; do not reintroduce this.
- **E2E isolation landmine (T4780)**: `test-login` sets `rb_session` cookie for the shared `e2e@test.local` user. The middleware checks `rb_session` BEFORE `X-User-ID` (db_sync.py:474 vs :494), so unique `X-User-ID` values are silently overridden by the cookie — ALL tests hit the same shared user. Fix: call `page.context().clearCookies()` BEFORE navigating; omit `test-login` entirely; the app's `initSession` calls `GET /api/auth/me` which checks `X-User-ID` BEFORE `rb_session` (auth.py:383-395), so without a cookie the test user is truly isolated. No Postgres record needed for `X-User-ID` users: `user_session_init` creates a fresh profile on first call (session_init.py:136-154).

## Quest system (routers/quests.py)
`/api/quests/` routes: `GET /definitions` (quest structure), `GET /progress` (per-user step completion), `POST /achievement` (record a user action), `POST /{id}/claim` (claim reward). Step completion is derived in `_check_all_steps()` via 4 batched SQL queries on the user's profile SQLite. The per-quest list built by both `GET /progress` and the achievement POST is assembled by the shared `_assemble_quests(all_steps, completed_ids, claimed_ids)` helper — one source of the `{id, steps, completed, reward_claimed}` shape.

**T6270 (2026-08-28): the achievement POST returns the updated progress.** `POST /achievements/{key}` folds the post-write progress into its response ADDITIVELY (`{key, achieved_at, progress: {quests: [...]}}`) — it reuses its already-open profile connection for `_check_all_steps`, so no extra connection. The frontend `questStore.recordAchievement` consumes `progress` in place of the follow-up `GET /quests/progress` that used to chase every achievement POST (deploy-skew fallback: if `progress` is absent the client still fires the standalone GET). `GET /quests/progress` is unchanged and still serves boot.

**T4780 (2026-07-08)**: Added `watch_*_tutorial` as step 0 of each quest (quest_config.py). Steps that can't be derived from DB state use the `achievements` table + these sets in quests.py:
- `KNOWN_ACHIEVEMENT_KEYS` — keys the `/achievement` POST accepts
- `_STEP_ACHIEVEMENT_KEYS` — subset loaded by the batched IN query in `_check_all_steps`
- `ACHIEVEMENT_TO_MILESTONE` — analytics bridge (key → milestone name)

Tutorial achievement keys: `watched_annotate_tutorial`, `watched_framing_tutorial`, `watched_overlay_tutorial`, `watched_publish_tutorial`. These ride the existing batched IN query — no extra DB queries. No migration needed: achievement keys are row-based (new keys just get inserted as rows, no schema change).

**T5330 (2026-07-18): onboarding must be blind to content shared into a profile.** quest_1's DB-derived backfills (`upload_game`, `add_clip`, `rate_clip`, `annotate_brilliant` — the `rc[...] >= 1` / `games` probes in `_check_all_steps`) now exclude shared-in rows by provenance: `raw_clips.shared_by` (pre-existing) and the new `games.shared_by TEXT DEFAULT NULL` (`_copy_game`, materialization.py) are **never NULL for a shared row** — precedence `sharer_email -> sharer_user_id -> "lost"` (`materialization.SHARED_PROVENANCE_LOST`), own content stays NULL. The 5-star auto-draft-reel needs no marker of its own: its `auto_project_id` lives on the shared clip row, so it's excluded transitively via `raw_clips.shared_by`. Migration `v026_games_shared_by.py` adds the column AND backfills legacy shared games by deriving provenance from their own already-shared clips in-profile (no Postgres read) — a legacy **game-only** share (zero clips) has no in-profile signal and is left NULL (documented residual: `upload_game` alone may read pre-checked for that one legacy cohort, but quest_1 as a whole is still correctly incomplete since there are no shared clips to inflate the other three steps either). Any future materialization path (e.g. T4910 link-share) MUST route through `materialize_game_share`/`_copy_game`/`_materialize_clips` to inherit this — see the T4910 acceptance criterion.

**`/progress` and `/claim-reward` must AGREE (bugs 34p/35p).** `GET /progress` reports a quest complete when it is in the user-scoped completed/claimed set (`get_completed_and_claimed_quest_ids`, user.sqlite — survives profile switch) WITHOUT re-deriving steps. `claim_reward` now honors that SAME set FIRST (returns idempotent `already_claimed`) before re-checking `_check_all_steps` against the active profile. Pre-fix, claim ignored the set and re-derived steps → a quest `/progress` showed complete 400'd on claim ("step 'watch_annotate_tutorial' is incomplete") whenever the active profile lacked the step data (zombie/resurrected account, or a different selected profile). This is idempotency, not a loosened gate: a quest only enters completed/claimed via a prior legitimate grant, so an UNclaimed quest still faces the full step check.

## Invariants & rules
0. **Account deletion must be COMPLETE + cache-safe (bugs 33p/34p/35p).** Two delete endpoints exist: the UI's `DELETE /api/privacy/delete-account` (AccountSettings.jsx — the REAL path) and `DELETE /api/auth/user` (test cleanup). Both now delegate to `auth._purge_user_data(user_id)`: rmtree local folder, `storage.delete_user_r2_data(user_id)` (paginated whole-user-prefix R2 delete, raises on error so a partial delete is never reported complete), invalidate in-process caches (`session_init.invalidate_user_cache` + `user_db.forget_user_db` → `_initialized_user_dbs` + `database.forget_local_db_state` → `_initialized_users`/`_user_sqlite_versions`/`_user_db_versions`), and `invalidate_user_sessions`. **Why it matters:** `is_new_user` in `session_init._init_slow_path` derives from the fresh user.sqlite's ABSENT `selected_profile`, NOT the Postgres users row. So purging R2+local+caches alone flips a reregister to `is_new_user=True` (seeds `NEW_ACCOUNT_CREDITS`=8, clean quest slate) EVEN IF the PG users row survives. Pre-fix `auth.delete_user` only rmtree'd local → the R2 user.sqlite survived → restored on next login → `is_new_user` stayed False → no seed (33p) + stale user-scoped quest state (34/35). **Residual (not fixed, would need a tombstone/schema change):** a cross-machine R2 re-sync between delete and reregister can still resurrect the R2 copy; the single-machine common case is closed by cache invalidation. **Seeding stays idempotent via the `is_new_user` gate** — a returning user (selected_profile present) is never re-seeded; do not seed reactively outside that gate.
1. **User lookups always via Postgres `users` table**, never auth.sqlite (auth.sqlite is legacy; pg.py replaced it — T1960).
2. **Backend tests hit the REAL dev Postgres** (conftest.py `pg_conn` deletes test users and TRUNCATEs shared tables, conftest.py:105-121). Guard refuses DSNs containing staging/prod keywords (conftest.py:95-99). Warn the user before running the suite. **Ledger-replay landmine (T6750):** `pg_conn` replays migrations 5+ on every test (`DELETE FROM schema_migrations WHERE version >= 5` then `RUNNER.run`). Migration **v003 adds a NARROW `shares_share_type_check`** (`video`/`game`/`annotation_playback`); v016/v020 later widen it to add `collection`/`game_link`. Replaying v003 against a DB that already holds a wider-type share row (real dev data under a non-`_TEST_USER_IDS` account, or a test that didn't clean up) raises `CheckViolation` — and any test that wipes the ledger below v003 without restoring it (T2930 runner test, T6345 gap tests) leaves it **permanently poisoned**, cascading 100s of setup errors on every later run. Fix in place: (a) `pg_conn` re-asserts v001–v004 as applied **before** `RUNNER.run` (so the stale narrow v003 CHECK is never replayed — `_SCHEMA_DDL` already builds HEAD schema, so 1–4 are fresh-DB no-ops) and clears leftover test-user `shares` **before** the replay; (b) the `preserve_schema_migrations` fixture snapshots/restores the ledger around the two ledger-wiping tests, pass or fail. **Never re-apply v003's narrow constraint manually in a test/fixture** — pg_conn already leaves the shares table at v020's full 5-value constraint (this was the bug in `test_share_playback.py::isolated_auth_db`, since removed).
3. **Postgres migrations do NOT auto-run** on deploy or startup. `init_pg_schema()` only applies `_SCHEMA_DDL` for fresh DBs (it does run pending PG migrations as a retry if the DDL hits UndefinedColumn — pg.py:333-340, but never rely on this). Trigger explicitly after deploy: `POST /api/admin/migrate-postgres` (admin session required, admin.py) or fly ssh (see CLAUDE.md § Migration System). `user_db`/`profile_db` are the opposite: they auto-migrate JIT at the per-user seam on first access (T5083/T5085, hardened by T8190) — no operator action, no admin endpoint at all since T5087.
4. Schema change workflow: Implementor updates the live schema (`_SCHEMA_DDL` / `ensure_database()` / `_USER_DB_SCHEMA`) AND the Migration agent writes a versioned file — both, always.
5. No silent fallbacks / no defensive fixes for internal data (CLAUDE.md); missing internal data = log + fail visibly. T4280 is the enforcement sweep.
6. **Admin credit read is R2-canonical (T4870):** `get_credit_stats_for_admin` now calls `sync_user_db_from_r2_if_newer(user_id, user_db_path, None)` for missing files on the explicit-ids admin path — NOT `ensure_user_database` (that creates a balance-0 stub on R2 error, poisoning subsequent admin loads). `local_version=None` is intentional: the file is absent, so any cached version is stale. `admin.py` passes `credits: null` (never `credits: 0`) when stats can't be read; frontend renders `—`. Stale-but-present files are read as-is (staleness window = machine lifetime). Write paths (`grant_credits`/`set_credits`) unchanged.
6. Modal function edits (`app/modal_functions/`) require asking the user about redeploy (`modal deploy app/modal_functions/video_processing.py`).
7. Admin endpoints each call `_require_admin()` imperatively (admin.py:51, ~25 call sites) — until T4610 lands, ANY new admin handler must call it explicitly or it is an open endpoint.
8. **No silent success on a zero-row write (T7500).** A handler running `UPDATE`/`DELETE ... WHERE id = ?` on internal data must check `cursor.rowcount == 0` and, on a no-op write, fail visibly (404 + WARNING naming the id) and fire NO side effect — a zero-row write means the target row is gone (e.g. a cascade-deleted game, T7470), and reporting success or recording a milestone against it manufactures a false trail. This is the write-side corollary of rule 5. Use the rowcount check on the write itself, NOT a defensive pre-existence SELECT (which is racy AND a banned defensive fix). Milestone/side-effect audit (2026-08-25, all `record_milestone`/`record_achievement` sites in `routers/` + `services/`): `finish_annotation` (games.py) was the ONLY unguarded write-then-side-effect-by-id — now fixed. Every other milestone site is safe by construction: INSERT-with-lastrowid (game_created, clip_created, game share), SELECT-then-404 before the milestone (video_downloaded, collection_downloaded, export_started), atomic Postgres grant gated on `result['applied']`/`['finalized']` (quest_completed, credits, recovered export), or a background worker on a genuinely-processing job (export_worker). `credit_ledger.confirm_reservation` already SELECT-guards (`return False` if no reservation). `save_playhead` (games.py) is an UPDATE-by-id with NO side effect (sendBeacon target, response unconsumed) — deliberately left unguarded. A future write handler with a side effect must not re-open this class; re-sweep is unnecessary unless a NEW milestone/side-effect call site is added after an unchecked by-id write.
9. **Counted-once analytics reads must lock the row they gate on (T7570).** `analytics.update_session` decides `is_new_session` (activity older than the 30-min `SESSION_IDLE_CAP_SECONDS` boundary) and, only when True, increments the PG `user_actions` session_started count AND writes a SQLite `user_action_log` row. It is reachable CONCURRENTLY for one user: `/api/auth/me` schedules it fire-and-forget (`asyncio.create_task`, decoupled from the response) and the client's `fetchWithRetry` re-issues `/me` on a Fly cold-start, so the first call and the retry's call overlap on the offload threadpool (`/api/auth/heartbeat` is a second overlapping caller). A plain `SELECT` of `last_active_at` takes NO lock, so two overlapping calls both read the stale value, both see `is_new_session=True`, and both count — a lost-update race that inflated prod session counts ~2x (SQLite `user_action_log` pairs ~200ms apart are the fingerprint: that row is is_new_session-gated, so a pair PROVES two concurrent True evaluations, not two real sessions). Fixed by `SELECT ... FROM user_segments WHERE user_id=%s FOR UPDATE`: concurrent callers serialize on the row lock and, under READ COMMITTED, the second re-reads the post-commit row so its `is_new_session` is False. This is the single-owner fix, NOT a dedupe window — correct under ANY number of invocations. **PG `user_actions` is the AUTHORITATIVE session store** (SUM-aggregated, feeds the T7460 scorecard/admin); SQLite `user_action_log` is a best-effort is_new_session-gated audit trail and legitimately runs a small constant BELOW PG (PG is keyed by `(user_id, action, PLATFORM)` and records each platform-debut's `first_at` even when not a new session; a brand-new user's FIRST session has `is_new_session=False` because `user_segments.last_active_at` DEFAULTs to `now()` at signup, so PG's first-insert records it but the gated SQLite log skips it). Do NOT re-gate the PG first-insert strictly on `is_new_session` — that would undercount every user's genuine first session. Guard: `tests/test_analytics.py::TestUpdateSession::test_concurrent_calls_count_one_session`.

10. **Attempt/outcome/failure taxonomy — success NEVER fires from an intent-side path (T7510).** Every funnel action now emits an `_attempted` at gesture time and, at its durable completion point, EITHER `_succeeded` OR `_failed` carrying a coarse `reason` (`record_milestone(user_id, event, reason=...)`, closed vocabulary `analytics.MILESTONE_REASONS = {timeout, network, refused, sync_failed, user_abandoned, unknown}`). A failed attempt is stored as `"{event}:{reason}"` in `user_actions`/`user_action_log` — a NEW ROW in the existing aggregate, not a new table (`feedback_analytics_in_house_aggregates_only` is satisfied: extension, not a parallel store). `admin.py::_rollup_failures` splits on `:` at read time to fold reason-rows under their own base event for the journey view. The core fix: `game_created` (games.py:455, fires on the PENDING insert, no R2 bytes yet) is relabeled "Upload Attempted"; the durable success is `game_upload_succeeded`, emitted from `finalize_upload` (games_upload.py, after R2 HEAD-verify) — this is the ONLY new server-side upload-success emission, and the funnel's "Uploaded" stage now reads FROM it, not from `game_created`. `list_pending_uploads`'s T7490 reaper emits `game_upload_failed:user_abandoned` for each orphaned pending row it flips to `upload_failed`. `clip_save_failed:sync_failed` is NOT emitted from clips.py itself (durable sync resolves AFTER the handler returns, inside the middleware) — it's emitted from `db_sync.py`'s generic durable-sync-503 branch via a small `DURABLE_SYNC_FAILURE_ACTIONS: {(method, path): action}` table, the pattern to extend for any FUTURE durable route that needs a taxonomy failure emission. `payment_failed:{refused|unknown}` emits from the Stripe `payment_intent.payment_failed` webhook. `move_succeeded` emits from `move_reels_to_profile` (downloads.py) after the target profile's explicit sync confirms OK, replacing the old frontend-fired `moved_to_my_reels` achievement bridge (quests.py — the achievement row still exists for quest-step derivation, it just no longer double-bridges to a content milestone). `annotation_completed` is NOT a content outcome — it's relabeled "engagement" (watched video, not clips) and rendered in a visually separate band in `UserDetailPanel.jsx`. Tier-5 frustration signal (retry-burst, partial scope — repeat-visit/rapid-fire deferred to T7515) is a READ-TIME-ONLY derivation over per-user `user_action_log` timestamps (`admin.py::_detect_retry_bursts`, >=3 of the same attempt action within 60s) — no new storage. Tier-2 (client error capture) reuses T7560's existing ring buffer (`clientLogger.js`) and adds one log-only ingest beacon (`POST /api/client-errors/report`, telemetry.py) mirroring the T7480 upload-failure-beacon contract exactly — no DB write, explicit `get_current_impersonator_id()` guard BEFORE logging anything (this endpoint does not route through `record_milestone`, so it can't rely on that function's built-in guard). Guard tests: `tests/test_analytics_dashboards.py::TestAttemptOutcomeTaxonomy`/`TestRetryBurstDetection`, `tests/test_t4320_durable_clip_gestures.py::test_clip_save_forced_sync_failure_emits_clip_save_failed`, `tests/test_telemetry.py::TestClientErrorReportBeacon`, `tests/test_payments_webhook_idempotency.py::TestPaymentFailedWebhook`.
11. **An R2-DELETING migration has no dry-run step once registered (T7830, v048 is the first).** Unlike the admin-run standalone-script pattern (`--apply` gated behind an interactive `type 'yes'` confirmation), a `profile_db` migration runs unattended the first time each account's JIT seam fires (T5087: no admin bulk-trigger exists anymore either) — there is no separate confirmation gate inside the migration itself. Safety for this class of migration rests entirely on (a) the deletion PREDICATE being as narrow/reviewed as possible (v048 reuses `app/services/orphan_raw_clips.py`'s sweep-signature gate — only `auto_`-prefixed unreferenced objects, never a broader "unreferenced" set) and (b) logging every deleted object at INFO (user/profile + path + bytes) so server logs are the only post-hoc audit trail. Any future migration that deletes R2 objects (or is otherwise irreversible) should keep the predicate in a separately-reviewed/tested pure function rather than inlining new "delete if X" logic directly in `up(conn)`, and log every deletion the same way.
    - **T7515 (tiers 3 + 4 — mid-funnel frustration instrumentation, extends this taxonomy, NO migration).** Two new fire-and-forget sinks in `telemetry.py`, both routing to new `analytics.py` functions that copy `record_milestone`'s impersonation guard verbatim (they don't route through it). **Tier 3 `record_impression(kind, name, session_count)`** (`POST /api/telemetry/impression`) counts blocking-dialog / error-toast IMPRESSIONS: a new `"{kind}_impression:{slug}"` row in the existing PG `user_actions` aggregate (`kind` closed = `{toast,dialog}`; `name` slugified server-side to a bounded PII-free key) + a per-user `user_action_log` detail row carrying the raw name + per-session repeat count. **Tier 4 `record_session_exit(user_id, last_screen, dwell, trail)`** (`POST /api/telemetry/session-breadcrumbs`) writes an exit breadcrumb (last screen + per-screen foreground dwell + ordered screen trail) ONLY to the per-user `user_action_log` — **no PG write at all** (per-event detail stays out of the aggregate; screens bounded to `BREADCRUMB_SCREENS`). Frontend transport is `utils/uiTelemetry.js`: `recordUiImpression` fires from the SHOW gesture (Toast `addToast` / `ConfirmationDialog` open); `installSessionBreadcrumbs` (main.jsx, once) subscribes to `editorStore` mode transitions for in-memory dwell accumulation and flushes via `sendBeacon` on `visibilitychange:hidden`/`pagehide`. **Three review-hardened invariants (easy to regress):** (a) impression key MUST be a caller-pinned stable `impressionKey` prop, NEVER the rendered title — titles are runtime-interpolated with PII and would explode `user_actions` cardinality (one singleton row per distinct message); a dialog/toast with no `impressionKey` is simply not counted. (b) the breadcrumb buffer is RESET after every flush (`_resetBreadcrumbBuffer`) so a hidden→resumed→re-hidden tab sends only crumbs accrued since the last flush, never a duplicate `session_exit` row with the whole trail. (c) the session-breadcrumb endpoint LOGS + drops an anonymous beacon (`"no resolvable user context"`) rather than silently accepting — the cross-site `sendBeacon` transport can arrive with the session cookie stripped, and a silent drop would hide an auth/transport regression. `/api/telemetry/` is in the middleware no-auth prefix list (fire-and-forget must never 401). Guard tests: `tests/test_telemetry.py::TestImpressionBeacon`/`TestSessionBreadcrumbBeacon` (incl. anon-drop + impersonation-zero-trace), `tests/test_analytics.py` (impression aggregate upsert + breadcrumb sanitize + impersonation guards), `src/frontend/src/utils/uiTelemetry.test.js` (impersonation zero-send, resumed-tab dedup). Deferred second-tier items (device/UA capture, viewed_duration semantics, acquisition attribution, help-seeking signals) remain OUT of scope / open.

## Migration system (3 tracks)
Files: `src/backend/app/migrations/{track}/v{NNN}_{description}.py`; each defines a `BaseMigration` subclass with `version`, `description`, `up(conn)` (migrations/base.py:7). Track runners in each track's `__init__.py`.

**`user_db`/`profile_db` migrate ONLY just-in-time at the per-user DB-load seam** (T5083/T5085, hardened by T8190, cutover completed by T5087) — `ensure_database`/`ensure_user_database` run a leaner migration primitive (`migrate_local_profile_db_at_seam`/`migrate_local_user_db_at_seam`, operating on the file the seam's own restore already downloaded — no second R2 fetch) on every FIRST access, before the DB is marked initialized; a warmed profile costs nothing further. **There is no bulk-sweep backstop anymore** — `run_all_migrations`/`_migrate_user`/`_migrate_user_db`/`_migrate_profile_db`/`POST /api/admin/migrate` were all deleted once JIT covered every access path (T5085's non-login writers) and proved itself in prod. An account that never comes online simply migrates whenever it next does (the long-tail property is by design, not a gap — see CLAUDE.md § Migration System). **Postgres stays deploy/admin-triggered only** (`POST /api/admin/migrate-postgres`, backed by `migrate_postgres()`) — the JIT seam covers `user_db`/`profile_db` exclusively, per the JIT-migration epic's settled decision 7; Postgres has no per-user seam to hang a JIT trigger off of. Any per-user failure (WAL-busy, CAS-refused sync, below-head R2 verify, an outright missing/registered profile) raises `migrations.MigrationBlocked`, mapped by a `main.py` handler to a retryable HTTP 503 `{"code": "pending_migration"}` — never a silent below-head open. Full mechanism (locking, CAS re-pull-retry-once, orphan policy, gesture-persistence framing): persistence-sync.md §T5083.

| Track | DB | Version mechanism | Latest (2026-07-03) |
|---|---|---|---|
| `postgres` | Fly Postgres | `schema_migrations` table | v025 (v019 credits T5840; **v020 `game_link` share_type + share_games.game_date T5720**; **v021 `share_claims` T5730**; **v022 `user_usage_daily` T5770** — v020/v021 were reserved by the then-unmerged Share the Game branches, so T5770 landed at v022; all three merged together and the track is contiguous 1..22 again; **v023 `pending_teammate_shares` recipient_email->invited_email rename T7550** (sibling branch, landed on master); **v024 `daily_counters` attempt/outcome columns T7510** (`game_uploads_succeeded`/`_failed`, `clips_attempted`/`_failed`) — additive only, no new table; **v025 clear stale `game_storage_refs` T6770** — `DELETE FROM game_storage_refs` (dead pre-T2930 sediment, see the T6770 note above); paired with profile_db v047 which repopulates it as the live derived ref-set. Re-verify sibling branches before numbering the NEXT postgres migration.) |
| `profile_db` | profile.sqlite | `PRAGMA user_version` | v048 (**v048 delete sweep-orphan `raw_clips/` extracts T7830** — DATA-ONLY (no column), FIRST migration that calls `delete_from_r2`: reuses `app/services/orphan_raw_clips.py`'s classification logic (extracted from `scripts/cleanup_orphan_raw_clips.py`, the standalone dry-run/`--apply` script T7830 shipped first — both now import the same module rather than duplicating the reviewed reference-set-union + sweep-signature-gate logic) to delete ONLY `auto_`-prefixed unreferenced `raw_clips/` objects per profile, R2_ENABLED-guarded, idempotent, logs every delete at INFO (no dry-run step once wired as a migration, so logging is the only audit trail — see the migration's own docstring before copying this pattern); v024 poster_filename T4890; v025 slowmo_section_start/end freeze T5090 — backfills from R2 archive; v026 `games.shared_by` + backfill T5330 — see Quest system section; **v027 `working_videos.detections_data` T5600** — video-level player-detection store, backfills by hoisting the union of existing regions' embedded detections via `app/services/video_detections.hoist_video_detections`, see keyframes-framing.md § Video-level player-detection store; v028 export_jobs.stage/output_key; v029 working_clips.rotation; **v030 games source reference T5800** (cross-profile game attribution); **v031 reclassify teammate-tagged clips to Team T5725** — DATA-ONLY (no column): moves every teammate-tagged My-Athlete/NULL `raw_clips` row to `my_athlete = 0`, tags preserved, idempotent, positional tuple-row reads, numbered v031 to avoid the v030 collision with T5800, see annotate.md § Teammate tagging is Team-layer only; v032 poster_frame_time/poster_source + projects.poster_marker_time T5410; v033 heal moved-reel attribution T5830 (DATA-ONLY); v034 intro card library T5195 — CREATEs `intro_cards` (per-profile card library) + `final_videos.intro_card_id` (nullable), see § Intro card library below; v035-v043 intro-card/text-overlay follow-ups (subtitle_text, dead-field nulling, backfill, regions shape, intro_min_duration add+drop — not individually re-audited here, see each migration file's docstring); **v044 `working_clips.framing_version` T4330** — mutation counter for framing-action 409 conflict detection; **v045 canonicalize `working_clips.segments_data.boundaries` T4340** — DATA-ONLY (no column): rewrites pre-existing splits-only rows to the full-list `[0,...splits,duration]` format, duration JOINed live from `raw_clips` (no new column — one canonical duration source), reuses `highlight_transform.canonicalize_segments_data`, idempotent, skips+logs orphan rows with no derivable duration, see annotate.md § segments_data write-time-canonical; v046 `working_videos.framing_snapshot`/`highlight_carry_note` (sibling branch, landed on master, not otherwise documented here); **v047 backfill `game_storage_refs` T6770** — DATA-ONLY (no column): re-derives every profile's Postgres ref rows from its real `game_storage` rows via `insert_game_storage_ref`, idempotent, doubles as the one-time drift reconciliation the 2026-07-23 retrospective flagged) |
| `user_db` | user.sqlite | `PRAGMA user_version` | v006 |

- **Pending is track-dependent (T6345, base.py `get_pending`):** for `postgres`, pending = every registered version **NOT IN** the applied SET (`SELECT version FROM schema_migrations`, via `get_applied_versions`) — NOT `> MAX(applied)`. A migration numbered BELOW an already-applied version (out-of-order branch merges: v022 landing before v020/v021 were merged) is a real gap that MUST still be applied; the old MAX comparison skipped it silently and permanently while `run_all_migrations()` reported success (dev+staging incident 2026-08-02). Pending stays ascending (migrations sorted in `__init__`). For the SQLite tracks (`user_db`/`profile_db`) pending is still `> current` — `PRAGMA user_version` is a single integer that cannot express a gap on disk. `get_current_version` (still `SELECT MAX(version)` for postgres) is now only a REPORTING value (`migrate_postgres` results), not the pending gate.
- **SQLite-track silent-skip assessment (T6345):** the same out-of-order-merge shape exists for `user_db`/`profile_db` — a migration numbered BELOW the stored `user_version` is `<= current` and never runs — but there is no per-version ledger to diff against, so no set test can recover it. It is a numbering-discipline hazard (never renumber below an applied version), not a runner bug; the postgres fix does not translate. Documented in a comment on `get_pending`'s non-postgres branch.
- Never reuse a version number; renumber only UP (never below an applied version — see the SQLite assessment above).
- **`up(conn)` receives a TUPLE row factory for SQLite** (plain `sqlite3.connect`, migrations/__init__.py:91/119) — index rows positionally (`r[0]`), NOT `r['col']`. String-indexing crashed the v017 backfill for 4 prod users (memory: v017 rowfactory bug). Test the row-reading path with data, not just the empty case.
- `PRAGMA user_version` (schema) and the `db_version` table / R2 `db-version` metadata (sync) are independent — see persistence-sync.md.
- Status check: `get_migration_status()` (migrations/__init__.py) reports CODE head versions only. `get_migration_status_for_user(user_id)` (T5970) additionally reports each registered profile's ACTUAL R2 `PRAGMA user_version` vs head — READ-ONLY (temp download + read + unlink, no R2 write), one user only (never the mutating full-R2-walk). Exposed at `GET /api/admin/migration-status[?user_id=]` (admin-gated); no user_id -> head only (zero cost). Use it to answer "is this env at head?" without running migrate.

## Intro card library (profile_db, T5195)
The player-intro card library lives in `profile.sqlite` (epic decision 7 — a card names one athlete and its image sits under the per-profile R2 `intro/` prefix, so the row belongs beside the media). Storage layer only in T5195: no editor UI (T5205), no rendering (T5210).
- **Table `intro_cards`** (v034 + `ensure_database()`): `name` (library label, NEVER rendered), `shown_fields` (JSON list ⊆ `['position','class','team']` = `user_db.INTRO_FACT_FIELDS`; records WHICH facts show, the VALUES live on the profile), `treatment` (`gold|dark|photo-forward`), `title_text`, `image_key` (R2 key, T5190; `image_cutout_key` is a DEAD column — T6950 removed its write surface and all reads, ONE image rule = image_key everywhere; T5200 never shipped), `focal_x`/`focal_y`/`zoom` (framing), `text_elements` (msgpack `{slot: TextSpec}`, STYLING only), `duration`, `is_default` (**dead column, T6680** — see below). **NO template/layout column** — composition is DERIVED from `(has_photo, len(shown_fields))` in the ONE shared helper `app/services/intro_cards.derive_composition` (mirrored in frontend `utils/introCardComposition.js`); storing it would be redundant state. `no photo|0 facts -> title-only; +1 -> hero; +2 -> broadcast; +3 -> recruiting`.
- **`focal_x`/`focal_y`/`zoom` NULL = inherit the profile's framing** (decision 3b) — a distinct valid value, NOT defaulted at write time (or a later profile-framing change stops propagating). Validators (`app/services/intro_cards.py`) return None for NULL, raise (→400) out of range; `bool` is rejected from numeric fields. (Unrelated to the retired card-level default below — this is a per-FIELD inherit-the-profile's-framing value, still live.)
- **`final_videos.intro_card_id`: NULL and 0 are now EQUIVALENT — both mean "no intro"** (T6680, superseding decision 8). There is no profile default to inherit; `NULL` = "nothing explicitly attached" and `0` = "explicitly opted out", but both resolve to no intro at every egress site. The ONLY value that reaches egress is a concrete positive id, and every writer of a concrete id is consent-gated at the attach gesture (verified T6680 — no other writer exists; move-reels deliberately omits the column, re-export carries forward the same profile's already-consented value). `resolve_intro_card_id(intro_card_id)` (`services/intro_cards.py`) is the single-argument pure resolver: `0/NULL -> None`, `<positive> -> that id`. `get_default_intro_card` and the duration-gated inherit branch are DELETED, not dead-branched — `grep get_default_intro_card` / `grep is_default` should show zero live resolution callers (the T6680 design's auditability requirement). Column-guarded on the delete write (`column_exists`); `ensure_database()` creates the `intro_cards` TABLE independently of v034, so a below-head profile can hold cards while the column is still absent — the guard is load-bearing.
- **Historical note (closed hole, T6680):** before T6680, `NULL` inherited the profile's `is_default=1` card with NO consent gate (only an explicit non-null/non-zero pick was gated) — combined with `v040` having already backfilled every profile's default, this was a LIVE, not latent, unconsented-exposure hole (a NULL-attached reel on an unconsented profile published a minor's default card on share pages, download burns, and frozen collection-share links). T6680 closed it by REMOVING the inherit path structurally (fewer code paths, one consent chokepoint at attach) rather than adding a second egress gate. See `docs/plans/tasks/T6680-design.md` §1.4 for the full incident writeup.
- **CRUD** `routers/intro_cards.py` (`/api/intro-cards`, session+profile scoped, writes ride normal TrackedConnection R2 sync): list/create/`PATCH {id}` (SURGICAL — SET clause built from `model_fields_set`, so explicit `null` clears focal/zoom to inherit while an unset field is untouched)/`DELETE {id}`. **`is_default`/set-default retired end-to-end (T6680):** no `POST {id}/default` endpoint (removed), no auto-default-on-first-card write, no delete-time promotion, `is_default` dropped from `_serialize`/`load_profile_cards`. The column stays in the schema as harmless dead data (no destructive migration; existing `is_default=1` rows are real user-created cards, still fully usable via explicit attach). **Delete** detaches referencing reels (`UPDATE final_videos SET intro_card_id=NULL WHERE intro_card_id=?`) + deletes the row in one transaction, then removes the R2 image **only when nothing else references it** (T6650 — see § Intro image shared ownership). TextSpec/treatment/shown_fields/focal/zoom/duration invalid → **400** (never 422, never silent clamp).
- **Intro image SHARED OWNERSHIP (T6650).** One R2 intro object can be referenced by the PROFILE (`user.sqlite` `intro_photo_key`) AND by any number of CARDS (`profile.sqlite` `intro_cards.image_key`, SEEDED from the profile key on card create via `introCardDefaults` and copied again on duplicate). A delete triggered by ONE owner must NEVER destroy the object while another owner still points at it — the historical bug was card-delete hard-deleting the shared object and silently blanking the profile photo. The guard is the shared helper `services/intro_cards.intro_image_has_other_reference(cursor, user_id, profile_id, key, *, exclude_card_id=None, include_profile_key=True)` (checks the profile key via `get_intro_photo_key` + a `SELECT 1 FROM intro_cards WHERE image_key=?`). Wired at exactly two GESTURE sites: card delete (`routers/intro_cards.delete_intro_card`, checked in the same delete txn — the row is already deleted so surviving cards are counted without `exclude_card_id`) and profile photo remove/replace (`routers/profiles`, `include_profile_key=False` since the profile is the deleter; a non-current-profile request conservatively reports "referenced" to never destroy an unprovable object). An EXCLUSIVE object is still removed (no orphan growth). The key-belongs-to-profile-prefix check is the single source of truth `intro_media.validate_intro_key` (raises → 400), called by `delete_intro_image` and unconditionally up front on the remove path. **T5230's compliance purge is a SEPARATE whole-prefix path (`storage.delete_user_r2_data`) and stays UNCONDITIONAL** — the reference check is NOT in `delete_intro_image` and does not reach the purge. A DANGLING key (R2 object gone / a card still pointing at a re-uploaded profile's old key) surfaces as a visible, logged "photo missing" state on both the card photo control (`IntroCardRail.jsx`) and the Edit Profile thumbnail (`ProfileIntroSection.jsx`) via an `<img> onError` handler — never a silent broken image; the card's "Use profile photo" recovery is un-gated so it can refresh in place.
- Frontend `stores/introCardStore.js`: raw rows only, no derived state (composition via the shared util selector) — never transform API responses before storing. `setDefault` action and `selectDefaultCard` selector are retired (T6680); the picker (`IntroCardCarousel`) has no inherit affordance — `[specific card | no intro]` only, no-intro as the initial selection. **T6670: the picker host `IntroCardPicker` has an inline "New card" flow** — the carousel surfaces an optional `onCreateNew` tile; the host swaps into an inner CREATE view that mounts the SAME `IntroCardEditorContainer` the library uses (on a `createCard`'d row, which the store prepend makes appear in the carousel), stays mounted throughout (never cycles `isOpen`, so the reel/collection identity + draft survive), and on finish lands back on the carousel with the new card pre-selected as the DRAFT — the actual attach is still the caller's ONE existing `onSelect`-on-OK write (no second path). Consent is NOT bypassed: a no-consent profile hitting "New card" gets the same inline `ConsentGate` before create (and `create_intro_card` still 403s). The share-dialog carousel (`CollectionShareModal`) omits `onCreateNew`, so no create tile leaks into the frozen-at-share flow.
- **Children's-data compliance (T5230).** An intro card is a MINOR's likeness + parent-typed facts, publicly visible when shared, so three guardrails bind the pipeline: (1) **Consent gate on CREATE** — `routers/intro_cards.create_intro_card` refuses (**403**, matching the attach-time gates in `downloads.py`/`collections.py`) unless `user_db.get_intro_consent(user_id, profile_id)` is set. The raw photo upload (`profiles.upload_intro_image`) is INTENTIONALLY not gated: per EPIC.md the risk is public EXPOSURE not storage — the photo sits in a private per-profile prefix and only becomes shareable via a created+attached card, both gated. (2) **Privacy export + purge cover intro data.** `privacy.py` `POST /export-data` now emits, per profile, the `intro_cards` rows (+ decoded free text) AND the user.sqlite KV (`intro_consent_at`, `position/class/team`, `full_name`, `intro_photo_key`) — including KV for a profile whose `profile.sqlite` isn't locally cached (glob would miss it). Account-delete purge needs NO extra code: `_purge_user_data` → `storage.delete_user_r2_data` deletes the WHOLE `{env}/users/{uid}/` prefix (subsumes `profiles/{pid}/intro/` images) + rmtree local + R2 user.sqlite (which holds the KV) — VERIFIED by `test_t5230_intro_compliance.test_purge_deletes_r2_intro_object` (T6090 lesson: prove the real R2 object, not just the row). (3) **No biometrics, ever** — no face recognition/templating anywhere in the intro pipeline (T5200 cut-out is background SEGMENTATION only); enforced by the static grep guardrail `test_no_face_recognition_in_intro_pipeline` (scans `player_intro.py`, `intro_cards.py` router+service, `intro_media.py`, `text_render.py`). **No DOB/birthdate/age field exists** — guardrail #1/#2 satisfied by ABSENCE (`INTRO_FACT_FIELDS == ('position','class','team')`, `class` = grad year free text); a future DOB feature must be app-encrypted on R2 SSE, not scaffolded now.

## Migration-window column guard audit (T5970)
The bug class this audit closed: versioned migrations did NOT auto-run on deploy (they ran via
`POST /api/admin/migrate` afterwards), so between deploy and that action every per-user SQLite was
at the OLD schema while NEW code was live. A hot read that NAMES a column a pending migration adds
-> `sqlite3.OperationalError: no such column` -> 500 for every affected user in that window
(`SELECT *` is safe — only an explicitly-named column crashes). **T5083/T5085 (2026-08-31) closed
this specific window for every path they cover** — the JIT seam now migrates a request's DB to
head before the handler runs (T5083) and every non-login opener too (T5085, see
persistence-sync.md § T5085) — but the GUARDS below are all kept, not deleted, and the audit's
findings stay accurate as a map of every hot read/write that names a versioned column: they are now
permanent defence in depth against ROLLING-DEPLOY SKEW (EPIC decision 8 — old code on machine A can
still serve a request against a DB machine B's newer code just migrated one version past what A's
queries expect), not the closed deploy->migrate window. See `column_exists`'s docstring in
database.py for the canonical up-to-date rationale.

**Key structural fact (why the risk is narrow):** `ensure_database()` / `_USER_DB_SCHEMA` base schema already contains EVERY audited column, so a FRESH DB is always at head. The only below-head state is a PRE-EXISTING DB that predates the column and hasn't migrated. In any deploy->migrate window only migrations NEWER than the last-run baseline can be un-run. Prod is guaranteed ≥ v18 (v017 re-migration, see Landmines). **profile_db v017-v023 add NO columns** (grep `ADD COLUMN`). So the ONLY columns that can realistically be missing on a live DB are v024-v029.

Audit result (profile_db unless noted; "≤v18 → window closed" = provably migrated everywhere, guarding would defend an impossible state, CLAUDE.md forbids it):
- v003 my_athlete/tagged_teammates/raw_clips.shared_by, v004 stroke_width/fill_enabled/fill_opacity/dim_strength, v005 highlight_shape, v007 aspect_ratio/tags, v008 game_ids, v009+v010 clip_count/quality_score/rating/rd/match_count/source_clip_id/clip_start_time, v013 auto_export_attempts, v015 last_playhead_position, v016 clip_game_start_time — all read on hot paths (clips/games/gallery/collections/rank/overlay-render) BUT **≤v18 → NO GUARD (window closed)**.
- **v024 poster_filename**: NO SELECT names it (only overlay.py INSERT at render + poster.py UPDATE) → NO GUARD (not read).
- **v025 slowmo_section_start/end**: named on `POST /downloads/publish` (downloads.py, `publish_to_my_reels` SELECT) and the render INSERT (overlay.py `_finalize_overlay_export`); poster.py self-migrates per-profile to head first (T5110). **GUARDED (T6030):** both sites now `column_exists(cursor,"final_videos","slowmo_section_start")` — the SELECT projects `NULL AS slowmo_section_*` when absent (poster reconstructs the section from live working clips during the window), and the INSERT omits both columns from the column list AND the positional VALUES tuple (never inserts into a nonexistent column; the v025 default is NULL and the backfill populates it later). The INSERT was the worse of the two — no export could finalize at all on a below-v025 DB until the migrate ran. Red-first proven with-rows in `test_t6030_slowmo_migration_window.py` (drives the shipped functions; reverting either guard turns the guarded test red with the OperationalError); one `PRAGMA table_info` per path, not per row (query_counter).
- **v026 games.shared_by**: read at quests.py `_check_all_steps` (`SELECT 1 FROM games WHERE shared_by IS NULL`), which runs on **bootstrap** (bootstrap.py) → every user's app load. **UNGUARDED → FIXED (T5970):** `column_exists(cursor,"games","shared_by")`; during the window fall back to `SELECT 1 FROM games` (nothing can be shared-in yet since materialization is what sets the column, so shared_by-IS-NULL ≡ any game). Test: `test_t5970_quest_shared_by_migration_window.py` (with-data, red-first proven).
- **v027 detections_data**: GET overlay-data — ALREADY GUARDED (overlay.py, 6cc0c2a1). **v028 stage/output_key**: `_has_stage_columns` (exports.py). **v029 rotation**: clips.py x3 + export SELECTs.
- **user_db/v004 total_usage_seconds**: read only at admin.py:137 (admin-only) → NO GUARD (not a user load path).

Net: exactly one unguarded hot read existed (games.shared_by on bootstrap); fixed. The sanctioned guard is `column_exists()` (database.py) / `_has_stage_columns()` (exports.py) — probe then project a default; one PRAGMA per call, never per row.

**Write-side sibling guard (T6550, poster_marker_time v032).** The window bites WRITES too, and asymmetrically: a guarded READ can degrade to a default and hide the gap, so the matching WRITE gets forgotten. `projects.poster_marker_time` (v032, T5410) had a guarded read (`get_project_poster_marker_time`) but a bare-UPDATE write (`set_project_poster_marker_time`) — dragging the overlay poster marker in the deploy→migrate window raised `no such column` → 500. **Sanctioned guarded-WRITE pattern:** the service fn `column_exists`-probes and returns a distinct not-written signal (`set_project_poster_marker_time` now returns `bool` — False = column absent) instead of a bare UPDATE; the router maps that to **HTTP 503** "not available yet (pending migration)", NEVER a lying `{success:true}` (silent-fallback violation — user believes their drag saved) and NEVER a raw 500 (this is a known, time-bounded state, not a bug). 503 (not 4xx) is deliberate: the overlay action layer (`overlayActionStore.isRetryableFailure`) treats 5xx as retryable → bounded retry + persistent "your edits aren't saving / Retry" toast + export-gate block, so the drag re-lands automatically once the admin migrates — hence NO frontend change was needed. This is the SAME 503 convention already used by every other guarded write: `profiles.update_current_intro_min_duration` (intro_min_duration_seconds v041), `downloads` intro-attach + `intro_cards` clear (intro_card_id v034), `clips` set_rotation (rotation v029). Test: `test_t5410_poster_selection.py::test_marker_time_column_guarded_pre_migration` (setter returns False, no self-repair) + `test_t6030_...::test_poster_time_write` (real router → 503, red-first vs the OperationalError). **Sweep result (T6550):** every guarded READ whose column also has a WRITE path was checked — rotation/intro_card_id/poster_frame_time+poster_source/slowmo(T6030)/subtitle_text/intro_min_duration writes are all ALREADY 503-guarded (symmetric). Two guarded-read/unguarded-write asymmetries remain, both on write-heavy render/materialize paths needing their own design call (NOT trivial mirrors), filed as a follow-up: **(A)** `working_videos.detections_data` (v027) — guarded read in GET overlay-data vs unguarded `upsert_working_video` INSERT+UPDATE (needs: confirm the working-video render path is reachable below-v027, then either mirror T6030's conditional-column-list guard or document a force-migrate exemption); **(B)** `games.shared_by` (v026) + `source_profile_id`/`source_game_id` (v030) — guarded reads vs the unguarded `materialization.py` copy-game INSERT into a possibly-below-head recipient DB.

**Both asymmetries CLOSED (T6780).** Reachability was confirmed for BOTH under the project's operating model — neither write path self-migrates the DB to head (export finalize uses raw `get_db_connection`; materialization opens the recipient raw via `_open_profile_db` + `ensure_profile_db_local`, which only R2-syncs, never migrates), and both sit next to LIVE sibling deploy-window guards, so a below-head DB genuinely reaches them. Crucially the fix SHAPE differs by column semantics, and that is the reusable lesson: a guarded WRITE mirrors the READ's degrade-to-default ONLY when the column is OPTIONAL; a column the row's *meaning* depends on must REFUSE, not omit.
- **(A) `working_videos.detections_data` (v027) → COLUMN-OMIT (degrade-and-continue), NOT 503.** `upsert_working_video` (`services/export_finalize.py`) named `detections_data` unconditionally in its resume UPDATE + both INSERT branches — the exact gap the T4350 note in `test_t6030_...` had already flagged and deferred. Now a single `_has_detections = column_exists(cursor,"working_videos","detections_data")` (one PRAGMA per finalize) drops the column from the SET / column-list when absent. A bare 503 was the WRONG shape here: this is a render/finalize path (aborting the whole export mid-way is worse UX than a UI drag failing), and `detections_data` is an OPTIONAL video-level detection cache that v027's migration backfills later — NULL-now is correct. Mirrors T6030's slowmo INSERT guard. (The v046 carry-column branch never needs the guard: v046 > v027, so carry-cols-present ⇒ detections_data present.)
- **(B) `games.shared_by` (v026) + `source_profile_id`/`source_game_id` (v030) → REFUSE (raise `RecipientProfileBelowHead`), NOT column-omit.** Both cross-profile copiers (`_copy_game` share-materialization; `ensure_game_reference` move-reel) route through the shared `_insert_game_with_videos` primitive, which now PRAGMA-probes the target `games` table and raises `materialization.RecipientProfileBelowHead` if ANY column the copy writes is missing — guarding the ONE shared primitive covers both copiers and any future copied column, so a new call site can't skip it. Omission is NOT an option: `shared_by` is the onboarding-blind quest provenance marker (T5330) and `source_profile_id`/`source_game_id` DEFINE a reference row (T5800) — a row missing them is CORRUPT, not merely incomplete. The 503-hypothesis T6550 floated was REFINED, not adopted wholesale: the refusal maps to the right shape per caller — **share paths DEFER via the existing `create_pending_share` fallback** (`games.py` share_game/share_playback add `RecipientProfileBelowHead` to their `except RefreshFailed`; `clips.py` `_materialize_or_pend` catches it → pending; `session_init.py` T3230 auto-materialize already swallows+leaves-pending), so the share re-materializes on the recipient's next login post-migration — better than 503-ing a whole multi-recipient batch for one below-head recipient; **the move-reel path (`downloads.py`) returns HTTP 503** (retryable, no deferral channel, rolls back so no partial reference persists). NEVER a lying success, never a raw 500. Note the pre-existing `downloads.py:1459` guard was on the SOURCE cursor projection; the TARGET write was the unguarded half. Tests: `test_t6030_migration_window_structural_guard.py::test_upsert_working_video_below_head` (A, drives the write against the floor-v23 fixture that drops detections_data) + `::test_game_copy_below_head_refuses` (B, asserts the primitive raises rather than 500s). No `POST_V023_COLUMNS` / `HEAD_VERSION_AUDITED` change — all four columns were already tracked; T6780 added write-side DRIVERS only, no new migration.

**Structural guard (T6030) — the window is now a TESTED property, not a remembered one.** `test_t6030_migration_window_structural_guard.py` builds a real profile DB at a below-head schema WITH ROWS and drives every hot-path read (quests `_check_all_steps`, games list, projects list, clips lists, overlay-data, gallery downloads, collections summary, exports lists), asserting none raises `OperationalError`. It synthesizes below-head by calling `ensure_database()` (head) then DROPping an EXPLICIT, greppable `POST_V023_COLUMNS` map (v024 poster_filename; v025 slowmo_*; v026 games.shared_by; v027 detections_data; v028 stage/output_key; v029 rotation) and stamping `PRAGMA user_version=23` (FLOOR — prod is ≥v18 and v017-v023 add no columns, so v024+ is the only realistically-missing set). **Auto-extension decision:** it does NOT auto-COVER a future v030 column (profile_db migrations can't be replayed in a lightweight test — v002 needs the PG pool + context, v017-v023 need R2 — so the below-head schema can't be derived by running the registry; CLAUDE.md rule 6 also favours greppable explicitness over registry magic). Instead `test_registry_head_is_audited` asserts `max(MIGRATIONS.version) == HEAD_VERSION_AUDITED`, so the NEXT added migration turns that test RED with instructions to extend `POST_V023_COLUMNS`, confirm the read is `column_exists`-guarded, and bump the constant — the durable warn the one-time T5970 sweep lacked. When you add a column-adding migration, update both constants here. (T5725 bumped `HEAD_VERSION_AUDITED` 29->31 for its DATA-ONLY v031 migration — no column added, so `POST_V023_COLUMNS` was correctly left unchanged; the head-audit alarm still fired and had to be acknowledged. Numbered v031 not v030 because the sibling T5800 branch owns v030 and merges ahead — on that merge, resolve `HEAD_VERSION_AUDITED`/`POST_V023_COLUMNS` to cover T5800's v030 too.)

## Landmines & history
- v017 profile_db migration crashed on `row['col']` under the tuple row factory; fixed positionally and re-migrated — all prod users at v18+. v023 uses `r2_head_object_global` at module level (not inside `up()`) so it's patchable in tests. (profile_db head has advanced well past v023 since — see the track table above for the current latest.)
- **`game_storage.storage_expires_at` write authority**: `auth_db.expire_game_storage()` marks a row as expired (past sentinel `2000-01-01T00:00:00+00:00`). Called by sweep Phase 2 via `_expire_game_storage_all_profiles()` after R2 deletion; each call is followed immediately by `sync_db_to_r2_explicit(user_id, profile_id)` — without that sync the write is lost on next cold-load from R2.
- **Heal path must not resurrect deleted sources**: `_ensure_game_storage_refs` in `games.py` guards ref insertion with an R2 head_object check when R2 is configured. Skipping this guard re-creates a storage ref with a future expiry for a game whose source is gone, making `_compute_storage_status` return 'active' again (bug 27p/29p, T4820).
- Live bugs mapped by the 2026-07-03 audit (docs/plans/audit-2026-07-03-code-quality.md): `exports.py:279` NameError (recovery reports failure after success — T4240); overlay blob decode failure silently becomes `[]` (overlay.py:308-313) then the next gesture's whole-blob RMW persists it = permanent highlight loss (T4210); `clips.py:483-497` remove_segment_split wipes segmentSpeeds (T4220); `projects.py:1275` catch-all NULLs crop_data (T4230).
- `routers/export/` is 5,878 lines with ~300 raw `cursor.execute` across routers; the service split never happened — Export Write-Path epic (T4370-T4410) is the plan. Don't add new pipeline logic to routers.
- `export_worker.py:28-33` imports from a router (inversion, fixed in T4380/E1). Two competing export-job create helpers exist (exports.py:86 vs export_helpers.py:37) with different initial statuses.
- `privacy.py` opens SQLite with no pragmas/busy_timeout (lock errors under sync load) — T4660.
- **`bootstrap.py` runs its two read groups in PARALLEL (T4771):** the user.sqlite group (profiles/credits/settings/quests → `_read_user_scoped`) runs on a worker thread via `loop.run_in_executor(None, lambda: ctx.run(...))` while the profile.sqlite group (projects/games/downloads/exports/pending, via `await list_projects()`/`list_games_metadata()` + `_read_profile_misc`) runs on the event loop. Wall ≈ max(group) not sum (live TTFB ~657ms→~360ms median). **Landmine: the worker thread needs the request contextvars** (user id, profile id, req id) — `bootstrap()` captures them with `contextvars.copy_context()` and runs the sync group via `ctx.run(...)`; a bare `run_in_executor` (or `to_thread` without the copy) would make `get_current_user_id()` raise `RuntimeError: No user context set` inside the thread. Response shape is UNCHANGED (same 9 top-level keys). Reads only — no writes, so no TrackedConnection R2 sync fires. Group timings logged as `[PROFILE bootstrap] user_group=..ms profile_group=..ms wall=..ms`. Test: `tests/test_bootstrap.py` (drives the coroutine via `asyncio.run` — TestClient is broken in this env's starlette/httpx pairing).
- Tests not importing `app.main` must load `.env` manually (memory: backend tests need dotenv).
- **Test-infra hygiene (T5000/T5050/T5060/T6070):** `src/backend/pytest.ini` holds a **scoped `filterwarnings` guard** that errors ONLY the fastapi `on_event` + starlette per-request-`cookies=` DeprecationWarnings (by exact message) so they can't regrow — do NOT broaden it to all DeprecationWarnings. **(T6070: the `local_disk_timing` marker is GONE.** It existed only for `test_session_init_recovery.py`'s wall-clock `assert elapsed < 0.1` fire-and-forget probe, which flaked on the `/workspace` bind mount because it timed the WHOLE `user_session_init` (real sqlite/Postgres work), not just the scheduling — two false regression alarms (T5970/T6030). That assertion is now state-based: the recovery spy sleeps before setting a `gate`/recording its arg, and the running-loop path schedules via `create_task`, so immediately after `user_session_init` returns the gate must be unset — deterministic regardless of disk speed, so the test runs everywhere with no marker/deselect. Do NOT reintroduce wall-clock timing asserts; assert the property.) Throwaway-PG note: the `pg_conn` fixture is schema-first (`_SCHEMA_DDL` before the migration RUNNER), so a brand-new throwaway Postgres needs no manual `init_pg_schema()`. `test_ai_upscaler.py` is guarded by module-level `pytest.importorskip("torch")` (torch-less env skips, doesn't abort collection).
- **Test-context landmines:** (1) pg-backed test classes that call `insert_game_storage_ref`/other `get_db_connection` helpers must `set_current_user_id()`+`set_current_profile_id()` in their fixture (get_db_connection reads the CURRENT context, not the passed user_id) — else `RuntimeError: No user context set`; isolate the incidental SQLite in `tmp_path` (mirror `temp_auth_db`), and restore context on teardown so it can't leak under full-suite order (T5050a). (2) Tests calling an async handler directly must use `asyncio.run(handler())`, NOT `asyncio.get_event_loop().run_until_complete()` — the latter depends on an ambient current loop a prior async test can leave cleared → "RuntimeError: There is no current event loop" only under full-suite order (T5050c). (3) Frontend: `profileStore.test.js` must mock the `./index` store barrel + `ExportWebSocketManager`, else `switchProfile`/`createProfile` dynamically import the real 12-store graph and flake on timeout under parallel vitest load (T5050b).
- **T4790 lint-backlog sweep found 3 latent NameErrors** (ruff F821, all fixed): `exports.py:279` `presigned_url` in `finalize_modal_export` (== T4240 from the 2026-07-03 audit — recovered-export finalize always fell into `except`, returned `finalized=False`; fix drops the undefined key, URLs are generated on-the-fly not stored); `export/multi_clip.py` `canonicalize_segments_data` used in `_run_multi_clip_background` but the import lived only in the sibling `export_multi_clip` fn (crashed whenever a clip had gesture-saved `segments_data`); `games_upload.py` `generate_presigned_url_global` used at 2 sites without importing it from `app.storage`. Regression tests: `tests/test_t4790_undefined_name_bugs.py`.
- **ruff `--fix` pitfall (T4790):** the `Optional[X] -> X | None` rewrite is UNSAFE for `Optional[callable]` — `callable` is the builtin FUNCTION, and `callable | None` is evaluated at def-time in py311 (no `from __future__ import annotations`), raising `TypeError: unsupported operand for |`. `typing.Optional` masked it (lazy). Fixed to `Callable` in `ffmpeg_errors.py`. After any bulk ruff autofix, `python -c "from app.main import app"` is necessary but NOT sufficient — grep new `<lowercase-builtin> | None` annotations.
- **Lint regression gate (T4790):** `.github/workflows/branch-ci.yml` now fails a branch if `ruff check app` exceeds 261 errors or `eslint src` exceeds 0 errors / 1019 warnings (on top of the existing changed-file lint). Baselines are ratchets — lower them as the remaining backlog is cleared, never raise. The remaining 261 ruff errors are all unsafe-autofix or manual-only rules; `UP042`/`SIM105` are config-ignored in `ruff.toml` (deliberate team choices).
- **`scripts/copy_user_between_envs.py` copies in TWO halves that are NOT atomic (T6150):** (1) Postgres rows, then (2) R2 objects — `profile.sqlite`/`user.sqlite` + media — mirrored in-bucket (only the `{env}/` prefix differs). The DB half of a profile lives INSIDE the copied `profile.sqlite` (an R2 object), so it only becomes authoritative when the destination backend cold-loads it from R2. **Copying into a LIVE destination half-applies silently:** `ensure_database` (`app/database.py` ~657) restores from R2 on FIRST ACCESS ONLY (`if local_version is None`; a per-request HEAD costs 20s+), so a live machine never notices R2 moved — it keeps serving its OWN `profile.sqlite` and, on its next write, the local-ahead CAS guard re-uploads that stale copy OVER the one just copied in. Net result observed on staging: the 47 mp4 objects landed but the DB half reverted, leaving `final_videos`/`working_videos` rows ("Brilliant Control", project 31) pointing at mp4s the freshly-copied object store never contained → 404 → black player, while the tool reported success. Prod was never affected (it was the source). **Ordering requirement: STOP the destination's Fly machines BEFORE copying** (makes R2 the sole authority so the cold-load on restart picks up the copied DB); the T6160 recovery gap is filed separately — do NOT rely on a running machine noticing the swap. Two guards now in the script: (a) `main()` refuses `--to staging/production` unless `--dest-machines-stopped` is passed (dry-run exempt) — this is the RELIABLE fix; (b) `verify_media_refs()` post-copy downloads each authoritative `profile.sqlite` and asserts every `final_videos.filename`→`final_videos/{fn}` and `working_videos.filename`→`working_videos/{fn}` key exists (fatal), posters `final_videos/posters/{poster_filename}` warn-only (`poster_filename` already ends in `.jpg`; column absent on pre-migration DBs → skipped). Key scheme = `storage.py:r2_key` = `{env}/users/{uid}/profiles/{pid}/{path}` — omitting that prefix 404s everything and produces a false audit. **`verify_media_refs` is defense-in-depth, NOT the primary fix:** it runs synchronously right after copy, but the live-clobber is asynchronous (can land after the script exits), so only guard (a) reliably prevents the incident. Same script has a history of silent partial copies — missing `user_segments` once made an account invisible in the admin dashboard (fixed earlier); this is the media instance of that systemic pattern. Tests: `tests/test_t6150_copy_verification.py`.

## Edge (Cloudflare Pages Functions)
- **T4840** `src/frontend/functions/shared/[token].js` — edge-rendered public share page. Matches ONLY single-segment `/shared/{token}` (`/shared/collection/*`, `/shared/teammate/*` are two segments -> never match -> keep hitting the SPA via `public/_redirects`). Fetches `GET {api}/api/shared/{token}` server-side (API base by hostname: `app.reelballers.com`->prod, else staging), edge-caches the JSON via Cache API keyed by token ONLY when `is_public===true` (TTL 600s < 4h presign), renders a self-contained <15KB HTML page (muted-autoplay `<video>`, OG/Twitter meta, preconnect, escaped interpolations), and fires a `waitUntil` POST to the `/viewed` beacon on every render. Non-public / 403/404/410 / error / 2s-timeout -> `env.ASSETS.fetch(request)` SPA fallthrough (returns index.html at 200, URL preserved -- do NOT fetch `/index.html` directly, ASSETS 308-canonicalizes it to `/`). **Never make a share less accessible than today -> fallthrough on any doubt.** Deploy: wrangler auto-bundles `functions/` from cwd `src/frontend` (both `.github/workflows/deploy-frontend.yml` and `scripts/deploy_production.sh` already `cd src/frontend` + `wrangler pages deploy dist` — no script change). Revoke latency: up to the 10-min cache TTL for public shares.

- **T5720** `src/frontend/functions/shared/game/[token].js` — edge-rendered PUBLIC GAME LINK watch page ("here's the game" broadcast link). Two-segment `/shared/game/{token}` (no collision with the single-segment `[token].js`). Clones the T4840 structure: fetches `GET {api}/api/shared/game/{token}`, edge-caches when `is_public===true && recap_url` (TTL 600s), renders muted-autoplay **team recap** + static clip rail + an **"Add this game to your account"** CTA → `/claim/game/{token}` (the T5730 seam; token in the path, nothing auto-materializes on auth). Any doubt (missing `recap_url`, 410 revoked, timeout) → SPA fallthrough. SPA route: `SharedGameView.jsx` (App.jsx `gameShareToken` detector, no auth).
  - **Anonymous-scope guarantee is STRUCTURAL.** The public resolve endpoint returns `PublicGameLinkResponse` (routers/shares.py) — a Pydantic model with **NO `game_url`/`game_blake3`/`video_warm_url` field**, so a future careless edit that computes a full-game value has nowhere to serialize it (FastAPI drops undeclared fields at the boundary). Anonymous visitors watch the **TEAM RECAP ONLY** (EPIC decision 3). Test teeth: `test_t5720_public_game_link.py::test_response_model_has_no_game_source_field` goes RED the instant such a field is ADDED. **NEVER add `game_link` to the teammate resolve/poster `share_type` tuples** (shares.py:382/:569) — those carry `game_blake3`/`video_warm_url` and are the leak site; `game_link` resolves/posters ONLY through its own `/api/shared/game/{token}[/poster.jpg]` endpoints.
  - **share_type audit result:** `game_link` is a public link that lives until revoked. Added ONLY to `SHARE_TYPE_TO_CHANNEL` (sharing_db.py, referral attribution). Deliberately EXCLUDED from `cleanup_old_shares` (never auto-swept), teammate resolve/poster (leak site), and the `annotation_playback`/`collection` branches. Row shape: `share_type='game_link'`, `recipient_email` = sharer's own email (self-share convention; public link has no recipient), `share_games.clip_names` = the frozen TEAM rail (`{name, recap_start, recap_end, player_tags}` snapshot, read only by game-link resolve so the public path opens ZERO per-profile SQLite), `share_games.game_date` = date snapshot (v020). Create is idempotent per (game, sharer) via `get_active_game_link_share`; revoke is game-scoped (`revoke_game_link_share` sets `revoked_at` → 410, NOT an `is_public` PATCH). Stitch-on-share: `POST /api/games/{id}/share-link` runs `ensure_recap(...,'team')` + `warm_recap_poster(...,layer='team')` before returning; `ensure_recap` status `'empty'` → 409 `empty_team_layer` ("Tag some team plays first"), no row written. Expired game SOURCE degrades to recap-only playback (recap survives; NOT T3970's hard block). **DDL:** v020 widened the `shares.share_type` CHECK to include `'game_link'` AND added `share_games.game_date` — migrations do NOT auto-run; admin must hit `POST /api/admin/migrate` after deploy.

- **T5730** claim & import flow (closes the game-link growth loop). Endpoint `POST /api/shared/game/{token}/claim` (`routers/shares.py`, beside T5720's resolve routes). **Auth is enforced IN the handler**, not by middleware — `/api/shared` is in `AUTH_ALLOWLIST_PREFIXES`, so an unauthed request reaches the handler with no user context; the handler calls `_get_user_id_from_request(request)` and 401s if absent (a claim is a signed-in GESTURE — never a silent materialize on auth, EPIC decision 8). Body = frozen DTO `ClaimGameRequest {share_token?, import_annotations=True, target_profile_id?}` (path token authoritative; `share_token` carried only for the shared identifier). Errors: 401 unauth / 404 unknown-or-not-game_link / 410 revoked / 400 missing-or-foreign profile / 503 `sync_failed` (RefreshFailed → retryable, never a lying 200). Profile resolution: explicit `target_profile_id`, else the sole profile (single-profile accounts skip the pick), else 400.
  - **Orchestration: `materialization.claim_game_link(share, claimer_user_id, claimer_profile_id, include_annotations, sharer_email)`.** It (1) short-circuits on the `share_claims` idempotency row (already-imported-annotations OR game-only re-claim → returns the SAME `local_game_id`, no re-materialize; game-only→annotations upgrade re-materializes into the SAME profile the game first landed in), (2) `ensure_profile_db_local(sharer...)` read-only-pulls the SHARER's profile.sqlite from R2 (the claimer is a different user, so it's usually not cached locally — `materialize_game_share`'s `_open_profile_db` is local-only), (3) for `include_annotations` reads the sharer's TEAM-layer clips via `team_layer_clips_for_game(conn, game_id)` (`WHERE my_athlete = 0` exactly — NULL/1 are My-Athlete and NEVER cross), (4) calls `materialize_game_share(..., tag_name="", clip_data=<team clips or []>)`. **tag_name MUST be falsy** so the referral attributes via the share's own type (`SHARE_TYPE_TO_CHANNEL['game_link']` = `"game_link_share"`), not `"teammate_share"`. (5) upserts `share_claims`.
  - **T5330 invariant (THE correctness property):** routing through `materialize_game_share`/`_copy_game`/`_materialize_clips` stamps a NON-NULL `shared_by` (`sharer_email → sharer_user_id → "lost"`) on the copied game AND every clip → onboarding quests stay blind to imported content. Imported clips land `my_athlete=0` (recipient Team layer). Asserted column-level both paths in `tests/test_t5730_claim_import_flow.py`.
  - **Storage refs:** `claim_game_link` does NOT fabricate a `game_storage` ref. `materialize`'s `_create_storage_refs` runs under the CLAIMER's request context (`get_game_storage_ref`/`insert_game_storage_ref` key off the ContextVar, not their args), so it reads the claimer's own (empty) `game_storage` → no-op. Source availability is resolved honestly by the head-object-guarded heal path (`_ensure_game_storage_refs`, T4820) on the claimer's next game-list load: live source → active, gone source → recap-only degradation. This is the intended "no fabricated storage refs" behavior.
  - **Schema:** new Postgres table `share_claims` (`id, share_id→shares, claimer_user_id, claimer_profile_id, include_annotations BOOL, local_game_id, claimed_at`) — powers idempotency + the T5740 funnel. `UNIQUE(share_id, claimer_user_id)` (a re-claim UPDATEs in place, `include_annotations` OR-ed so it never downgrades). Helpers `get_share_claim`/`record_share_claim` in `sharing_db.py`. **DDL: v021 `share_claims` + `_SCHEMA_DDL` (both).** Migrations do NOT auto-run — admin hits `POST /api/admin/migrate` post-deploy. `test_migrations.py` postgres count lock bumped 20→21.
  - **Frontend:** SPA route `/claim/game/{token}` → `ClaimGameView.jsx` (App.jsx `claimGameToken` detector, rendered BEFORE the auth wall so signed-out users get the sign-up CTA and signed-in users get the import dialog; token in the URL path survives the signup reload). QuestPanel suppression reuses the existing `shared_annotation_flow` sessionStorage flag (App.jsx clear effect now also gates on `!claimGameToken`). Post-import landing = the claimed game's recap (`initialTab='team'`) + a one-time "tag your athlete's plays" nudge, driven by the `pendingRecap` breadcrumb consumed in `ProjectManager.jsx` once games load — NOT Annotate. No reactive `useEffect`→claim; the claim fires only from the Confirm gesture.

- **T5740** per-recipient clip scope on the email game share + read-only share funnel. NO new share entry point / native share sheet (user-rejected) — `ShareGameModal.jsx` stays the single share surface, restructured Google-Docs-style: **People with access** (per-recipient scope dropdown + expandable clip preview) + **General access** (the T5720 public link + revoke confirm/revoked state).
  - **Wire contract change (single shape, no back-compat):** `POST /api/games/{id}/share` body went from `{emails: list[str]}` → `{recipients: [{email, scope}]}`, `scope ∈ ShareClipScope {all_team, tagged_only, game_only}` (`app/constants.py`; default `all_team`). `ShareGameModal` is the ONLY caller (SharePlaybackDialog → `/share-playback`, unchanged). Pre-T5740 tests that posted `{"emails":[...]}` were migrated to the `game_only` scope (faithful to their game-only intent) — no legacy `emails` branch exists.
  - **Clip selection is ONE resolver:** `materialization.resolve_scoped_clips(conn, game_id, email, scope)` — ALL_TEAM → `team_layer_clips_for_game` (`my_athlete=0`); TAGGED_ONLY → `_filter_clips_for_tag` per tag from `_tag_names_for_email` (the profile-DB `teammate_emails` table, case-insensitive), **INTERSECTED with the team-layer clip ids** so a My-Athlete clip that happens to carry the tag NEVER crosses (EPIC 1/3 — `_filter_clips_for_tag` alone does NOT filter by layer, this is the guard); GAME_ONLY → `[]`. Same resolver feeds BOTH the send path (`share_game`, computed once up front on the sharer conn AFTER the T3970 expiry 410) AND the plain read `GET /api/games/{id}/share-preview?email=` → preview can't lie about what's sent (asserted in `test_t5740_share_scope.py`). Scoped clips flow into `materialize_game_share(clip_data=...)` and `serialize_clip_data(...)` for the pending-share / multi-profile / RefreshFailed fallbacks. `tag_name` stays None on the game-share `materialize`/`create_game_share` call (channel unchanged; scope is expressed only by `clip_data`). T5330 `shared_by` NON-NULL holds for every scope (reuses the copy path, asserted column-level).
  - **T5745 — `_materialize_clips` overlap merge is LAYER-AWARE (behavior-only, no schema).** Incoming share clips are always Team layer (`_insert_clip` hardcodes `my_athlete=0`). An incoming clip merges with an existing clip **only when the existing clip is ALSO Team layer** — `if _is_team_layer(ex) and clips_overlap(ex, clip)`, where `_is_team_layer(clip)` is an explicit `clip.get("my_athlete") == 0` (layer semantics: NULL/1 = My Athlete, 0 = Team; NULL is My Athlete, NEVER a truthiness check on a possibly-NULL value). A cross-layer intersection falls through to a plain INSERT so the recipient's own My-Athlete clip and the imported Team clip **coexist as two rows**. The UPDATE **no longer writes `my_athlete`** (a merge must never change a row's layer; two Team rows leave it 0 anyway). Before this fix the merge was layer-blind (`_get_existing_clips` didn't even SELECT `my_athlete`) and forced `my_athlete=0` onto the survivor — every claim converted the recipient's intersecting My-Athlete clip to Team, stretched its bounds to the union, and swallowed the team clip (same play = same timespan = guaranteed intersection once claims import Team clips onto plays the owner tags for their own athlete). Same-layer Team-vs-Team merge STILL merges (re-claim idempotency, `tagged_teammates` union, no duplicate reels — the must-not-regress property). Tests: `tests/test_t5745_layer_aware_merge.py`.
  - **Funnel = READS of existing data, no new tables/writes (count stays 21).** `share_created` already exists as T5720's `share_completed` milestone at link creation; `share_viewed` already emitted by T5720; claims already in `share_claims`. `GET /api/admin/analytics/share-funnel` (`routers/admin.py`, `_require_admin`) returns per `game_link` link: **views** (`analytics.share_view_counts(sharer_user_id, tokens)` reads the sharer's per-user `user_action_log` for `share_viewed` rows whose `context.share_token` matches, via `json_extract`; grouped per sharer so each user.sqlite opens ≤once; returns `None` = "unavailable" ≠ 0), **claims** (`COUNT(DISTINCT claimer_user_id)` on `share_claims`), **activated** (claimers with an `export_completed` `user_actions` row). Frontend: `admin/ShareFunnelTable.jsx` + "Share Links" sub-tab in `AnalyticsDashboard.jsx` + `fetchShareFunnel` in `adminStore.js`. Analytics never on a user-facing path (admin read only).
  - **Modal UX:** preview fetched from the ADD gesture (`handleEmailsChange` diffs added/removed emails), NOT a reactive `useEffect` → no reactive-persistence violation. Zero-clip tagged-only recipient is warned BEFORE send twice (inline amber row warning + send-time banner); send stays enabled (game-only is legitimate). Revoke goes through a confirm dialog (backdrop inert — no-backdrop-dismiss rule; Escape closes the confirm first, not the whole modal) → revoked state with "Create new link". Approved verbatim scope labels live in `src/constants/shareClipScope.js`: "All team clips" / "Only clips they're tagged in" / "Game only (no clips)".

## Enumeration consistency (T5110 / T4970)
- **Poster backfill migrates each profile to head before querying (T5110; re-pointed T5085).**
  `backfill_posters` (poster.py) enumerates via UNFILTERED `_get_profile_ids` (includes orphan
  profiles the retired bulk runner used to registry-skip, T4830). A below-head/orphan profile
  missing `final_videos.poster_filename` made the `WHERE poster_filename IS NULL` query raise
  `no such column` and ABORT THE WHOLE RUN (prod 2026-07-13). Original fix (T5110): call
  `_migrate_profile_db` per profile after `ensure_database()`. **T5085: re-pointed at
  `migrate_local_profile_db_at_seam`** (the JIT seam primitive T5083 built) — `_migrate_profile_db`
  is the bulk-runner primitive T5087 deletes, kept here (not removed) only because the seam inside
  `ensure_database()` is gated on `R2_ENABLED` and this call is what migrates a below-head profile
  in local/no-R2 dev mode (`test_t5110_poster_backfill_below_head.py` pins it). Also: the
  `ensure_database()` call itself is now the JIT seam and can raise `MigrationBlocked` — wrapped in
  its own try/except so one blocked profile doesn't abort the whole backfill (a gap T5110 didn't
  need to guard against, since `ensure_database()` couldn't raise before T5083). The candidate
  query stays wrapped in try/except so a still-below-head/corrupt profile is recorded in
  `result["failed"]` and the sweep continues either way. Any admin sweep that iterates
  `_get_profile_ids` + queries a migration-added column must do the same (see
  `sweep_scheduler.do_sweep`, `auto_export.backfill_hiq_recaps`, both similarly guarded — T5085).
- **`get_all_users_for_admin()` has NO segment join; `list_users` uses LEFT JOIN (T4970).**
  `user_segments` rows are created ONLY in the OAuth/OTP signup flows (`create_user_segment`,
  guarded by `is_new`); test-login (auth.py:877), the test seam, and copied accounts create
  users via `create_user` with NO segment row BY DESIGN. So enumeration must never inner-join
  `user_segments`. `get_all_users_for_admin` (poster-backfill/migrations iterator) already had
  no join. The `GET /api/admin/users` `list_users` endpoint INNER-joined and dropped segmentless
  users (5 listed, not 6) → now LEFT JOIN on both COUNT + page query; segment fields come back
  NULL and the frontend UserTable already renders NULLs as `—` (T4870 null-not-fabricated). An
  origin/date filter still legitimately excludes NULL segments.

- **Engaged-time / admin "Usage" model (T5660).** `total_usage_seconds` on `user_segments`
  measures engaged time. ONE shared cap constant `analytics.SESSION_IDLE_CAP_SECONDS = 1800`
  is BOTH the new-session boundary AND the ceiling on the unconfirmed idle tail. ONE shared
  helper `analytics.session_engaged_seconds(session_start, last_active, now=None)` is used by
  BOTH the write side (banking) and the read side (`admin.py` list_users estimate) — this
  symmetry is the whole point: the pre-fix asymmetry (uncapped write vs capped read) inverted
  heavy-continuous vs come-and-go users (D1). Rules: **confirmed span** (`last_active -
  session_start`) is UNCAPPED (the 30-min new-session boundary already bounds every internal
  gap ≤ cap, so a heavy user isn't clamped); **idle tail** (`now - last_active`) is credited
  only while ≤ cap, else trimmed to ZERO (not clamped to cap — clamping would itself over-count
  idle, D4). `now=None` banks an ended session with no tail. Banking happens on: new-session
  transition in `update_session`, `close_session` (now BOTH logout AND the tab-close beacon).
  Endpoints (auth.py, cookie-resolved via `validate_session`, return 204): `POST
  /api/auth/heartbeat` (reuses `update_session` — foreground keep-alive so a long session's
  `last_active` stays fresh; per-tick gap capped by the same new-session boundary so a
  backgrounded tab can't inflate) and `POST /api/auth/session-close` (reuses `close_session`,
  idempotent via its open-session guard — banks the last session without a logout, fixes D2).
  Frontend: `src/frontend/src/hooks/useSessionHeartbeat.js` (wired in App.jsx next to
  useExportRecovery) — visibility-gated 60s heartbeat + `navigator.sendBeacon` close on
  `visibilitychange→hidden`/`pagehide`. Admin UI `UserTable.jsx fmtDuration` renders hours-only
  (no days branch: `26h`, not `1d 2h`). Decisions: NO backfill (values self-correct as new
  sessions accrue), all-time cumulative window (unchanged). Tests: `test_analytics.py`
  (`TestSessionEngagedSeconds`, `TestCloseSessionBanking`, `TestHeartbeatGapCap`).
- **Admin weekly-usage columns: Avg/wk + Last 7d (T5770).** Two metrics, two
  storage strategies (no-redundant-state rule). **`avg_weekly_seconds`** is
  DERIVED at read time in `admin.py list_users` — `effective_usage / max(1,
  weeks_since_signup)` — and stored nowhere; `effective_usage` is the SAME
  value the Usage column shows (all-time total + any still-open live session).
  Weeks-since-signup uses `user_segments.acquired_at`, falling back to
  `users.created_at` when the LEFT JOIN segment is NULL (there is no
  `signup_completed_at` column on `user_segments` — that name lives only on
  the separate `user_milestones` table from v005; do not confuse the two).
  Clamped to a minimum of 1 week so a brand-new signup neither divides by zero
  nor produces an absurd average. **`last_7d_seconds`** is REAL bucketed data:
  new table `user_usage_daily(user_id, day, seconds)` (postgres v022 — see
  Migration system table), summed over `day >= CURRENT_DATE - INTERVAL '6
  days'` as ONE grouped query keyed by the page's user ids (joined into the
  existing admin list query, no per-user N+1). History is NEVER backfilled —
  the column honestly under-reports for ~7 days after this migration runs,
  same wait-and-self-correct decision as T5660's Usage column.
  **Single write path:** `analytics.add_usage_seconds(cur, user_id, seconds)`
  bumps `user_segments.total_usage_seconds` AND the `user_usage_daily` bucket
  (`ON CONFLICT (user_id, day) DO UPDATE SET seconds = seconds + %s`) on the
  SAME cursor passed in by the caller, so both numbers move inside the
  caller's existing transaction — they can never disagree. Both prior
  `total_usage_seconds` write sites (the session-rollover branch in
  `update_session` and `close_session`) now call this helper instead of
  writing the column directly; a 0-second call (an instant open/close) is a
  no-op that writes no empty bucket row. Day = `CURRENT_DATE` at write time —
  a session spanning midnight attributes its whole increment to the day the
  increment lands, not split across days (acceptable for admin metrics, noted
  not engineered around). **Impersonation:** already covered by the existing
  T1515 guards in `update_session`/`close_session` (both early-return on
  `get_current_impersonator_id()` BEFORE reaching the new helper), so an
  admin browsing as a user cannot inflate either store — no separate guard
  was needed in `add_usage_seconds` itself. Tests: `test_t5770_usage_daily.py`
  (helper atomicity, ON CONFLICT accumulation, both write sites, impersonation
  no-op, endpoint fields, weeks-clamp, bounded-query perf guard via a
  psycopg2-execute counter — the project's `query_counter` fixture wraps
  SQLite only and can't see this Postgres path).
- **Stripe revenue reconciliation (T5760).** Stripe is the source of truth for money;
  `user_segments.total_spent_cents` is a local cache written at fulfillment (admin-view
  speed — no per-page-load Stripe latency). **`total_spent_cents` is NET of refunds AND
  lost disputes/chargebacks** ("what the user paid us and we kept" — Stripe's Revenue
  Recognition standard; contra-revenue for both). Reconciliation is **on-demand only, NEVER
  on the `list_users` load path** (Stripe latency isolation — the reason the local column
  exists). Pure classifier lives in `app/services/revenue_reconciliation.py`
  (`build_stripe_net_by_user` + `classify_users` are pure over fetched data → unit-testable
  with mocked Stripe; only `fetch_stripe_intents` touches network — `PaymentIntent.list`
  auto-paginated, `expand=['data.latest_charge','data.latest_charge.dispute']` to avoid N+1;
  **status is filtered client-side** — `PaymentIntent.list` has no server-side status param).
  Group by `metadata.user_id` (on the PI, never customer id — wiped by user_db v007). Per
  user `net = Σ(amount_captured - amount_refunded - lost_dispute_amount)`; subtract a dispute
  ONLY when `dispute.status in {lost, charge_refunded}` (won/open never subtracted; open →
  `has_pending_dispute`). **The lost-dispute amount is netted against the charge's refund**
  (`max(dispute.amount - amount_refunded, 0)`): a `charge_refunded`-status dispute sets BOTH
  `amount_refunded` and the dispute amount for the same money, so subtracting both would
  double-count and drive net negative. `DriftCause` priority: aligned → `test_mode_era` (pi_count==0 &
  local>0, zero live history = pre-go-live 2026-07-22 noise) → `dispute` → `refund` →
  `unknown`. Endpoints (admin.py, both `_require_admin()` + `_require_stripe_configured()`):
  `GET /api/admin/revenue-reconciliation` (report: rows drifted-first + summary), `POST
  /api/admin/revenue-reconciliation/heal` (`{user_ids?|all_drifted}` — recomputes Stripe net
  server-side, never trusts a client amount, sets `total_spent_cents` via
  `analytics.set_total_spent`; explicit admin gesture, never automatic). Heal helpers next to
  `increment_total_spent`: `set_total_spent` (returns prior value) + `decrement_total_spent`
  (reads-then-writes, floors at 0 with a WARNING — never persist a negative aggregate).
  **Webhook hardening:** `charge.refunded` branch in payments.py decrements at refund time
  (delta = newest `charge.refunds.data[0].amount`, fallback cumulative `amount_refunded`;
  user_id via `charge.metadata` then `PaymentIntent.retrieve` since we set metadata on the PI
  not the Charge). **OPERATOR STEP:** add `charge.refunded` to the LIVE-mode webhook endpoint
  in the Stripe dashboard (events are per-endpoint + per-mode) or it never fires. Dispute
  webhooks are a deliberate follow-up (the on-demand pass is their safety net). Frontend:
  `components/admin/RevenueReconciliation.jsx` (drifted-only table + per-user/all "Adopt
  Stripe value" heal, collapsed section — Stripe pass runs only on explicit click). Tests:
  `test_revenue_reconciliation.py`. No schema change / no new table (computed on demand).
- **Credit packs single-sourced + ~5c repricing (T4940, 2026-07-25).** `CREDIT_PACKS`
  (`payments.py:68`) repriced to the sub-$1-per-clip ladder **starter 80/$3.99, popular
  160/$6.99, best_value 340/$12.99** (starter = worst-case 4.99c/credit, best_value =
  3.82c/credit). `GET /api/payments/config` now returns
  `{publishable_key, packs[]}` — the **only** source of pack pricing; frontend
  `BuyCreditsModal.jsx` renders packs from it (the old duplicate frontend `PACKS` array is
  gone; only presentational icon/badge stay client-side in `PACK_META`). Grants read pack
  metadata off the Stripe session/PI, not the constant, so existing balances + in-flight
  payments are unaffected by a reprice. `storage_credits.CREDIT_VALUE` → **0.05** (worst-case
  $/credit, was 0.072) — the frontend mirror `utils/storageCost.js` must stay in lockstep or
  the upload preview disagrees with the charge. Lower CREDIT_VALUE ⇒ more credits per GB (a 4GB
  game 2→3 cr incl. surcharge), still cost-recovering (R2 cost × 1.10 margin). Usage history:
  `GET /api/credits/transactions` (unchanged) surfaced via `CreditHistoryModal.jsx` (running
  balance walked back from the authoritative current balance; deductions store negative
  `amount`, grants positive). "1 credit = 1 second of exported video" rule stated on every
  credit surface. Tests: `test_t4940_pack_pricing.py`, `BuyCreditsModal.test.jsx`,
  `CreditHistoryModal.test.jsx`. **OPERATOR follow-ups:** (a) create GitHub secret
  `VITE_STRIPE_PUBLIC_KEY_STAGING` — `deploy-frontend.yml:29` now references it instead of a
  hardcoded `pk_test_`; (b) overlay-render GPU cost remains an unmeasured operator measurement
  (see modal-gpu.md) — overlay stays free per product decision; (c) prior test-mode-era credit
  grants (T5760 `test_mode_era` classification) — documented decision, no cleanup executed.
- **`GET /api/version` + `AppVersionHeaderMiddleware` + `POST /api/sync/flush-verify` (T5070).**
  Backend advertises its build id as `X-App-Version` on every response — middleware added LAST
  (outermost) so the header survives 401s/preflight; CORS `expose_headers` includes it. Value =
  `COMMIT_SHA` build-arg (Dockerfile ARG/ENV; deploy-backend.yml passes `github.sha`,
  deploy_production.sh passes `git rev-parse HEAD`), `version.py` falls back to `"dev"` locally.
  Consumed by the frontend update-gate version handshake (see persistence-sync.md § T5070).
  `POST /api/sync/flush-verify` (health router) is the update-gate's step-3 durable-flush barrier:
  checkpoints WAL + awaits R2 upload → **200** clean / **503** still-pending / **401** unauthenticated
  (the client treats 401 as a no-op, not a failure — logged-out users have nothing to flush).

## Deploy + build-number lockstep (T6220)
Follow-up from T6210 (which made a server-ahead-of-bundle update-gate loop *structurally impossible*
regardless of build-number accuracy — see appVersion.js's Tbug41s comment). This section is the
*belt* that keeps the numbers themselves accurate, on top of that *brace*; if this section's
machinery is ever dropped, T6210's fix must stay intact — the reverse is not true.

**The invariant:** a master push (or a prod deploy) never leaves the deployed bundle's build number
and the backend's reported build number unequal — either both halves deploy from the same commit, or
neither does. Drift used to be routine (measured on staging 2026-07-30: bundle #3163 vs backend
#3165, zero frontend files in between) because the two workflows deployed independently on disjoint
path filters; a backend-only commit raised the server's number with no matching bundle published.

**Where the number comes from (two independent computations, kept in agreement by comment, not by
sharing code):** `git rev-list --count HEAD` — `src/frontend/vite.config.js` bakes it into the bundle
as `__APP_BUILD__`; `src/frontend/generate-version.js` (runs via `prebuild`/`predev`) computes the
SAME expression independently and writes it to `public/build.json` (`{build, commit}`), which Vite
copies verbatim into `dist/` — a fetchable fact, since scraping `__APP_BUILD__` back out of a hashed,
minified bundle would be fragile. `.json` is outside `vite.config.js`'s `workbox.globPatterns`, so the
service worker never precaches (and never serves stale) `build.json`. The backend gets its number as
the `APP_BUILD` build-arg (`deploy-backend.yml` passes `git rev-list --count HEAD` computed in CI;
`deploy_production.sh` computes it locally the same way), read by `app/version.py` and served via the
`X-App-Build` header + `GET /api/version`'s `build` field (see the T5070 entry just above). Both
workflows keep `fetch-depth: 0` on checkout (Tbug40p) — a shallow clone would bake in `1` and can
never match.

**Union path filter (staging), not "always deploy":** `deploy-frontend.yml` and `deploy-backend.yml`
carry the SAME `paths: [src/frontend/**, src/backend/**]` block (plus `workflow_dispatch:`) — byte-
identical by convention, verify with a diff after editing either. A commit touching EITHER half now
deploys BOTH from that commit, so the numbers move together; a commit touching NEITHER (this repo's
frequent `docs(plan): ...` commits) deploys NEITHER, so both stay at the same older — still equal —
number, with no redundant CI. **Known, documented, unfixed hole:** a manual `workflow_dispatch` run
still fires only that one workflow, so a deliberate single-half manual dispatch breaks lockstep on
purpose — that's an operator's call, not a bug (commented in both YAMLs).

**Asymmetric prod policy (`deploy_production.sh`), not a symmetric block:** `--frontend-only` stays
allowed, unchanged — a bundle AHEAD of the server is harmless by construction:
`appVersion.checkServerVersion` (src/frontend/src/utils/appVersion.js) raises the gate only when
`serverBuild > clientBuild`, so `server <= clientBuild` can never fire it. `--backend-only` is the
dangerous direction (server ahead, no bundle to load — the T6210 stranding precondition) and now
REFUSES by default; it requires the explicit `--accept-build-drift` flag to proceed (order-
independent relative to `--backend-only` — the old single-`$1` `case` was replaced with a proper arg
loop). Do not "helpfully" symmetrize the two flags; they are not equivalent.

**The assertion:** `scripts/verify-build-lockstep.sh --frontend-url <site-root> --backend-url
<.../api/version>` fetches `<frontend-url>/build.json` and `<backend-url>`'s `build`, polling (both
halves deploy concurrently and Cloudflare Pages propagation lags) every 10s up to 300s by default
(overridable via `--interval`/`--timeout`), succeeding the instant the two numbers are equal. Exits
non-zero with both numbers (or the specific fetch failure — 404, unparseable JSON, non-numeric field)
named on timeout; no silent "assume ok". Wired into `deploy-frontend.yml` as the last step (covers
the concurrent `deploy-backend.yml` job via polling) and into `deploy_production.sh` at the end, but
ONLY when both halves deployed together in that run — an allowed `--frontend-only` or a drift-
accepted `--backend-only` prints an explicit "skipping lockstep assertion because X" line instead of
asserting the very inequality the operator just chose to accept.

## Active/upcoming work
Bug tier (TODO): T4210 (overlay decode → 500, delete orphaned PUT), T4220-T4270, T4280 (silent-fallback sweep). **T4870 DONE** (admin credits read R2-canonical; silent-fallback removed). Guardrails first: T4290/T4300. Backend consolidations: **T4610** require_admin router Depends, T4620 fetch_or_404 + enums, **T4630** R2StreamProxy service (4 streaming-proxy copies), **T4640** games.py activation/share services (depends on T4360), **T4650** raw_clips write-path consolidation, **T4660** open_sqlite factory + game_display service. Export Write-Path epic T4370-T4410 (strict order: characterization tests → ExportJobRepository → finalize/publish single writer → backend-authoritative export → orchestration move). Full map: docs/plans/PLAN.md § Code Quality & Refactoring.
